# P3 ledger ARCHIVE — the full handoff notes

> Created 2026-07-29. The live `P3-LEDGER.md`'s `done` rows were compacted to
> one-line summaries to cut the per-orient read cost (~27k tokens → a few k);
> this file preserves the ledger VERBATIM as it stood at compaction — every full
> handoff note is here, unedited. Statuses here are frozen; the live ledger is
> the source of truth for current state.

---

# P3 merge ledger — rows for the loop

> Created 2026-07-28 (Fable 5 review session). The merge phases (P3-0..P3-4) were
> tracked as prose in `P3-ROADMAP.md`; this file gives them the row form the loop
> protocol (`docs/ARCHITECTURE.md` §11) orients on. **Orient HERE for merge work**,
> not on `/MIGRATION.md` (wizard's pre-merge ledger, kept for the record).
>
> Statuses: `todo` / `in-progress` / `done` / `blocked(<reason>)` /
> `awaiting-decision` / `awaiting-user` / `awaiting-signoff`.
>
> **The four rules** (P3-ROADMAP + memory `verify-in-the-real-host`):
> tests pass ≠ it works ≠ it shipped ≠ **you can get to it**. Every UI row's
> handoff note must say what the visible door is and how a person reaches it in
> the JUCE WKWebView app — synthetic events and Chromium-only checks do not count
> as reachability evidence.
>
> **Decision protocol for this stretch (user, 2026-07-28):** the user is away;
> decisions queue in `MORNING-DECISIONS-2.md` with options + recommendation and
> DO NOT block the loop. Rows marked `awaiting-decision` are skipped; rows marked
> `provisional(D-n)` may build the recommended option, logged, re-tunable on
> sign-off.

## Done before this ledger existed (for the record)

| id | item | where recorded |
|---|---|---|
| P3-0 | The collapse — one tree in scoopy.git, sources not vendored | P3-ROADMAP "Sequence", commits 3d6fa22 · 765aa24 |
| P3-1 | Universal transport — a grid strip's ⟳ ▸ ↻ ◼ drives the deck | c589211 |
| P3-2 | Master tempo from scoopy — deck params, three tempo modes, djSyncLaw as authority, four defects fixed | P3-ROADMAP "P3-2 — DONE", dad8341 · 62b5fbd · ee56aef |
| — | Strip menu (visible door replacing right-click), session-open failure surfaced, OPFS writeFile atomic-ish | 8b0e9dc · 502bc1d · 0cccde4 |

## The queue

**Loop order** (why: broken-build first per §11, then the two rows that make the
product USABLE at all — audio in the plane, sessions that save — then the honesty
fixes, then the phase's named domains):
P3-F1 → P3-U1 → P3-SES-1 → P3-SES-2 → P3-U2 · U4 · U5 · U6 → P3-2b-1..6 →
P3-U3 → P3-U7 → P3-3-1..2 → P3-4-1..2 → P3-M-1 → P3-F2 → P3-AUDIT.
Push (P3-PUSH) after every green commit.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P3-F1 | fixture | `recorder_drain_test` intermittent SEGFAULT: passed 15× in isolation, SEGFAULTed once under the full 64-test suite run (2026-07-28). Reproduce under load/full-suite, find the race (suspect: RecordService drain-thread vs teardown), fix, and pin | done | **TSan found it first run** (a TSan build of this one test needs no JUCE — cheap, keep the trick): `wz::Deck::ensureCapacity`'s `chunks.push_back` on the service thread REALLOCATES the pointer vector while the render indexes it — the exact defect sl_tape.h had already fixed (reserve-in-reset, never-reallocate, retire-don't-free, null-checked readers); deck.h was the un-upgraded donor. Ported the tape's discipline into `wz::Deck` + engine-side `deckRetireSink` (TapeBank pattern, engineTimeSamples as passage proof). Second TSan pass then flagged the render reading the vector's SIZE member vs push_back — readers in BOTH deck.h and sl_tape.h now go through `chunks.data()[ci]` gated only by the atomic `chunkCount`, never `size()`. TSan 0 warnings ×8; 120 stress runs green; 64 ctest + 1336 vitest + params:check green. Test's fixed /tmp dir → mkdtemp (parallel runs collided). NOT closed: the reset-while-render-mid-walk window both engines document as survivable-not-absent (render adopting storage via atomic pointer is the real fix — P2-KICKOFF note; not this row's scope) |
| P3-F2 | fixture | `browser_prod_test` 4 pre-existing failures — WASM worklet's Emscripten `-sENVIRONMENT` flags (noted in 0cccde4's gate line as pre-existing). **Also `browser_grid_test`: fails on clean main (2026-07-28, timeout waiting `.cmp-play`) — same WASM-worklet species, verify together** | done | One root: the glue regenerated 2026-07-27 detects "worker" by `!!globalThis.WorkerGlobalScope` — a symbol an AudioWorkletGlobalScope lacks — so BOTH the shipped bundle and the dev server fell through to SHELL and aborted at engine boot: **the browser companion has been a mute app since that regeneration.** One shim at the top of `scoopy-worklet.js` (declare the worklet scope AS a worker scope — honest: no DOM, no importScripts fetches, WASM embedded) covers dev and prod, because MODULARIZE runs the detection inside the factory call, after module evaluation — the only reason a source-level shim can work. **SHIP GATE PASSED (every browser plays the built bundle) · GRID GATE PASSED** · all other gates green both tiers. The gate line carries no asterisk any more |
| P3-U1 | build | **The plane never starts the engine sink** — `companionEngine.play/publish` early-return on `!audio.running`; `startEngine()`'s only caller is `CompanionPanel.tsx:316` (a button in a DIFFERENT WebView with its own store). So in the main window, grid transport ⟳ ↻ ◼, scene clicks and every tempo intent are enabled and silently inert. Boot the sink from `PlanePanel`'s boot effect when the native link is present (or auto-start on first grid action), with a visible failure note if start fails | done | `plane/bootEngine.ts` (`autoStartEngine`) called from PlanePanel's boot effect: NATIVE host only — `NativeWorldSink.start()` is gestureless by design, while the browser keeps the companion's click (AudioContext autoplay: an auto-started context sits `suspended` while every call "succeeds"). A failed start lands on the PLANE's note line (the 502bc1d lesson — a store error only the companion panel renders is invisible here), including when the failure carries no message. 5 vitest cases pin host-asymmetry + failure surfacing + idempotence. Bundle REBUILT + webdist fresh + browser_plane_test on the shipped bundle. **Visible door: the ⟳ ▸ ↻ ◼ row on a grid strip in WizardMerged now reaches a RUNNING sink at boot** — but hearing a deck actually play on the user's machine remains the P3-G1 ear check; not claimable headless |
| P3-SES-1 | build | **Native session store — THE FLIP** (P3-ROADMAP "next session's first job"): the merged host's library on real disk, OPFS only in the browser companion | done (provisional D-1) | Built one tier LOWER than the row imagined, deliberately: not an `slSession` document API but **`slFiles` — a contained library FILESYSTEM** (list/read/write/remove/exists/mkdirs under `<takesDir>/../Library`), because `opfs.ts` is already a clean path→store adapter, so routing IT wholesale (`nativeFiles.ts`, active when a JUCE backend exists) flips sessions, SAMPLES and the file browser in one move with the browser path byte-identical and sessionStore.ts untouched. Writes are atomic (stage `.tmp`, verify size, rename — the zero-length landmine class is unmanufacturable), names round-trip verbatim, traversal is refused segment-wise + `isAChildOf` (the web_resources rule), remove = trash-first. Routed through the app's own MergedLink (`slFiles` added to NATIVE_METHODS + COMMANDS schema + the routing test — the P3-2 allowlist lesson applied in advance). Gates: sl_dispatch_test slFiles block (round-trip text+b64 incl. zero bytes, verbatim "Untitled 2", atomic replace w/ no stale tmp, containment ×4, honest refusals), 6+1 vitest (delegation, b64 past chunk boundary, OPFS throw parity, walk/copy ride-along, allowlist), ctest 64/64, vitest 1348, hotframe:check, params:check, bundle + webdist fresh, browser_plane + browser_opfs green. NOTE: browser_grid_test fails PRE-EXISTING on clean main (WASM worklet family, same species as P3-F2 — folded there). Real-host walk (create → save → reopen in WizardMerged) = P3-SES-2 |
| P3-SES-2 | build | Session content into strips end-to-end in the REAL host: New → edit → save → reopen → load into strip, through the visible strip menu | machine-half done / awaiting-user | `tools/browser_session_walk_test.mjs`: drives the BUILT bundle in Chromium against a fake JUCE backend whose slFiles is a server-side library with SlDispatch's exact semantics — New in the companion → `pattern.json` lands non-empty on the native route → the strip menu lists it → click → the strip renders scene pads + the grid row → the library survives a reload. Green first run. What it deliberately is NOT: the real JUCE bridge/WKWebView — **user check remaining: open WizardMerged, sessions ⇱ → New, then ⋯ on a strip → load it; expect scene pads and (after P3-G1's ear check) sound** |
| P3-2b-1 | schema | Tape tempo identity, document side: tape element gains `bpm` / `syncToMaster` / `tempoMode` / `pulseRelation` (grid element parity; map schema migration, existing docs default to sync-off so nothing changes for saved maps); take sidecar gains `bpmAtStart` (MAP-SCHEMA.md:60, spec'd never built) stamped from master BPM at record start | done | Map v5 (migration: sync OFF, bpm NULL — honestly unknown, never inferred in a migration; timePitch = D-3 default, inert while sync off; 4 fixtures). Tape `tempoMode` is `timePitch|timeStretch` only — `tempoOnly` is a step-clock concept a tape has no steps for. Sidecar chain end-to-end: Strip.onRecord sends `bpmAtStart: masterBpm` → slRecord (SCHEMA_VERSION 89) → beginTake → writeSidecar, field OMITTED when unknown (0 would be a lie; parser refuses it too) so pre-v89 sidecars parse unchanged — pinned in recorder_drain_test (C++ emission) + takeLibrary (both parse directions). ctest 64/64 · vitest 1369 · all gates · bundle fresh |
| P3-2b-2 | build | Tape beat length: derive tape `bpm` from loop region + `bpmAtStart` (nearest power-of-two bars heuristic), editable beats/bpm field in the Inspector as the override; loaded-file tapes get manual bpm only | done (provisional D-2) | `inferTapeBpm` (persist/tempo.ts, beside the law it feeds): loop seconds × stamp → raw beats → snap to nearest power of two in LOG space (neighbours 2× apart, so a hand-stopped loop lands unambiguously) → the bpm that makes that count exact over the real audio. HONESTY GUARD: >±20% off every power of two → null, unknown beats confidently wrong. 6 vitest cases incl. the refusal band. PlanePanel auto-fills ONLY a null bpm from the take index (a typed value or prior inference is never re-derived behind the user's back); Inspector gains the editable bpm field, empty = unknown. Loaded-file tapes: manual only, by construction (no stamp). All gates green |
| P3-2b-3 | build | **Tape sync v1 — timePitch (varispeed), zero new DSP**: `mapTempoIntents` tape branch (persist/tempo.ts:111 is grid-only today) → djSyncLaw → `sl_tape_set_rate(master/tapeBpm)`. A tape follows the master, pitch moves with rate — audible product movement, ships before any stretcher | done | `tapeEffectiveRate` resolves through THE LAW (pulse relations: 70 under 140 auto lands 1:2 at rate ~1, not chipmunk 2×; sync owns the MAGNITUDE, the hand keeps the SIGN so reverse survives; timeStretch returns the manual rate — silent varispeed fallback would change pitch behind the user's back, deferred to 2b-5). `applyTempo` pushes every tape every pass (unsynced ones at the manual rate — un-sync restores the hand); `planApply` opens a synced map at the EFFECTIVE rate. Strip UI: SYNC/FREE on the tape row (the grid row's own control), dim without a bpm with a title naming where bpm comes from; rate slider disabled while synced (sync owns it) and the readout shows the rate the engine was SENT. `updateTapeTempo` = updateGridTempo's twin, built WITH the push. +10 vitest (law, ops-inclusion, planApply effective-rate, updated gridless-map premise); layout gate confirms the row fits 340×196. **Ear check on the real host = the P3-G1 walk: record a loop at 120, move the master — the loop follows** |
| P3-2b-4 | spec | Tape stretch spec: instantiate `NativeBusStretcher` (self-contained, channels=2, already on slengine's link line) per tape for `timeStretch` mode; the engage/declick machinery (busHistory_/crossfade, NativeAudioEngineCore.cpp:912-1002) is core-private and must be ported, not shared; loop-wrap discontinuity handling (wrap-aligned block split); ~5120-frame group delay policy per D-3; per-tape CPU (6 texture nodes ×8 tapes) needs a config choice | done | `docs/merge/TAPE-STRETCH.md`. Load-bearing choices: the stage-1 read IS the existing varispeed reader (one reader, two modes — the stretcher only restores pitch, so no second timeline law); declick is a one-block primed-engage + equal-power crossfade OWNED BY THE TAPE TIER (no history ring needed — the material is RAM-addressable, unlike the deck bus's transient input); C-3 always closes DRY, stretch engages on the next tempo intent; wrap-contiguous split reads (a sub-block loop degenerates to per-lap calls, capped by the rate clamp); lazy per-tape stretcher with asyncWarmup; ABI = `sl_tape_set_tempo_mode` + the EXISTING rate setter (mode decides what the one number drives, applyDeckParams' shape) |
| P3-2b-5 | build | Tape stretch build, per the spec row: tape `tempoMode=timeStretch` stretches to master without pitch change; C-3 handoff NEVER engages the stretcher in its same block (loop closes dry; stretch engages on the next tempo intent with crossfade) | done (provisional D-3) | End to end: `Tape.tempoMode`/`stretch` atomics + lazy per-tape `NativeBusStretcher` (2ch, asyncWarmup — render dry until warm; stretchers never freed while the bank lives, spec amendment) · the stage-1 read is the SAME reader at unity magnitude staging into per-tape scratch, wrap handled in the copy so the staged stream is exactly what varispeed would play · engage = floor-playhead + `engagePrimed` from the material just played; one-block equal-power crossfades both ways via a dry leg with playhead save/restore · stretch restricted to LOOPING (oneShot/overdub stay dry — overdub needs zero-latency monitoring) so C-3 closes dry by construction · ABI `sl_tape_set_tempo_mode`/`_tempo_mode`/`_stretch_ready` (declared in the coverage ledger), dispatch `slTape setTempoMode`, SCHEMA_VERSION 90 · TS: mode travels BEFORE rate in applyTempo (the deck trio's rule), `tapeEffectiveRate` feeds both modes, tape row gains the T+P/STR button. **`sl_tape_stretch_test` green first run: 2× timeline (playhead 1.9–2.1/frame), pitch HELD ~440 vs varispeed's ~880 after disengage, async warm never blocks.** ctest 65/65 · vitest 1382 · all gates · bundle fresh. Ear check rides P3-G1 |
| P3-2b-6 | fixture | `tape_sync_test`: a 2s loop at stamped 120 under a 140 master — timePitch: rate == 140/120 measured at the reader; timeStretch: duration matches master period within tolerance, pitch (zero-crossing rate) unchanged within tolerance; sync survives a session publish (the P3-2 hazard class, for tapes) | done | Covered across two fixtures rather than one file: `sl_tape_stretch_test` (P3-2b-5) owns duration + pitch + disengage; `plane_audio_test` gains the remaining unique halves — **a tape's rate AND tempoMode survive a session publish** (the deck hazard in the other element's clothes: edit one grid step, every synced loop falls silently to unity — the assertion that catches sl_snapshot regressions reaching tapes) and **the ratio measured AT the reader** (playhead 1.9–2.1 source frames/output frame through the dispatcher path, after re-triggering the stopped tape — a frozen playhead measures 0 whatever the rate claims). The law-side ratio arithmetic (140/120, pulse relations) is tempo.test.ts's 21 cases. ctest 65/65 |
| P3-U2 | build | A loaded grid strip never shows its session: `loadSession` doesn't rename, `recordSourceLabel` says `deck N`, Inspector says `grid deck N`. Name the strip after the session on load (keep manual rename wins) | done | `nameAfterSessionLoad` (stripOps): the DEFAULT name and a PREVIOUS session's name follow the load, a user's rename is sacred — 4 vitest cases. REC label reads `<session> · deck N` (a deck index is a slot, not a provenance), Inspector element row likewise. Walk test asserts the loaded strip is named "Untitled", not "STRIP 1". Visible door: the strip header + status line in the shipped bundle |
| P3-U3 | build | Scrub + overdub UI on tape strips: `scrubBegin/scrubTo/scrubEnd`, `overdubStart/overdubStop`, `slTape seek` all exist in the dispatcher with engine tests and have ZERO web callers; `enabledControls().scrub` is computed and never consumed. Functional baseline: OVR/RPL toggle + punch button on a tape strip (wizard had this UI pre-merge — see MIGRATION P3-13a), scrub via pointer-drag on the wave per pd-scrub-interaction's core gesture; visual polish stays D-4 | done | **SCRUB SHIPS**: pd-scrub-interaction's law applied — the wave's UNMODIFIED drag scrubs (positions in frames; the engine derives rate from the gap — the turntable law — and arms the cue on release, D-WZ-SCRUBCUE-01), the loop brace moves to ⇧-drag (the spec's one modifier), gesture locked ONCE at pointerdown (the WaveformView lock). `can.scrub` consumed at last; wavefield title teaches both gestures. ⚠️ BEHAVIOR CHANGE: plain wave-drag no longer sets the loop — the spec decided this, the title says it. **OVERDUB SHIPS (second half)**: the dispatcher's `overdubStart` now SETS THE SOURCE (unset = a punch that layers silence) and BRACKETS A TAKE — `beginTake` with the start stamped at open (an overdub's start is known at open, unlike a recording's; `endTakeKeepStamp` closes with it), `overdubStop` returns the pass's path. SCHEMA v91. The strip gains OVR beside the verbs: punch in (SUM; ⌥ = REPLACE for that punch), latched while punching, punch out closes the pass and bumps the wave revision; enabled only with an INPUT patched + a LOOPING tape (the engine's overdub taps device inputs — the titles say what to do, not just no). `plane_audio_test` pins both halves the phase-proof way: REPLACE-with-silence makes the punched span QUIETER (the P3-13a distinction — a sine summed into a sine can cancel), and the pass lands as its own take with a path in the reply. Real-host feel check rides P3-G1 |
| P3-U4 | build | Dead status plumbing: `unresolvedRef`/`decoding` never supplied by `Plane.tsx`/`PlanePanel.tsx`; `statusLine` ctx `noOutput`/`takeName`/`takeSeconds` never passed; so "audio missing", "decoding N%", "no output — this strip goes nowhere" can never render and `hasMaterial` can never be falsified by a missing take. Wire the real values (takeRef resolution against `slTakes list`) | done | PlanePanel builds a take index from `slTakes list` (sidecars parsed with takeLibrary's own `parseSidecar`/`takeSeconds` — first real callers; sidecar-less takes stay NAMED, no length), refreshed when any strip's takeRef moves, `null`-while-unknown so nothing flashes "missing" before the disk answers. Plane derives per strip: unresolvedRef / takeName / takeSeconds + `noOutput` via new `cables.hasOutput` (whole-graph: a CHAINED strip counts as routed — the `▸—` chip alone would lie; 5 vitest cases). Engine defaults wire every channel → main and bootMap captures them, so the warn is earned only by real unpatching. NOT wired, deliberately: `decoding` — nothing in the merged app produces decode progress (takes load natively); the prop stays for the day something does. Known staleness: a take deleted from the takes panel (separate window) shows as missing only after the next refs change/boot |
| P3-U5 | build | Master bar fixes: (a) ±1 bpm buttons claim "transient nudge, never the document" but call `setMasterBpm` → dirty; either wire `nudgeBpmDelta` (has zero callers) or fix the comment + intent; (b) master transport buttons never disabled with zero grid strips (silent no-ops over an empty array) | done | (a) the steppers are now HONEST document steps — the comment matches the code, and the real transient nudge (hold-to-bend, snap-back) is queued as a D-4 feel question with the law's `nudgeBpmDelta` input kept waiting for it; (b) ⟳ ↻ ◼ disable at `deckCount === 0` with a title naming the next step ("load a session into a strip"). Master.test.tsx (house SSR pattern) pins both. Bundle rebuilt; all gates green |
| P3-U6 | build | Error surface: every `slChannel/slRoute/slTape/slDeck` failure routed through `plane/send.ts` is one `console.error` and invisible; route failures into the plane note line (the mechanism 502bc1d built for session opens) | done | `send.ts` gains an `onRefusal` seam — subscribers get EVERY refusal (the note line is idempotent), the console keeps its once-per-method damping; PlanePanel subscribes and writes `"<method> refused — <msg>"` to the note line. 4 vitest cases (both paths, damping, unsubscribe). Note: in a plain-BROWSER plane preview, methods the companion genuinely lacks (e.g. slTakes) now show an honest note where they were console-only — correct, that host really cannot do them |
| P3-U7 | build | Carve lands: take importer for `takeLibrary` (none exists outside its own test) + `doCarve`'s permanent refusal replaced by the real bridge (tape loop region → session grid track referencing the same take, STRIP-MODEL "carve frees the tape") | done | The invariant DECIDED the design: a carve copies NO audio, so instead of an importer the `slFiles` namespace gains a **read-only `/takes` mount** (SCHEMA v92) — a kit sample's `filePath` points straight at the recorder's WAV; writes/removes there refused by name (the take library has its own verbs). `carveIntoSession` (companionEngine): decode via the mount, land on the FIRST EMPTY track (section-A authority; a carve must never overwrite a sequenced track), region rides as `sampleStartMs/EndMs` on every section's row, kit entry named after the region, publish + autosave. `doCarve`: targets the first grid strip with an open session, FREES the tape layer (element → none, channel unbound — the freed strip must be silent, not playing a ghost) and says where the region went. Refusals at every step name the next move. Dispatch-test pins the mount (reads work, mutations refused, containment inside it). ctest 65/65 · vitest 1382 · all gates · bundle fresh. Real-host click-through rides P3-G1 |
| P3-3-1 | build | FX returns ON in the merged host: `returnFx: true` in `SlDispatch::capabilities`, verify what that unhides on scoopy's surfaces and what the render now does; strip sends 1–4 audibly reach the return section for BOTH element kinds | **unblocked (D-WZ-PDC-01 SIGNED 2026-07-29) — rides the P6 queue below as P6-3** | **The measurement the roadmap asked for (2026-07-28):** the TRANSPORT is fully built — strip sends render into the core's send lanes (`DestEndpoint::sendBus` → `map.send[di]`, mono per the core), the core's return WET lanes come back as routable sources (`SourceEndpoint::fxReturn` → `map.returnWet`), and grid tracks' send1..4Level travel in the world (zeroed only by `disableReturnFx`). What is MISSING is the CONFIG path: the merged host has no `fxSlot` dispatch and no return params in `kParamMap`, so the return processors would run at hard C++ defaults (delay 250 ms / fb 0.4 — "a wrong-sounding echo", the schema comment's own words for why the capability is false). Flipping today would be the dishonest state. **Second measurement (2026-07-28, after the fxslot door landed): the core retired its internal return delay — "a return is either external or a hosted plugin" (NativeAudioEngineCore.hpp:1205). So the config path IS plugin hosting, which is P6, PARKED behind the signed-decision gate D-WZ-PDC-01. This row leaves the decision-free queue: it unblocks when P6 opens.** The fxslot door (P3-4-2) already works for its mode/output controls; the plugin picker stays honestly inert behind `pluginHosting: false` |
| P3-3-2 | fixture | Extend `plane_audio_test`: send level up on a tape strip and a grid strip → return bus energy appears; send at 0 → silence on the return | **unblocked (D-WZ-PDC-01 SIGNED) — rides P6-3** | follows P3-3-1 wherever it goes |
| P3-4-1 | spec | Panel audit, the list: measured 2026-07-28 — App.tsx routes 19 panels; reachable in the merged app are exactly 3 (plane · companion, which embeds filebrowser + grid · grid via Composer). **15 unreachable**: spectral, paintmode, import, general, template, audio, appearance, midi, perf, deckmixer, fxslot, instrument, transport, djmode, capture. Table each: panel → job → door (hosted in plane / menu / settings / DELIBERATELY retired with the job's new home named) | done | `docs/merge/PANEL-AUDIT.md` — 19 panels tabled: 3 reachable (+1 embedded), 14 mechanical menu doors (5 grouped under `settings ▸`; **fxslot FIRST — it unblocks P3-3-1**), 1 PARKED with its reason (instrument: `pluginHosting: false` — a door to a panel whose every action fails is worse than none; un-parks at P6). Taste calls named for D-5: deckmixer/djmode survival beside the plane, capture-job rehoming, audio-panel vs the host's own device picker, spectral's long-term home. P3-4-2 = the `≡` panels menu + `__slPanelArg` injection |
| P3-4-2 | build | Doors for every panel the audit marks mechanical (a panels menu on the plane bar or native menu bar — there is no MenuBarModel anywhere in shell/src), each reachable in the real host; inject `__slPanelArg` (read by FxSlotPanel/InstrumentPanel, never injected by MergedMain) | done | `≡ panels` on the plane bar: FX 1–4 (fxslot, ADDRESSED per slot), the eight mechanical surfaces, and the five settings panels — every audit row with a door now has one, next to `sessions ⇱` where the pattern already lived. Shell: the `arg` the schema always carried finally travels — `openPanelFn(panel, arg)` → `window.__slPanelArg` injected (sanitised to alphanumerics; it rides inside a user script). WizardMerged builds; ctest 65/65; all web gates + bundle fresh. Visible door: the `≡ panels` button itself. Real-host click-through of each panel rides P3-G1 |
| P3-M-1 | spec | Master fold-in — THE MEASUREMENT (2026-07-28): the transport panel is DESKTOP-SHELL-COUPLED — it waits on pushed UiState topics the merged shell deliberately never publishes, and speaks `menuTransport`/`menuSession`/`djSetting`/`toggleDjMode`, none of which the merged dispatch answers. So the ≡ door opens it (P3-4-2) but most of its surface is inert here; folding its VERBS means reimplementing them against the merged engine, verb group by verb group. THE LEVER for the signature verb: beat repeat lives in the core's `DeckWorld` (`beatRepeatStartStep/Length/Subdivision`, NativeAudioEngineCore.hpp:531-544) — fields the sl snapshot API cannot set (begin takes bpm/playing/startStep; track_set is track-scope). | measured → split | Split into: **P3-M-1a** (ABI: deck-scope snapshot fields — beatRepeat trio + `reverseActive` — via a `sl_snapshot_deck_set` keyed like track_set; SlWorldApply carries them from the world; worldFromSession emits them from companion state) · **P3-M-1b** (Master bar verbs: BR toggle + length cycler + REV, driving the companion state that publishes — the same store→publish lane scenes use; playful arrangement stays D-4) |
| P3-M-1a | build | Deck-scope snapshot fields: `sl_snapshot_deck_set(e, param, value)` with keyed names (beatRepeatStartStep · beatRepeatLength · beatRepeatSubdivision · reverseActive) stamped onto the DeckWorld at commit; SlWorldApply reads them from `world.…`; fixture: publish with BR on → core snapshot fields land; publish without → defaults | done | `sl_snapshot_deck_param_id/_set` (6 keyed names incl. `beatRepeatStartSubcell` + `reverseTransport` — the core's own vocabulary; subdivision validated to the spec'd 2/4/8/16/32, a wrong roll rate being worse than none); SlWorldApply forwards present fields, absent = fresh defaults; the World type gains the optionals so P3-M-1b is TS-only. **Fixture is AUDIBLE by necessity**: the HotFrame step is the MASTER step, which advances underneath a repeat BY DESIGN — a step assertion measures the wrong tier (found by the first draft failing). A pattern loud only in steps 0–1: repeating [0,2) holds ~100% loud blocks, free running ~25%, margin 0.3. ctest 65/65 · vitest 1382 · bundle fresh |
| P3-M-1b | build | Master bar verbs: BR (latched) + length cycler + REV on `Master.tsx`, held as COMPANION runtime state per deck (never the document — a beat repeat is a hand gesture) and republished on change, the scene-select lane. Titles name the scale (16…2, 1, then 1/N rolls) | done | The fields ride `worldFromSession` OPTIONS (pure, tested: absent = engine defaults, whole-step windows carry NO subdivision field — writing 1 would claim a roll that is not happening); companion holds per-deck runtime state with setters that republish, CLEARED at closeDeck (the slot-reuse rule sync follows); Master gains BR + fused-scale cycler (16…2, 1, 1/2…1/32 — live while latched) + REV, fanned over every loaded deck, disabled at deckCount 0 with next-step titles. startStep 0 (repeat the top) — "from where you are" needs per-deck playhead steps the HotFrame only carries for deck 0; uniform honest behaviour first, noted for revisit. vitest 1384 (+3) · ctest 65/65 · all gates · bundle fresh. **P3-M-1's functional baseline is COMPLETE — scoopy's signature transport verbs live on the plane's master; the panel stays reachable via ≡ for the rest; playful arrangement remains D-4** |
| P3-PUSH | gate | Push `main` to the `scoopy` remote after each green commit (repo private; roadmap: "sixteen commits exist only on this machine" — 6 unpushed as of 2026-07-28 session start) | recurring | never push a red tree |
| P3-AUDIT | spec | Phase audit before offering P3-G1: diff P3-ROADMAP's goal table (four domains × every element kind) + STRIP-MODEL against built rows; materialize gaps as rows | done | **The four domains, audited (2026-07-28):** mixer — ✅ every element (sl_channel projection; input a full citizen). transport — ✅ one vocabulary (⟳▸↻◼ on strips, master fan-out + BR/REV; ▸ absent on grid/master by design). master tempo & sync — ✅ every element, both costs (deck 3 modes · tape timePitch/timeStretch, publish-survival pinned both ways). FX sends — transport ✅ end to end; RETURNS blocked(P6/D-WZ-PDC-01), measured twice, reason recorded. Sessions/panels/carve/scrub/overdub — ✅. **Gaps materialized below:** P3-X1 (grid+tape coexistence — STRIP-MODEL says a strip holds BOTH; today the element is a one-of union and REC on a grid strip REPLACES the grid in the document while correctly capturing the deck's output to the new tape), P3-X2 (DRV master DSP per strip — deferred since P2 with reasoning, still open), P3-X3 (presets — "new looper strip" one-click). Only decision/user-gated rows remain beyond those |
| P3-X1 | build | **Grid + tape COEXIST in one strip** (STRIP-MODEL's own sentence: "a scoopy deck with a looper recording the deck's own output") — today `element` is a one-of union, so REC on a grid strip captures the deck's output correctly but REPLACES the grid element in the document. The data-model half (a strip holding both, lanes = sum) is buildable; HOW the two render in one 340×196 box is exactly the morph question | **retired-by-decision (D-4 SIGNED 2026-07-29, D-SL-MORPH-01)** | the user chose ONE KIND PER STRIP — no composite element, no migration. The use case survives as two routed strips: P3-R3 (REC on a grid strip spawns its looper strip) fixes this row's REPLACES defect the signed way |
| P3-X2 | build | DRV (master DSP) per strip — STRIP-MODEL: "reaches every strip, not just full decks"; deferred in P2 (the core has per-deck DRV; tapes have none) and still open | todo (measured) | **Measured 2026-07-28, the next loop's opening row:** the core's `NativeMasterDrive` is ALREADY per-deck (`deckMasterDrive_[kMaxDecks]`, four curves incl. the legacy-exact soft knee, decoupled drive mode) and self-contained — the tape-tier build is a per-channel instance in `sl_channel` at the STRIP-MODEL tap point (post-element, pre-level), grid channels PROJECTING onto the core's own deck drive (the no-double-gain rule), curve+amount as channel params, Inspector UI, audible fixture. The stretcher's reuse pattern, one tier over. Decision-free; sized like P3-2b-5 |
| P3-X3 | build | Strip presets (STRIP-MODEL "presets keep the quick-looper fast"): the + menu offers "looper strip" (channel + tape armed, one click) beside the plain empty strip | done | `+ looper` beside `+ strip` (a second one-click button — the existing gesture untouched, so the walk test and muscle memory keep working): channel + tape element bound up front, named `LOOPER N`, input route patched, REC one press away. Recording deliberately NOT auto-started — a preset that captures the room on click is a surprise, not a speed-up. Honest refusals when tapes/lanes run out. vitest 1384 · all gates · bundle fresh |
| P3-G1 | gate | **Phase gate (human):** on the user's machine — a grid deck and a tape in strips, both answering level/sends/transport/sync identically; sessions save and reopen; nothing of scoopy's unreachable | awaiting-user | offered only after P3-AUDIT |

## Decisions queued (do not block)

See `docs/merge/MORNING-DECISIONS-2.md`: D-1 session store flip (veto window),
D-2 tape beat inference, D-3 tape stretch latency policy, D-4 the playful strip
morph / visual direction, D-5 panel hosting taste calls from the audit.

# P3.5 — the strip becomes the deck (queued 2026-07-29, D-4 signed)

> D-4 SIGNED live 2026-07-29 (D-SL-MORPH-01): the strip's expanded face hosts the
> REAL `GridPanel` at DJ density; strips are one-kind-each (grid OR looper — P3-X1
> retired); dead panel doors retire; compose opens in a real separate window.
> Plan file: `~/.claude/plans/lets-continue-the-merge-shimmying-treasure.md`.
> Same loop protocol, same four rules. **Loop order:**
> P3-U8 → P3-L1 → P3-D4-M → P3-C1 → P3-C2 → P3-D4-3 → P3-D4-1 → P3-D4-2 →
> P3-R1 → P3-R2 → P3-R3 → P3-P1 → P3.5-AUDIT. Push after every green commit.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P3-U8 | build | Scene pads tell the truth: labels 1–8 via `sceneDisplayLabel` (strip is the ONLY surface showing raw A–H storage letters); pads = `enabledScenes(pattern)` not a fixed 8 (today a 3-scene session shows 5 dead pads); `+` add-pad via new `setEnabledSceneCount` companion verb, disabled at 8; adopt ScenePads' ⇧=queue where `selectScene` can express it — no fake verbs | done | Letters stay the storage/type identity end to end; only the FACE is 1-based (`sceneDisplayLabel`, the app's own rule). Pads render the SESSION's `enabledScenes(pattern)` prefix with `scenePadCount` demoted to the geometry CAP; the `+` pad wears the number it would become (`+4`, ScenePads' add-slot convention), renders only when the row has room (room ⇒ not full, cap ≤ 8, so a shown add always works; at 8-of-8 the row is full of real pads), disabled when unwired. New store verb `setEnabledSceneCount` (clamp 1..8, DOCUMENT edit + autosave; shrink below the active/queued scene falls back to A seamless-immediate and republishes — the defensive half, the UI only grows). ⇧=queue deliberately NOT mapped: a plain click here already IS the queued switch — a second modifier saying the same thing would be a fake distinction (noted in the pad comment). 5 SSR pad cases + 5 store cases. ctest 65/65 · vitest 1394 · params/hotframe · bundle + webdist fresh · plane layout + session-walk gates green. **Visible door: the pad row on any loaded grid strip in WizardMerged — load a 3-scene session, see 1·2·3·+4, not 8 letters with 5 dead** |
| P3-L1 | build | The library lives on the plane: `library ▾` popover (list · New · rename · delete · import) against sessionStore/slFiles; `sessionStore` gains `renameSession` + `createSession(name)` (create-NOT-load); the `sessions ⇱` → companion-window door is DELETED (companion = web bonus, user decree). Rewrite `browser_session_walk_test` to walk the plane library | done | `plane/Library.tsx` — PROPS not store reads (found the hard way: under `renderToStaticMarkup` a zustand hook serves the SERVER snapshot = initial state, so a store-reading component is untestable in the house SSR pattern; Strip/Master already obeyed this rule for their own reasons). `createSession` owns the fresh-document + unique-naming logic (moved from `newSession`, which now rides it — create-and-open unchanged for the companion); `renameSession` = open→save-as→delete-old, refuses an existing target (silent merge = data loss), moves identity never audio (kit filePaths keep pointing at `/samples/<import name>/…`, pinned in test). LOADED GATE: a session open in a deck refuses rename/delete with the deck named — the autosaver holds the open session BY NAME, a rename under it would re-create the old dir on the next autosave. Delete = two-click arm. Loading stays the strip menu's gesture (the library is the shelf, not the decks). Companion now reachable ONLY as `?host=browser`'s default shell. 4 Library SSR + 6 sessionStore cases; walk test rewritten (create in the plane popover → strip menu load → reload persistence → RENAME end-to-end through the slFiles route, dir moved server-side). vitest 1404 · params/hotframe · bundle fresh · plane + walk gates green. **Visible door: `library ▾` on the plane bar. Real-host check remaining: the `<input type=file>` import picker in the JUCE WKWebView — if it does not open, import falls back to a note naming the gap (walk it at P3.5-AUDIT)** |
| P3-C1 | build | Compose in a separate window: route `panel==='compose'`; arg = base64url `{deck, session}` through the EXISTING `__slPanelArg` injection (sanitizer keeps A–Za–z0–9_- — zero shell changes for the arg); `ComposeWindow.tsx` + shared `useComposeBinding` hook (overlay stays as browser fallback); shell `panelClosed` broadcast (~20 LOC, the emitHotFrame loop's twin) | done | `composeArg.ts` — UNPADDED base64url of UTF-8 JSON (padding `=` would be EATEN by the sanitizer; the test's central property is that every encoded byte is already in the retained set, forced through names that produce `+`/`/`/`=` in naive base64). `useComposeBinding` extracted from Composer (both faces share one implementation — a drifted copy is an edit that never reaches the engine); Composer's header rewritten: its three separate-window blockers are now fixed/fixed/handled, overlay demoted to the browser host's fallback. `ComposeWindow`: sink first THEN open-from-disk (`open()` publishes only while the engine runs; every WebView's store boots cold), pagehide/visibility autosave flush (the debounce is 1.5 s and closing is exactly when the last edit must not be lost), unaddressed window REFUSES with the next step named. Shell: `slPanelClosed {panel, arg}` broadcast from removeWindow (identity captured before the erase), PanelWindow gains `emitEvent`/`panel()`/`arg()`; drive-by: the pre-existing -Wreorder init-list warning fixed. `composeDeck` in PlanePanel host-splits: JUCE → real window, browser → overlay. 4 codec cases. ctest 65/65 · vitest 1410 · all web gates · bundle fresh. **⚠️ INTERIM until P3-C2 (next commit): while a compose window is open the plane can still publish the same deck — the single-publisher rule is C2's row** |
| P3-C2 | build | The single-publisher rule: while a compose window owns deck d the plane suppresses d's verbs (disabled + naming title, chip COMPOSING, meters stay live); on `panelClosed`/focus-fallback reload from disk + republish phase-continuous; double-open refused. Update Composer.tsx's header (blockers #1–#3 resolved/superseded) | done | `composingDecks` set in PlanePanel; Strip gains the `composing` LOCK face (state word `COMP`, scene field greyed-with-reason, transport verbs + tempo row disabled via `can` override — the channel tier stays live, it never publishes); master fan-outs (BR/REV/⟳↻◼) run over `activeDecks` and `deckCount` counts only those, so the master disables honestly when every deck is composing; session swap/drop refused belt-and-menu (disabled rows + guard in loadSession/dropElement); double-open refused with the note naming the window. Return half: `handlePanelClosed` (own module, injectable → 3 unit tests: only compose closes release, garbage address refuses rather than guessing a deck) + a 500 ms grace before the reopen so the closing window's pagehide autosave flush lands first. **Machine walk extended and green first run: COMPOSE ⇱ → openPanelWindow recorded with a decodable {deck:0, session} address → strip locks → a second page wearing the injected address mounts the REAL GridPanel naming the session → `slPanelClosed` emitted the shell's way → the lock releases.** KNOWN LIMIT (logged in code): a window that dies without the event leaves the lock until the session is dropped — no shell verb enumerates windows yet. 3 Strip SSR + 3 ownership cases. vitest 1416 · all gates · bundle fresh. **Real-host walk rides P3.5-AUDIT: edit in the window, close, hear the edit on the plane** |
| P3-D4-M | spec | Measure + sketch: catalog `GridPanel variant="dj"`'s source contract (`djSource`, `djMeta/<deck>` topics, `djFocusBridge`, `deriveTrackState`, `cellsHidden`/`djSlotIndex`) vs plane equivalents; decide **web-side `planeDjSource` adapter over companionEngine (recommended)** vs shell-served topics; write `docs/merge/STRIP-DECK.md` (deck tile ~3×3, header verb row ▶·REV·BR·SYNC·SAVE·EJECT·name, pads row, GridPanel region, LCM bar, collapse⇄expand). Proportions/verb placement reviewable by the user; the DIRECTION is signed | done | `docs/merge/STRIP-DECK.md`. **VERDICT: plane-side adapter, ZERO shell changes** — GridPanel@dj's read surface is exactly `djMeta/<d>` + `djPattern/<d>/<i>` + `djRuntime/<d>/<i>` (all publisher-less today; meta absent = hard block "waiting for pattern state") + 3 HotFrame blocks (engine zero-fills them: the `?? -1` guard never fires on 0.0 → frozen step-0 playhead wash — D4-3's territory, adapter stamps −1 meanwhile); the write path TERMINATES IN THE COMPANION (nothing added to C++ dispatch; `publishTrackPattern`/`getSamplePeaks`/`gridEdit`/`getUiState` merely DROP the `deck` the schema carries). Shell-served topics would mean porting the grid document model into C++ — re-creating the carved-off Swift binding; actively wrong. THE REAL BLOCKER is `BrowserLink`: one compose-hard-coded `GridBackend`, six single-slot handlers (mount two composers → deck 0's edits land in deck 1), one peakPaths. Store half already deck-parameterised end to end; multi-mount proven by DjPanel itself. Deck column floor ~500px wide (five 88px min-width DSP boxes, clip not wrap) × ~136px/track → **expanded tile = 2×3 cells (~688×604), collapsed = today's 340×196 exactly**. MasterRow BPM/VOL/DRV paramWrites are REFUSED by MergedMain::handleParam → D4-1 reroutes through companionEngine/channel or hides (no dead controls in the tile). Inherited hazard accepted + noted: undoStore keyed by track only. keyboardActive must be arbitrated (one deck true). |
| P3-D4-3 | build | Per-deck playhead/cycle fields in the HotFrame (today deck 0 only — the P3-M-1b note); feeds the LCM bar and BR-from-where-you-are. C++ + schema + ctest fixture. Decision-free, may run before the sketch | done | Built one tier RICHER than the row imagined: the layout ALREADY declared per-deck fields (`playheadStepDeck0..2` were even written) — what was missing was the **per-TRACK dj telemetry**: all 144 `djTrackStep/Pos/Level` slots were zero-filled and never written, so the UI's `?? -1` guard never fired and every deck-tile track would paint a permanent step-0 playhead wash (the D4-M finding). `sl_hotframe` now writes them from the engine's own state, the desktop's semantics (WebDjBinding.djHotFields) ported to the tier that owns the render: step −1 when not live, else the deck playhead folded through the deck-scope BEAT-REPEAT window (display follows the repeat; the master step advances underneath by design — M-1a's lesson), then the track's own step count, then the reverse mirror (session XOR per-track, the render's composition rule); pos = `deckTrackSamplePos` (−1-honest by the core) gated on live; level = `deckTrackMixLevel` deliberately UN-gated (stopped-but-ringing is the point). KNOWN LIMIT in code + here: per-track speed multipliers/locator repeats deviate from the uniform fold — the desktop resolved those through the sequencer's full display logic; revisit if a tile playhead visibly drifts. Fixture §10b in plane_audio_test (indices hand-restated from schema per the harness rule): real step in range, nonexistent track −1, inactive deck −1, level >0 sounding /0 silent, **step ADVANCES across a boundary** (a frozen valid number is the old bug in a new coat), stop→−1. Green first run; ctest 65/65; vitest 1416 ×2 (one flake observed then twice green, test unidentified); hotframe:check. NOTE: `engine:check` fails PRE-EXISTING on committed main (vendor CMakeLists drift vs pin ec01e16) — not this row's doing, recorded for the audit |
| P3-D4-1a | build | **The MasterRow becomes REAL** (user decision 2026-07-29, live: "work from the real scoopy perspective — the new design adapts to the real version"; the D4-M item-5 options were reroute-BPM/hide — the user chose NEITHER: keep scoopy's full BPM·VOL·DRV row and wire all three). The session document's `masterVolume` + full clipper block travel world→engine per deck; the row's three writes land in the document | done | The core ALREADY renders per-deck master (deckSnap.masterVolume + deckMasterDrive_ in renderDJ) — what was missing was the world carrying the fields: 8 keyed names appended to `kSnapshotDeckParamNames` (masterVolume + the 7 clipper fields, curve/oversample validated — a wrong curve id is DSP the user never chose), SlWorldApply forwards them absent=defaults, `worldFromSession.masterStage()` emits them from the PATTERN (document fields, not options — the pattern format requires them; per-field guarded so a hand-built fixture publishes defaults, never NaN). Write path: `sessionBpm/sessionMasterVolume/sessionMasterDrive` are DOCUMENT edits, so `BrowserLink` gains a sessionParam seam (registered once at the App root — covers plane, compose window, browser companion) routing to `setBpm` + new `setMasterVolume/setMasterDrive` companion verbs (clamped 0..2 / 1..32, publish + autosave, deck-isolated); `MergedLink.paramWrite` tries the seam BEFORE the native lane (the shell refuses these three by design — kParamMap). `GridBackend.meta` now serves the document's masterVolume/masterClipperDrive (was hardcoded 1/1 — the row showed values the engine never heard). **Audible fixture green first run**: same world at masterVolume 0.5 → peak halves at main; decoupled drive 16 vs 1 → audibly hotter (legacy mode only engages >100%, so decoupled is the assertable config). +4 vitest (world emission absent-means-defaults, verb isolation+clamps, meta honesty, MergedLink routing with the rAF queue flushed so "nothing leaked" can't pass vacuously). ctest 65/65 · vitest 1427 · params/hotframe · bundle+webdist fresh · plane+walk gates. **Visible door: the compose window's master row TODAY (BPM·VOL·DRV above the tracks — edits are audible and survive reload); the deck tile inherits it at D4-1.** syncedBpm strike-through + nudge display ride D4-1/D4-2 |
| P3-D4-1 | build | The deck tile renders the REAL GridPanel: `planeDjSource` adapter + `<GridPanel variant dj cellsHidden>` in the expanded face (`DeckFace.tsx`, `planeLayout` expand/collapse op; cell size is already a document value). Tile keeps scoopy's FULL MasterRow (D4-1a wired it real) | done | Built exactly on D4-M's verdict — plane-side, ZERO shell/C++ changes. **The address layer**: `GridBackend` gains a `GridTopicNames` parameter (compose family default; `djGridTopics(deck)` = the exact `djMeta/<d>`·`djPattern/<d>/<i>`·`djRuntime/<d>/<i>` strings `djSource(deck)` reads) + mount-owned `setMetaFacts` (deckIndex · syncedBpm from THE LAW · keyboardActive; no-op republish guard since the binding refreshes per render). **The routing layer**: BrowserLink's six single handler slots → per-scope MAPS (compose = every pre-D4-1 caller unchanged via default arg; 0-2 = dj), lazy `djGridBackend(deck)` whose hooks close over the deck (the "last mount wins" defect structurally impossible), per-deck peak paths, and the four deck-dropping commands now route on `p.deck` / the dj topic address (publishTrackPattern proven answered by the ADDRESSED backend: empty deck 1 refuses "no track 0" while compose holds a track). Browser HotFrame stamps `djTrackStep/Pos` −1 (D4-3's fix for the native tier, browser coat; levels stay 0 = honest silence). **The face**: `deckTile.tsx` — `useDeckTileBinding` (useComposeBinding's deck-axis sibling + BOTH sample doors, since the dj band carries browse·LOAD) + `DeckFace` (memoized `djSource`, pointerdown claims the keyboard — exactly one deck answers arrows, claim released on unmount) + `Strip.tsx` ⤢/⤡ header button (grid strips only; a VISIBLE door, the swallowable-gesture lesson). Expansion IS the cell geometry (`isDeckCell`, both dims — no half-expand): expand = document cell → `DECK_CELL` 692×612 (2×3 grid cells incl. gaps, on the placement grid), collapse restores `DEFAULT_CELL` to the pixel. `isCoarsePointer` gained the SSR guard the house test pattern needs. Inherited + noted: shared ⌘Z timeline (undoStore by track), no cross-tile arrow ring (no fixed slots → no djSlotIndex). +13 vitest (4 Strip faces, 3 dj-backend, 6 routing) · **walk test extended and green: load → ⤢ → the REAL `.track-strips.density-dj` + MasterRow materialise (not "waiting for pattern state") → ⤡ → 340×196 exactly**. ctest 65/65 · vitest 1440 · params/hotframe · bundle+webdist fresh · plane gate green. **Visible door: ⤢ on any session-loaded strip in WizardMerged. Real-host walk rides P3.5-AUDIT: expand, edit a cell, hear it; M/S/TrimBar/RateControl respond** |
| P3-D4-2 | build | Deck verbs into the strip header: ▶/REV/BR per-strip (M-1b verbs at deck scope), SAVE, EJECT (menu item promoted), NUDGE via `nudgeBpmDelta` (hold-to-bend, snap-back — the U5 leftover, provisional feel) | done | **The verbs** (`strip-deckverbs`, expanded tile only — the 340px header has no room and the master bar already fans BR/REV): ▶ play/stop toggle (the strip transport's verbs, DeckBlock-style) · REV · BR + fused-scale cycler (BR_SCALE moved to stripOps — ONE table for the master fan-out and every strip; live-while-latched; the latched window's own scale wins the label since a master fan-out may have set a different one) · ‹ › NUDGE · SAVE (flushAutosave — edits autosave, SAVE flushes the debounce) · ⏏ (onDropElement promoted). SYNC deliberately NOT duplicated (the tile's grid row owns it — one control one home). All lock under COMPOSING. **BR/REV state moved from module arrays INTO DeckState** — the tile lamps need the per-deck TRUTH reactively; publish reads it, closeDeck clears via idleDeck (slot-reuse rule pinned). **NUDGE is real**: `nudgeStore` (transient, NEVER the document — the U5 distinction made flesh), `mapTempoIntents(map, nudgeOf)` folds it through THE LAW → `applyTempo` pushes the bent ratio (test proves the engine hears 124/120), and the resolved-tempo displays follow the hand live (tile MasterRow strike-through via setMetaFacts + the grid row's synced label — the display the user called out 2026-07-29). Law semantics untouched (golden-mirrored): nudge bends the SYNCED target, so a FREE deck's nudge is disabled with the teaching title. **LCM bar** (the sketch's ▓▓░ row): computed WEB-SIDE from `playheadStepDeck<d>` × `lcmForScene` — the desktop's `lcmPosDeck*` HotFrame fields have NO writer in the merged engine and a bar on zero-filled slots would freeze at 0; painted at HotFrame rate via ref (StripMeter pattern). +8 vitest (verb row faces ×5, per-deck BR/REV+closeDeck, nudge fold per-deck, nudge push/no-noise-republish) · walk test extended (verb row + LCM present in the expanded tile). ctest 65/65 · vitest 1449 · params/hotframe · bundle+webdist fresh · plane+walk gates. **Visible door: the verb row in any expanded tile in WizardMerged. Real-host feel check (nudge bend amount ±4, snap-back) rides P3.5-AUDIT — provisional feel per the row** |
| P3-R1 | fixture | Chain→record is audibly TRUE: plane_audio_test — tone strip A → patch A→B → B `recordTap:'bus'` → REC → the take carries the tone; unpatched → silent. Fix whatever it finds in `sl_tape_set_record_source` channelBus handling. The ROUTING-MATRIX record-tap paragraph currently has this only on paper | done | §11b: input→strip0→strip1(bus tap, `sourceKind:2 chan0:1`)→REC → the tape PLAYS BACK the routed tone with the input dead (anything audible is the capture, not the live chain); unpatch → a fresh capture is silent (a tap that still heard the old cable would be recording the routing table). **The engine needed NO fix** — `channelBus` capture was sound; the fixture's one find was its own missing `setSource {kind:1}` (the channel↔tape binding Strip.onRecord always sends — captured-into-a-tape-nobody-sources plays into nothing, a good trap for R3's spawn to remember). With one-kind-per-strip signed, this chain IS "the looper records the deck's own output" — no longer paper. ctest 65/65 |
| P3-R2 | build | One gesture "loop that strip": strip menu `record from ▸` gains other strips by name — patch(that→me, wouldCycle consent) + `recordTap:'bus'` in one click; status line names the source | done | The ⋯ menu's `record from` section now lists every OTHER strip (`STRIP 2 → this strip's bus`): selecting sets the tap AND patches through the plane's OWN consent flow (wouldCycle → the feedback-with-its-price menu — no second cycle logic, the cable-drag lane reused; an already-cabled source just flips the tap, a duplicate cable would double the gain). Status line answers "what will REC capture" with NAMES: `records: STRIP 2 + STRIP 3 → this bus` (bus tap + routed sources; generic label kept when nobody feeds it — no invented sources, 3 SSR cases). Checkmark = tap is bus AND that strip feeds it. Engine half proven by P3-R1. vitest 1419 · all gates · bundle fresh. **Real-host walk (P3.5-AUDIT): two strips, route one into a looper via the menu, REC, hear the loop** |
| P3-R3 | build | REC on a grid strip SPAWNS/targets its looper strip (patched from this strip's bus, the `+ looper` machinery + R2's gesture) instead of replacing the grid element in the document (P3-X1's defect, fixed the signed way). Amend `STRIP-MODEL.md`: strips are one-kind-each; the pair is two routed strips | done | `Strip.onRecord`'s grid branch now hands off to the plane's `recordIntoLooper` — the grid element NEVER moves. `linkedLooperFor` (stripOps, 4 unit cases) defines the pair: a TAPE strip whose bus this strip feeds AND whose tap is the bus — the cable is the link, the tap is the intent, another grid never qualifies. No link → spawn: fresh channel + tape named **`LOOP · <src>`** (named after its JOB — the pair reads as a pair), `recordTap:'bus'`, patched from the SOURCE'S BUS not the device input (no cycle possible: the edge lands on a channel that did not exist a moment ago), channel bound to the tape (the R1 trap remembered: captured-into-a-tape-nobody-sources plays into nothing). Capture starts THERE (`channelBus` tap + `bpmAtStart`); the looper strip's own ■ closes the loop and lands the takeRef via its existing stop path; the grid strip's REC title says all of this. Honest refusals: channels/tapes/lanes exhausted → note. STRIP-MODEL amended in the D-4 signing commit; engine semantics proven by R1's §11b. vitest 1423 · all gates · bundle fresh. **Real-host walk (P3.5-AUDIT): REC on a loaded grid strip → a LOOP · strip appears beside it recording the deck; ■ there → the loop plays; the session never left the source strip** |
| P3-P1 | build | Retire the dead doors: djmode/transport/deckmixer leave the ≡ menu (their windows hang on WaitingForState — the merged host serves none of their pushed topics; their verbs land on strip/master). Code stays routed. PANEL-AUDIT rows → "retired — home: the plane"; note the ≡ menu itself is interim scaffolding | done | The surfaces list extracted to exported `PANEL_MENU_SURFACES` (spectral · paintmode · midi · perf · capture) so `panelMenu.test.ts` PINS the retirement — the trio cannot quietly return, and the five that remain are asserted to be exactly the ones that work. Panels stay routed in App.tsx (retired = no door, not deleted code); PANEL-AUDIT rows were updated in the D-4 signing commit. The ≡ menu comment records the user's direction: interim scaffolding, dissolving as jobs rehome. vitest 1406 · bundle fresh · plane + walk gates green. **Visible check: the ≡ menu in WizardMerged lists FX 1-4, five surfaces, five settings — no row that opens a tombstone** |
| P3.5-AUDIT | spec | Diff this section + STRIP-DECK.md against built rows; materialize gaps; then offer the USER GATE: library walk (no companion window), compose-window walk (edit heard, no stale world), deck-tile walk (real DJ rows in a strip), looper-routing walk, pads 1–8 | done (2026-07-29) | **Every P3.5 row built** (U8 L1 D4-M C1 C2 D4-3 D4-1a D4-1 D4-2 R1 R2 R3 P1); STRIP-DECK's sketch fully realized (tile = header verbs + pads + real GridPanel + LCM bar + channel row; SYNC not duplicated into the header — one control one home, recorded deviation). **User real-host confirmations so far: the deck tile's grid IS visible in WizardMerged (2026-07-29) — the first P3.5 feature real-host-verified; first ergonomic finding "hard to control its UI" → wheel-inside-tile was zooming the PLANE (fixed: the deckface stops wheel propagation, its rows scroll).** Gaps materialized: **P3.5-E1** (tile ergonomics pass beyond the wheel fix — pointer precision under plane scale, dj band sizing in a 692px tile — feeds on the user's walk notes) · known limits carried: C2 crashed-window lock, no cross-tile arrow ring, shared ⌘Z timeline, L1's WKWebView import-picker unverified, D4-2 nudge feel provisional (±4 hold-bend). **USER GATE (the remaining walks): library · compose window · looper routing (two strips) · pads 1–8 · tile MasterRow audible (VOL halves, DRV drives) · nudge bend-and-snap — on the user's machine, at their pace; findings land as E-rows** |
| P3.5-E1 | build | Tile ergonomics from the real-host walk: wheel fixed (this commit); remaining — dj band control sizing/precision at plane scale, tile-vs-plane gesture arbitration beyond wheel, whatever the walk surfaces | todo (user-fed) | opened by the user's "hard to control its UI" — collects walk findings |
| P3.5-E2 | build | Real-host find (2026-07-29): a session saved after a flam edit in the deck tile REFUSED to reopen — `sectionC[1].flamCounts[0..12]` null. Root: the grid reducers assign by index into a slice (`flamCounts[8] = 2`) and several per-step arrays start `[]` in a fresh session → SPARSE array → JSON.stringify spells holes as null → the strict decoder rejects the whole session. D4-1 made these gestures reachable on plane sessions for the first time | done | Two seams, both fixed: **(1) writes go dense** — `applyGridPattern` (the ONE place runtime grid state re-enters the document) copies every per-step array through `dense(a, fill)` with its native neutral (the same fills `canonicalGridSubset` pads with: flam/cellLengths 1, offsets/accents 0, step flags false, velocities 100); `.map` alone would have PRESERVED holes. **(2) broken files heal** — `migrateTrack` scrubs null entries in the per-step arrays back to those neutrals before the strict parse, so every session this bug already locked out opens again; nulls anywhere else still throw (rescue, not tolerance). +3 vitest (sparse→dense write-back; the screenshot's exact shape `[null×8, 2, null×4]` → `[1×8, 2, 1×4]`; boolean/offset lanes) · vitest 1452 · plane+walk browser gates · bundle+webdist fresh (the fix must ride the webdist to reach the host) |

# P6 — plugins on the returns (opened 2026-07-29, D-WZ-PDC-01 signed)

> The user's direction, verbatim intent: "we still need a strip mixer to control each
> volume and the fx send channel for plugin load, this can almost be as in scoopy —
> just needs to be fitted into the map system so we can also restore these settings
> (like a plugin loaded) within a map." Route: REUSE scoopyloops' own JUCE-based
> `NativePluginHost` (header already vendored; the core's per-return `NativePluginSlot`
> render machinery already in the vendored core behind `SCOOPY_PLUGIN_HOST=0`).
> The comparison audit (2026-07-29) confirmed the mixer/fx UI surfaces
> (DeckMixerPanel · FxSlotPanel · InstrumentPanel) are byte-identical in this tree —
> nothing to rebuild web-side; the work is host plumbing + map persistence.
> **Loop order:** P6-1 → P6-2 → P6-3 → P6-4 → P6-5 → P6-6 → P6-AUDIT. Same loop
> protocol, same four rules, push after green.
>
> **USER DECISION (2026-07-29, asked directly): the deck MIXER UI waits for the
> plumbing.** Run this queue as written — audible → editors → map → PDC — and
> then design the strip-integrated mixer (deck volume + FX send channels with
> plugin load + transport, per the recorded vision) as its OWN phase (P7), with
> everything underneath it already proven. DeckMixerPanel stays doorless until
> then; per-strip LEV/SEND faders on the plane remain the mixing surface.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P6-1 | build | The host compiles IN: vendor `NativePluginHost.mm` from scoopyloops (995 L, ObjC++ — the JUCE-using scanner + slot impl), flip `SCOOPY_PLUGIN_HOST=1` in the vendored-engine CMake, link juce_audio_processors + AU/VST3 format modules; the out-of-process `--scan-plugin` worker wired into WizardMerged's main | done | **The .mm lives at `ScoopyLoops/NativePluginHost.mm`** (the core's writable home; NOT lock-pinned) with two documented adaptations: the Xcode prefix header is gone (JUCE module settings come from CMake here) and the scanner re-launches THIS binary instead of a bundled ScoopyPluginScanner helper. **The flag is one option, two implementations of one file**: `SCOOPY_PLUGIN_HOST_ENABLED` (vendored-engine CMake, default ON for Apple native, OFF for EMSCRIPTEN/Windows/Linux — the .mm is ObjC++) flips the core's PUBLIC define; the app target compiles the .mm real (juce_audio_processors + JUCE_PLUGINHOST_AU/VST3 + CoreAudioKit — AUGenericView isn't in the module's default framework set); `scoopy_plugin_host_stub` compiles the SAME file's `#else` branch as plain C++ on scoopy_engine's PUBLIC link interface, and lazy-archive semantics do the dispatch — the app's real object defines the symbols first so the stub is never pulled, the JUCE-less gate tools pull the stub and link unchanged. **Flag-ON/no-plugin ≡ flag-OFF was verified region-by-region before flipping** (host-mode returns render silence without an instance; instrument paths sit behind `instrumentHostingEnabled_` = false; empty-slot `prepare()` touches no JUCE machinery) and then by the gates: ctest 65/65 including every DSP gate on the flag=1 core. `MergedMain` expands START_JUCE_APPLICATION and intercepts `--scan-plugin <format> <id>` before any application object exists (macOS macro variant — no iOS delegate line); OBJCXX is enabled at the ROOT CMakeLists (a subdirectory enable_language loads rule variables into that scope only and broke every other dir compiling JUCE .mm module sources). **Smoke-proven end to end: the binary scanned Apple's AUDelay out-of-process and printed its PluginDescription XML; the app launches and runs.** ctest 65/65 · vitest 1449 · params/hotframe · plane+walk browser gates · webdist fresh. engine:check carries its known pre-existing CMakeLists drift (now including this row's edit — the file is in-tree writable since P3-0; the lock note is stale). NEXT (P6-2): the scanner has no owner yet — nothing constructs `NativePluginScanner`; the fxSlot dispatch does that |
| P6-2 | build | The scanner reaches the UI: `fxSlot` dispatch (scan/list/pick/clear per return), capabilities `pluginHosting: true`, the FxSlotPanel's picker LIVE (it is byte-identical and waiting); scan results cached as XML per the host's own design | done | **The scanner is HOST property**: `HostServices.pluginScanner` (fwd-declared — the hpp is JUCE-free), owned by the app's Backend (real `NativePluginHost.mm` impl; ctor only restores the XML cache, the sweep runs on `rescanPlugins`); a headless dispatcher has none → `listPlugins`/`listInstruments`/`rescanPlugins`/`selectFxPlugin` refuse by name and `capabilities(services)` keeps `pluginHosting` false — the SAME code path, so the sl_dispatch_test assertions are the app's honesty proof. **The slots got ABI doors** (v3, `sl_fx_*`): `select` (scanner crosses as an opaque ptr; NULL id = unload; async by design), `name`, `latency_ms`, `teardown` (sync, message-thread, called in shutdown BEFORE the engine dies — the destroyNow contract); all four honest no-ops on a flag-OFF build. **The panel leaves WaitingForState**: `toolbarState()` builds the full strict `ToolbarUiState` — fxSlots truthful (name/latency from the slots, explicit-null when empty), every other field the schema-required NEUTRAL spelled out (deckmixer/djmode/transport are retired doors; nothing reads them) — pushed `slUiState`/topic `toolbar` at ~2 Hz off the HotFrame timer (async loads land on the next push; the panel's own listPlugins poll is the scan progress UI, per its design). MergedLink NATIVE_METHODS += the four commands (the allowlist lesson: an unlisted method lands browser-side and refuses invisibly). Tests: exact key-set pins for ToolbarUiState (23) + FxSlotState (13) — zod `.strict()` drift = silent WaitingForState, so the CONTRACT is pinned not sampled; refusal/accept matrix incl. out-of-range return and null-unload; **the C++-built JSON parsed by the REAL zod schema** (`sl_dispatch_test --dump-toolbar` debug door → `ToolbarUiState.parse` OK). ctest 65/65 · vitest 1452 · plane+walk · bundle+webdist fresh · app boots with scanner + pump. **Real-host walk (P6-AUDIT): ≡ FX 1 → RESCAN sweeps AU+VST3 out-of-process (first sweep is minutes, cached after), pick a plugin, its name appears; AUDIBLE is P6-3.** listInstruments answers too (same scan, opposite filter) — the instrument surfaces it unhides stay half-parked until their P6 row |
| P6-3 | build | A plugin is AUDIBLE on a return (= old P3-3-1/3-2): instantiate per-return `NativePluginSlot`, `returnFx: true`, strip sends 1–4 → return → plugin → wet lane audible for BOTH element kinds; plane_audio_test fixture (send up → return energy; send 0 → silence) | todo | |
| P6-4 | build | Plugin EDITOR windows, JUCE-hosted (the job FxSlotWindowController.swift did on the desktop): open/close per return through the existing openPanelWindow lane; editor visibility state per F1–F4 | todo | |
| P6-5 | build | THE MAP REMEMBERS (the user's sentence made literal): `.scoopyMap` gains per-return fx-slot state — plugin `identifier` (createIdentifierString, designed for persistence) + opaque state blob + mode/output; restored on map open, honest refusal when a plugin is missing on this machine | todo | |
| P6-6 | build | PDC per the signed policy: parallel-path compensation only, per-channel latency exposed in the UI, insert latency subtracted from record-path stamps | todo | |
| P6-AUDIT | spec | Diff this section against built rows; then the user gate: load a plugin on FX1, hear a strip's send through it, tweak it in its editor, save the map, reopen — the plugin and its settings come back | todo | offered after all rows |

---

# Compacted 2026-07-30 (Lane C audit) — the second wave of full handoff notes

> The snapshot above froze the ledger as it stood on 2026-07-29. **Every row
> completed AFTER it kept its full multi-paragraph note in the ACTIVE ledger**,
> which grew to 214 KB / 531 lines — the exact failure the first compaction fixed,
> and `CLAUDE.md` makes that file the first thing every session reads.
>
> These are those notes, moved here **VERBATIM — whole table rows, unedited** —
> with one-line summaries left in their place. Rows appear in the order they stood
> in the active file, grouped by the phase heading they sat under.
>
> Also archived here: the nine rows this audit MERGED or RETIRED (their originals,
> not their replacements) and the `H5 (original plan note)` pseudo-row, which had
> no id, type or status and was never a row.

## P3 (close-out rows completed after the 2026-07-29 snapshot)

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P3-3-1 | build | FX returns ON in the merged host: `returnFx: true` in `SlDispatch::capabilities`, verify what that unhides on scoopy's surfaces and what the render now does; strip sends 1–4 audibly reach the return section for BOTH element kinds | **unblocked (D-WZ-PDC-01 SIGNED 2026-07-29) — rides the P6 queue below as P6-3** | **The measurement the roadmap asked for (2026-07-28):** the TRANSPORT is fully built — strip sends render into the core's send lanes (`DestEndpoint::sendBus` → `map.send[di]`, mono per the core), the core's return WET lanes come back as routable sources (`SourceEndpoint::fxReturn` → `map.returnWet`), and grid tracks' send1..4Level travel in the world (zeroed only by `disableReturnFx`). What is MISSING is the CONFIG path: the merged host has no `fxSlot` dispatch and no return params in `kParamMap`, so the return processors would run at hard C++ defaults (delay 250 ms / fb 0.4 — "a wrong-sounding echo", the schema comment's own words for why the capability is false). Flipping today would be the dishonest state. **Second measurement (2026-07-28, after the fxslot door landed): the core retired its internal return delay — "a return is either external or a hosted plugin" (NativeAudioEngineCore.hpp:1205). So the config path IS plugin hosting, which is P6, PARKED behind the signed-decision gate D-WZ-PDC-01. This row leaves the decision-free queue: it unblocks when P6 opens.** The fxslot door (P3-4-2) already works for its mode/output controls; the plugin picker stays honestly inert behind `pluginHosting: false` |
| P3-3-2 | fixture | Extend `plane_audio_test`: send level up on a tape strip and a grid strip → return bus energy appears; send at 0 → silence on the return | **unblocked (D-WZ-PDC-01 SIGNED) — rides P6-3** | follows P3-3-1 wherever it goes |
| P3-M-1 | spec | Master fold-in — THE MEASUREMENT (2026-07-28): the transport panel is DESKTOP-SHELL-COUPLED — it waits on pushed UiState topics the merged shell deliberately never publishes, and speaks `menuTransport`/`menuSession`/`djSetting`/`toggleDjMode`, none of which the merged dispatch answers. So the ≡ door opens it (P3-4-2) but most of its surface is inert here; folding its VERBS means reimplementing them against the merged engine, verb group by verb group. THE LEVER for the signature verb: beat repeat lives in the core's `DeckWorld` (`beatRepeatStartStep/Length/Subdivision`, NativeAudioEngineCore.hpp:531-544) — fields the sl snapshot API cannot set (begin takes bpm/playing/startStep; track_set is track-scope). | measured → split | Split into: **P3-M-1a** (ABI: deck-scope snapshot fields — beatRepeat trio + `reverseActive` — via a `sl_snapshot_deck_set` keyed like track_set; SlWorldApply carries them from the world; worldFromSession emits them from companion state) · **P3-M-1b** (Master bar verbs: BR toggle + length cycler + REV, driving the companion state that publishes — the same store→publish lane scenes use; playful arrangement stays D-4) |
| P3-M-1b | build | Master bar verbs: BR + scale cycler + REV | done — **⚠️ FRAMING CORRECTED 2026-07-30 (user): these are DECK controls; the master only FANS OUT to them.** Original scoopyloops is explicit — `TransportPanel.tsx:25` documents the DECK block as `row 2: SYNC‹pulse› · TR‹±n› · WIN · BR‹›`, and REV/SYNC/TR/WIN/BR all read `section` (`DeckSectionState`, per-deck) via deck-scoped ops. The master row there is the tempo REFERENCE only (:257, :378). D4-2 later got the STATE layer right (`DeckState.beatRepeat`/`.reverse`), but this row's own note and its `brOn`/`revOn` mirror still read as if the master owns them — see **P11-0**, which is the live defect that framing produced | Companion runtime state per deck (never the document), republished on change, cleared at closeDeck; fanned over every loaded deck; startStep 0 uniform-honest (per-deck playheads arrived later via D4-3). P3-M-1's functional baseline complete |
| P3-X1 | build | **Grid + tape COEXIST in one strip** (STRIP-MODEL's own sentence: "a scoopy deck with a looper recording the deck's own output") — today `element` is a one-of union, so REC on a grid strip captures the deck's output correctly but REPLACES the grid element in the document. The data-model half (a strip holding both, lanes = sum) is buildable; HOW the two render in one 340×196 box is exactly the morph question | **retired-by-decision (D-4 SIGNED 2026-07-29, D-SL-MORPH-01)** | the user chose ONE KIND PER STRIP — no composite element, no migration. The use case survives as two routed strips: P3-R3 (REC on a grid strip spawns its looper strip) fixes this row's REPLACES defect the signed way |
| P3-X2 | build | DRV (master DSP) per strip — STRIP-MODEL: "reaches every strip, not just full decks"; deferred in P2 (the core has per-deck DRV; tapes have none) and still open | done | Built as measured: per-channel `NativeMasterDrive` in `sl_channel` at the STRIP-MODEL tap point (post-element **PRE-level** — character constant while fading, the deck stage's own topology: decoupled, 0 dBFS ceiling, amount [1,32], **1 = a bypass BRANCH so the bit-exact identity path survives**; engage resets ADAA history — the `deckMasterDriveActive_` discipline mirrored). ABI `sl_channel_set_drive/_drive_curve/_drive_amount`; `slChannel/setDrive` (SCHEMA **v93**); map Strip gains `drive{curve,amount}` (MAP **v6** + migration: off = how a v5 map sounded; applied EXPLICITLY on load like the monitor, after the bind). **The grid half is a DOCUMENT projection, not a live one:** the core's per-deck stage reads `deckSnap.masterClipper*`, re-stamped by every republish, so a live setter would be stomped — the Inspector's same knobs write the SESSION (`setMasterDrive` + new `setMasterDriveCurve`), no-double-gain preserved (the channel-tier stage shapes only what is ROUTED INTO a grid strip). **Door: select a strip → Inspector → drive** (every strip, incl. empty — the stage shapes routed input; a session-less grid strip says "session not loaded" instead of writing nowhere). Audible fixture in `sl_channel_test`: driven DC lands on tanh(2), fader-at-0.5 gives 0.5·tanh(2) NOT tanh(1) (the topology pin), bypass bit-exact both directions. Discovered en route → P3-F3 |
| P3-X4 | build | **`webdist:check` was RED at HEAD** (found 2026-07-29 running the full gate suite for the plan-adjustment commit) — the gate that exists because "the JUCE shell ships the committed `webdist/` bundle and never builds the web UI itself, so a stale bundle means the app runs old UI against new engine/protocol code" | done | **The alarm was real, the danger was not — and the difference is worth writing down.** Regenerating changed exactly two files: `.buildhash` and one `.js.map`. Every emitted bundle kept its content-hashed filename (`index-Ch43PeDP.js`, `index-CfYhyTm2.css`, …), i.e. the shipped code was byte-identical, so **no pending real-host walk was ever looking at old UI.** Cause: `git diff f25f26e -- web/src web/protocol web/index.html web/vite.config.ts web/tsconfig.json web/package.json` is EMPTY — no source has changed since the last bundling commit — yet the `.buildhash` committed by that commit (P6-3) disagreed with its own source. So the hash was written from an earlier state during that commit and shipped stale. A bookkeeping failure, not a shipping one. Lesson for the gate line: `npm run bundle` must be the LAST step before `git add`, or the hash records a tree that no longer exists |
| P3-X5 | build | **`check:tokens` was RED at HEAD — 12 violations, all in `plane/plane.css`** (same discovery): 7 hardcoded font-sizes, 2 hardcoded z-indexes, 2 hardcoded colors, one dangling var. Mostly the P3-L1 library-popover era | done | Sizes onto the scale exactly (9px→`--type-micro-size`, 7px→`--type-nano-size`, 11px→`--type-label/value-size`, 10px→`--type-caption-size` — every substitution is byte-equal at default tokens, so nothing moved). The library's z-pair becomes `calc(var(--z-popover) + 1 / + 2)`, preserving its old 39/40 ordering (above the matrix scrim, below `--z-menu`) with a comment saying so. **The gate earned its keep twice:** (1) `.plane-library-actions button:hover` read `var(--bg-hover, var(--bg))` and `--bg-hover` is defined NOWHERE — so the fallback fired and hover painted the SAME colour as rest, i.e. the library's New/Import buttons had no hover state at all; now `--bg-raised`, matching every other hover in the file. (2) `.plane-library-verb.arm` carried `var(--warn, #c66)` — a dead hex fallback behind a defined token, the kind of thing that quietly becomes the real value if the token is ever renamed. **One deliberate visual change:** the library popover's `0 8px 24px rgba(0,0,0,.45)` becomes `var(--elev-2)`, so its shadow is slightly tighter and gains the token's 6% top inset — the point of the gate being that the look must be theme-reachable |
| P3-F3 | fixture | **`plane_audio_test` intermittent heap-corruption SIGABRT at HEAD** (found 2026-07-30 gating P3-X2; PRE-EXISTING — 9/30 aborts with the P3-X2 diff stashed) | done | **A REAL SHIPPING BUG, not a test flake — ASan found it deterministically on run 1** (a `build-asan/` tree; the play worked exactly as written). `SlDispatch` `slRecord/start` sized the take from the REQUEST (`chan1 >= 0 ? 2 : 1`), but a `channelBus`/`mainMix` capture is ALWAYS STEREO in the engine's ring (sl_channel_test's own pin) — so REC-from-bus with `chan1:-1` (the P3-R2/R3 gesture, the UI's exact wire shape) opened a MONO take: `RecordService`'s drain scratch was allocated at HALF the delivered width and every bus-tap record STOP overflowed it 2× — the free-list corruption — while the WAV held interleaved halves at double speed (corrupt takes in the REAL app, not only the suite). Fix: ask the engine (`sl_tape_channels` after `record_start` — the pattern overdubStart used from day one); pin: §11b asserts the bus take is stereo. 0/30 aborts after (9/30 before); ASan end-to-end clean; ctest 66/66. Known ASan artifact recorded honestly: a DETERMINISTIC container-overflow report at `sl_engine_create` in vendored `signalsmith-stretch.h` (`tmpProcessBuffer` regrow inside libc++'s own `__append`) is a stale-annotation quirk, not a defect — with `detect_container_overflow=0` the independent heap checks pass the whole run, so the write is within its allocation. Run ASan loops with that flag |

## P3.5 — the strip becomes the deck

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P3.5-AUDIT | spec | Diff this section + STRIP-DECK.md against built rows; materialize gaps; then offer the USER GATE: library walk (no companion window), compose-window walk (edit heard, no stale world), deck-tile walk (real DJ rows in a strip), looper-routing walk, pads 1–8 | done (2026-07-29) | **Every P3.5 row built** (U8 L1 D4-M C1 C2 D4-3 D4-1a D4-1 D4-2 R1 R2 R3 P1); STRIP-DECK's sketch fully realized (tile = header verbs + pads + real GridPanel + LCM bar + channel row; SYNC not duplicated into the header — one control one home, recorded deviation). **User real-host confirmations so far: the deck tile's grid IS visible in WizardMerged (2026-07-29) — the first P3.5 feature real-host-verified; first ergonomic finding "hard to control its UI" → wheel-inside-tile was zooming the PLANE (fixed: the deckface stops wheel propagation, its rows scroll).** Gaps materialized: **P3.5-E1** (tile ergonomics pass beyond the wheel fix — pointer precision under plane scale, dj band sizing in a 692px tile — feeds on the user's walk notes) · known limits carried: C2 crashed-window lock, no cross-tile arrow ring, shared ⌘Z timeline, L1's WKWebView import-picker unverified, D4-2 nudge feel provisional (±4 hold-bend). **USER GATE (the remaining walks): library · compose window · looper routing (two strips) · pads 1–8 · tile MasterRow audible (VOL halves, DRV drives) · nudge bend-and-snap — on the user's machine, at their pace; findings land as E-rows.** ✅ **2026-07-30: the looper-routing walk is now WORTH WALKING** — it was gated on P3.5-E3 (a grid strip's bus was silent, so the walk would "pass" visually while the take held nothing). E3 shipped the `deckOut` cable and both gestures author it, so REC on a grid strip → its spawned looper now captures the deck for real. Expect audio in the take; if it is silent in the real host, that is a NEW finding, not the known defect |
| P3.5-E3 | build | **A grid strip's channel bus is SILENT** (D-SL-DECKOUT-01) — so P3-R3's signed gesture spawned a looper that recorded SILENCE | done | **THE MEASUREMENT DECIDED IT, and it went the second way.** (1) *Deck-out lane read inside `mixInto`* — REJECTED: the deck's audio would then sit in the grid channel's own bus, and the default `channelOut → main` route would pour it into main a SECOND time (the core already delivered it at the projected level). Suppressing just that pour makes `channelOut` mean the deck when it feeds a strip and not mean it when it feeds main — one endpoint, two meanings. (2) *Repurposing `AudioLane::deckA/B/C_L/R`* — REJECTED for a sharper reason: those lanes carry a deck only when it is split to a dedicated output, and `SlRenderSink` promises "a multi-out device gets the lanes the host maps" — so writing them unconditionally would put a deck's dry signal on physical outs 3/4 **as well as** main, in the real app. (3) **BUILT: `SourceEndpoint::deckOut` (kind 4)** — a route source naming the DECK, fed by a new read-only core accessor `NativeAudioEngineCore::deckDryOut` (the P6-3 `hostSendFeed` seam, read direction; post-stretch/carve, PRE-crossfader, PRE-deck-drive). External like a device input → constrains no render order, can never close a cycle, needed no scheduler change. Core edit is 2 lines + the accessor: an INACTIVE deck's dry buffers are now zeroed, because the core only ever WRITES them while a deck renders — a `deckOut` cable would otherwise loop that deck's last fragment forever after it was dropped (pinned as §4 of the fixture). Index space is the DECK's (3), never the channel's (8) — refused, not clamped. **Reachability: both shipped doors now carry audio** — `recordIntoLooper` (REC on a grid strip) and `record from ▸` author `deckOut`; `linkedLooperFor` matches it AND the old silent `channelOut` so pre-fix maps still read as a pair instead of spawning a second looper; `cablesOf` draws the pair; Inspector "in" says `<name> · deck out`; `hasOutput` counts it. Found en route: the routing ledger's `describe` ended in a catch-all `else` that would have labelled kind 4 "FX 5 return" — every kind is now named, unknown kinds say so. Gates: new `sl_deckout_test` (the empty bus PINNED, then the deck in the looper's bus, then **the take has energy**, then silence after `sl_deck_clear`) · ctest 67/67 · vitest 1467 · 7 drift gates · plane + session walks on the built bundle. SCHEMA v94, MAP v7 (identity migration — the enum only widened; the bump records the WRITE side). Remainder split out as **P3.5-E4** |
| P3.5-E5 | build | **Every map save silently ERASED the `deckOut` cable** (found 2026-07-30 by the plan audit, against SHIPPED P3.5-E3) — the one cable that carries a grid deck into a looper | done | **E3 shipped the write direction and not the read one, and the skip-the-unknown rule turned that into silent data loss.** `SRC_KIND` gained `deckOut: 4` (mapApply.ts:74, with its own ⚠️); its hand-written twin `SRC_KIND_NAME` still stopped at kind 3, and `captureRoutes`'s deliberate "a cable we cannot name is one we must not pretend to have saved" branch therefore DROPPED every deckOut route — on `saveMapAs`, on `exportMap`, and on the 4 s autosave. Reachability of the damage: REC on a grid strip → its looper is patched and records the deck (E3's whole point) → save → reopen → **the cable is gone and the looper is fed by nothing**, i.e. E3's silent-take failure restored, surviving only within one session. Nothing was red: the round-trip fixture simply carried no route of that kind. **Fixed as a class, not a line:** the two read maps are now DERIVED from the write maps (`invertKinds`), which are exhaustive over the route-kind unions BY TYPE — so a kind that can be issued is a kind that can be captured, by construction. Pins: the round-trip fixture gains a deckOut cable, plus a new test driven off `RouteSourceSchema.shape.kind.options` so the NEXT kind added to the enum fails until both directions know it. Both pins verified to FAIL against the pre-fix reader (`deckOut did not survive capture`). vitest 1485 · typecheck · 8 drift gates · bundle fresh |
| P3.5-E6 | build | **Clicking a `deckOut` cable unpatched the WRONG route — or none at all** (found 2026-07-30 by the plan audit, against SHIPPED P3.5-E3) | done | Second half of E5's lesson, same root: a site that switched on a cable's SHAPE instead of its KIND. `unpatch` (Plane.tsx) built the engine lookup as `srcKind = c.send === null ? 0 : 1` — true only while every drawable cable is a `channelOut` or a `channelSend`. A `deckOut` cable also has `send === null`, so it asked the engine to remove **kind 0 at the DECK's index** — and a deckOut's index is deck-space (3), not channel-space (8). Two silent failures: a `channelOut` route happening to sit at the same numeric index was **unpatched instead of the cable clicked**, or nothing matched and the **document dropped the route while the engine kept playing it** — audio from a cable no longer drawn, and no way to remove it. The document mutation runs unconditionally first, so the divergence was guaranteed in the no-match case. Fix: `routeKeyOf(cable)` in `cables.ts` — one exported pure function deriving the whole endpoint quadruple from the ROUTE via the same `SRC_KIND`/`DST_KIND` tables that ISSUED it (now exported from mapApply for exactly this reason: **one table, both directions**). Pinned in `cables.test.ts` per P6-2b's precedent (no jsdom in this project — pin the decision, not the markup): the deckOut key, the output/send keys, and the property that matters — **two cables at the same numeric index but different kinds must never share a key**, or removing one removes the other. Both pins verified to FAIL against the old inference (the property test collapsed 2 keys to 1). vitest 1488 · typecheck · 7 gates green (`params:check` red for an unrelated in-flight refactor, see header) |
| P3.5-E7 | build | **A `.scoopySession` FOLDER cannot be imported — the folder picker was LOST when the library moved to the plane** (user, real host, 2026-07-30) | done (machine, webkit) — **real-host walk pending (user)** | **A REGRESSION against a door that worked, introduced by shipped P3-L1.** The desktop's `.scoopySession` is a DIRECTORY. The deleted `CompanionPanel` had **two pickers plus drag-and-drop**, and its own comment says exactly why (`CompanionPanel.tsx:463-467`): *"a file input cannot select a directory (picking a `.scoopySession` folder fires NO event at all — the 'import does not react' bug), and a `webkitdirectory` input cannot select a file."* It carried `accept=".scoopySession,.zip"` **and** a second `webkitdirectory=""` input (`:489`) **and** a drop target. **P3-L1 rebuilt the import on the plane with only the FIRST of the three** — `Library.tsx:88-91` is a lone `<input type="file" accept=".scoopySession,.zip">`. So selecting the folder a user actually has fires no change event and the button appears dead. The reader side is FINE and needs no work: `packageFromEntries` (`sessionPackage.ts:74`) already takes folder-walked path→bytes entries, strips a common root prefix, and normalises separators — this is purely a missing DOOR. ⚠️ P3-L1's own handoff flagged the risk ("WKWebView `<input type=file>` import picker unverified") and P3.5-AUDIT never walked it; the user found it instead. Build: restore all three doors on the plane's library (file · folder · drop), funnelling into the one package reader. **Verify in the REAL host — WKWebView is where `webkitdirectory` is most likely to differ from Chromium, which is the whole reason this row exists.** gate: real-host walk — import a `.scoopySession` FOLDER, it appears in the library with its samples **BUILT 2026-07-30:** all three doors on the plane's library — `import` (zip), **`folder…`** (`webkitdirectory`), and the popover itself as a **drop target** (armed border + "or drop a .scoopySession here"), every one funnelling into `importSessionFile`/`importSessionEntries` → the one package reader. The reading moved to a shared `persist/folderImport.ts` and **`CompanionPanel` now uses it too** — its private directory walk is deleted, because two copies of this reading is precisely how the plane ended up with one door out of three. Pins (no jsdom, so the READING is what is tested): `readDrop` puts the entry API **before** `files`, since a dropped folder ALSO appears in `dataTransfer.files` as a useless 0-byte File and that branch would import an empty archive — **verified to FAIL against the naive files-first reading**; `dirNameFromRelativePaths` names the session after the ROOT segment, never after one of its own samples, and says `Imported Session` rather than guessing; a picked FileList carried end-to-end into `packageFromEntries` with its samples intact. **Walked in WEBKIT — the engine the app ships (H4):** the session walk gains a step that writes a real `Demo.scoopySession` directory to disk, drives the restored picker over it, and checks the library lists it, `/sessions/Demo/pattern.json` is non-empty on the native slFiles route, **both samples landed under `/samples/Demo/`**, and the note line says `imported Demo`. Green in webkit AND chromium. vitest 1509 · typecheck · 9 gates · walk matrix 7×2 · bundle fresh. **⚠️ REMAINS USER-GATED and the row's own warning is why:** Playwright's WebKit proves the DOM contract, not WKWebView's native open panel — the real host is where a `webkitdirectory` input might still refuse to offer a directory. If it does, the drop door is the working fallback and the fix is a native `chooseDirectory` verb (in the schema at `:457`, answered by NOBODY in the merged shell — measured this session) |
| P3.5-E8 | build | **In compose, nothing loads a sample** (user, real host, 2026-07-30: "in compose it wont let me load any audio samples") | **measured → split** | **THE ROW AS WRITTEN WAS WRONG ABOUT WHERE THE DOOR IS MISSING, and the measurement is the useful part.** The original note said "no door ANYWHERE" and proposed giving `FileBrowserPanel` a home. But `GridPanel` has always drawn TWO sample doors on every audio track row — **LOAD** (`trackRowControls.tsx:2351` → `trackEdit/loadSample`) and a library path dropped/double-clicked onto the row (`fileBrowser/load`) — and they render in the compose window like everywhere else. Each is only an INTENT: `BrowserLink` forwards it to a handler the mounting surface registers, and **`trackEdit` falls through to a silent `{ok:true}` when none is** (`browserLink.ts:332`, the same silent-accept the solo/locator comments there already call out). Three surfaces registered the pair by hand — the companion, and the deck tile per deck — and **`useComposeBinding`, the surface a person actually composes in, registered NEITHER**. So compose's LOAD accepted every click and did nothing. Split: **P3.5-E8a** (the doors are registered — the user's literal report) · **P3.5-E8b** (the file BROWSER still has no home, which is the original row's other half and the part that interacts with P11-1) |
| P3.5-E8a | build | The compose surfaces register their sample doors | done (machine, webkit+chromium) — **real-host walk pending (user)** | `panels/sampleDoors.ts` — ONE `registerSampleDoors(link, deck, scope)` now imported by all four surfaces (companion · deck tile · compose window · in-window composer), replacing three hand-written copies and the one omission that caused the row. **`deck` and `scope` are deliberately separate arguments**, because conflating them is the mistake: `deck` is whose DOCUMENT the sample lands in, `scope` is which handler slot answers — a deck tile addresses the grid WITH a deck (P3-D4-1), while compose surfaces mount one composer with no deck axis and must answer the DECKLESS intent while still writing into their own deck. **A SECOND REAL DEFECT, found by the walk and fixed here: `pickAudioFile` preferred `showOpenFilePicker` where it exists, and the LOAD intent crosses an async command hop that has already spent the TRANSIENT ACTIVATION that picker demands** — so in Chromium the click produced no picker AND no error (the same dead button, one engine over), while WebKit has no such API and worked. The FSA branch is deleted: unreachable in the shipping host, silently broken in the other engine, and the plain input takes the same `accept` list and returns the same File. Pins (`sampleDoors.test.ts`, 6): the silent-accept is pinned AS the defect (unregistered → `{ok:true}`, no picker), a registered compose surface opens a real picker, compose-scope answers deckless-only and deck-scope its deck only, and `fileBrowser/load` reaches the document (the store's honest "open a session first" is the proof it arrived). **Walk, in WEBKIT and chromium:** a compose window is opened at its real address, LOAD is clicked on a track row, a **real file chooser opens**, a WAV is picked and **lands in the library on the native slFiles route** — and the step was **verified to FAIL with the registration removed** ("LOAD opens a real file chooser" times out, which is the user's report reproducing). vitest 1515 · typecheck · 9 gates · walk matrix 7×2 · bundle fresh. **Real-host walk: open COMPOSE ⇱ on a strip → LOAD on a track → pick a WAV → it plays on that step.** |
| P3.5-E8b | build | The file BROWSER has no home anywhere | done (machine, webkit+chromium) — **real-host walk pending (user)** | **The home is a FILES drawer in the compose surfaces, and the same one in both.** `plane/ComposeFiles.tsx` — an always-visible vertical tab at the composer's right edge that opens `FileBrowserPanel` beside the grid — mounted by the compose WINDOW (`ComposeWindow.tsx:105`) and by the in-window overlay (`Composer.tsx:62`). One implementation, two surfaces, because E8a's root cause was three hand-written copies of a registration and a fourth surface that shipped without it. The tab renders while CLOSED and the panel mounts only when open (a hidden browser subscribes to a topic and decodes peaks — cost with nothing on screen). **Deliberately NOT in `PANEL_MENU_SURFACES`** (conductor ruling, adopting this row's own standing recommendation): P11-1 retires `≡ panels`, and a door that never lived there cannot be orphaned when it goes — so E8b needs no rework and P11-1 inherits no obligation. Pinned as a test so a later row cannot add it back by accident. **The transient-activation warning was checked, and the answer was the good one — so it is PINNED rather than assumed.** `onClick` → `browse` → `MergedLink.command` → `BrowserLink.command` → `FileBrowserBackend.handle` → `chooseFolder` → `input.click()` is ONE synchronous run; every `async` boundary calls through before its first `await`, so activation is still live. Unlike `pickAudioFile`, which E8a had to fix, this door was never dead. The pin asserts the input was clicked BEFORE the promise is awaited; inserting `await Promise.resolve()` ahead of the picker fails it. **Two REAL defects found by MEASURING the door rather than reading it, and in both the branch that ships in WebKit was the broken one.** (1) `pickViaInput` and `collectFiles` disagreed about `webkitRelativePath`: the File-System-Access walk starts *at* the picked folder (`kick.wav`), an `<input webkitdirectory>` includes the root segment (`Kicks909/kick.wav`), and `importFiles` joins that onto a destination already ending in the folder name — so the input branch wrote `/samples/Kicks909/Kicks909/kick.wav` while `chooseFolder` navigated to `/samples/Kicks909`, showing ONE FOLDER AND NO SAMPLES. Fixed by `stripRoot` (`fileBrowserBackend.ts:383`), which strips only the segment matching the folder's own name so sub-folder shape survives. (2) `importFiles` creates directories lazily per audio file, so a folder of non-audio left the destination non-existent and `chooseFolder` navigated there anyway — toast said "imported 0 samples", list said "cannot read /samples/Docs". It now stays put and says `no audio in that folder (N skipped)` (`:313`). **A third reachability hole, exactly this row's species:** the friendly `Choose folder…` button rendered only in the panel's `!root` empty state, and on this host `root` is NEVER null — the library exists, it is merely empty the first time. The only folder door was the `⌘` glyph, which reads as decoration. The empty LIST now carries the button too (`FileBrowserPanel.tsx:332`). Load side needed no work, verified by reading: `browserLink.ts:391` routes `fileBrowser/load` to the COMPOSE-scope handler E8a registered; because drawer and grid are now the SAME webview, the custom-MIME drag path applies rather than the cross-webview `text/plain` bridge. ⚠️ **E8b and E7 rest on ONE unverified thing** — see E8f. gate: real-host walk — in a compose window, open the FILES drawer, pick a folder of WAVs, land one on a track |
| P3.5-E8f | spec | **E7 and E8b share ONE unverified dependency: `<input webkitdirectory>` inside WKWebView** | **RESOLVED 2026-07-30 by the user's real-host walk — the good direction** | The user walked it: **E7's `folder…` button WORKS in WizardMerged.** So WKWebView's native open panel DOES offer a directory to an `<input webkitdirectory>`, and neither folder door needs the native `chooseDirectory` verb. That verb stays declared-and-unanswered in the schema at `:457`; it is not required by E7 or E8b and should not be built on their account. ⚠️ This also NARROWS P3.5-E8g: the picker element class is not the problem there, since this one delivers |
| P3.5-E8g | build | **The compose window's LOAD door opens the picker and the file never arrives — the track stays empty** (user, real host, 2026-07-30) | **DONE — confirmed by the user in the real host: "samples load now"** | Closed by P3.5-E8g-a. The row's whole value was the route it took: the door had five outcomes and four said nothing, so one user report could not name the seam and no test could either. Instrumenting every branch turned the next walk into a diagnosis — the header reached `<file> → track N` and the row stayed empty, which **exonerated the pick, the import, the decode and the document write in a single observation** and retired two candidate rows on the spot. ⚠️ The report inverted on measurement: the user's words were "the file never loads". It loaded every time. What failed was the only evidence a person had that it had. **The lesson worth keeping: when a door is silent, instrument it before theorising about it** — four silent branches cost a round trip, one honest line cost none |
| P3.5-E8g-a | build | **The compose grid does not repaint after a sample lands on a track** | **done — real-host walk PASSED on BOTH callers (user, 2026-07-30): "samples load now" · "files browser works too"** | **None of the row's four candidates was the defect.** The store→backend chain measured correct end to end; the hole was one layer out. The sample doors were **the only doors in their family with no path of their own to the screen** — `toggleLaunch`/`toggleSolo` republish from their own handler in all three surfaces (`CompanionPanel.tsx:66,71`, `useComposeBinding.ts:36,40`, `deckTile.tsx:89,93`, verified), while the sample doors republished nothing and delegated the repaint to a React effect in a different component, three layers from the store. When that effect did not fire, **the door was indistinguishable from the defect E8g was opened for**. Fix: `registerSampleDoors` republishes after `loadSample` resolves, via `updateRuntime` — the correct verb, since a sample landing is a runtime change and nothing about a sample is on the pattern wire (`toGridPattern` carries no sample field). Scoped peak paths move with it, or a runtime push would name a file `gridPeakPathsByScope` has never heard of and draw a named row with no waveform. In the one module both compose surfaces share, so neither can ship without it. ⚠️ **Its pins still cannot see a repaint** (no jsdom, no React renderer) — they stop at the topic push, and deliberately do not simulate the reload effect, because faking it would have pinned a chain that was never broken and stayed green straight through the defect. **The user's walk is what closed this, not the suite** (P3.5-E8g-f). Both callers were walked separately and both repaint — the LOAD door and the FILES-drawer double-click — which matters because they enter through different callers into the same `loadSample`: a fix that worked for one and not the other would have meant the caller, not the store |
| P3.5-E8g-d | build | **`toggleLocatorRepeat` repaints only via the reload effect** | done (machine) — **real-host walk pending (user)** | **The row inherited E8g-a's diagnosis AND its prescription. Measured at HEAD, the diagnosis holds and the prescription does not** — which is the whole value of this row. Same as E8g-a: `toggleLocatorRepeatTrack` writes the document and pushes nothing at the grid backend; its only route to the panel was the binding's session-keyed reload effect, three layers away in another component, and `companionEngine.ts:1141` admitted it in a comment. **Different from E8g-a:** a sample landing is a RUNTIME change, so `updateRuntime` was right there — but `locatorRepeatActive` is a `toGridPattern` field (`gridProjection.ts:181`) and appears **nowhere** in `toGridRuntime`. The proposed one-liner would have republished every `gridRuntime/<i>` and **left the lamp the user clicked showing the old state — a fix that repaints everything except the thing that changed.** Pinned as a driven falsification, not an argument. So the verb is new: `GridBackend.updatePatternRow(i, pattern)`, the pattern-wire twin of `updateRuntime`. It re-projects ONE row through `docRows` (the one place that knows a row is `sectionA[i]` paired with `baseSettings.trackSettings[i]`) and republishes only that track's topic — **an edit on track 3 cannot disturb a drag in flight on track 5**. It deliberately does NOT reset the cursor or the armed lanes the way `load()` does. `gridDocument(deck)` exports the **ACTIVE SCENE's** projection, and must stay a projection: the flip is scene-aware, so pushing raw `sectionA` while scene C is live would repaint with a pattern the engine is not playing. Pinned. Three registration sites matching the launch/solo shape beside them (`CompanionPanel`, `useComposeBinding`, `deckTile`) rather than a shared module — every surface already registers this handler, so `sampleDoors.ts`'s case (a fourth surface with NO registration) does not apply. ⚠️ Pins stop at the topic push — no jsdom, no React renderer. gate: real host — ↻ on a track and the row shows the locator window without any other interaction |
| P3.5-E8g-e | build | **A session reload resets the grid cursor to track 0** | done (machine) — **real-host walk pending; ⚠️ see P3.5-E8g-h, the user's actual want is different** | Confirmed by the user in the real host (*"yes it lands on track 1"*). Premise verified both halves, plus **a third thing that is what makes them a defect**: the bindings' reload effect is keyed on the session OBJECT, and every document edit replaces it — so `load` runs on **every edit**, not just when a session opens. Each of the three is reasonable alone, which is why nobody had reported it: **it takes two doors in a row to see.** ⚠️ **A second defect fell out of the same cause and was not in the row:** `load` also cleared `activeParams`, so **every value-drag fell back to pitch after one edit**. Same cause, same fix, pinned. The distinction IS the row, so it is made explicitly rather than inferred: `load` now takes the document's identity and resets only when it changes. **A cursor is a promise about a specific row and means nothing once row 5 is a different track**, so carrying it across a session change would put a dropped sample somewhere the user never pointed. Callers pass `session.name` — the identity on disk, since `createSession` de-duplicates and `renameSession` refuses a collision. An ABSENT id reads as "different" in both directions, so a call site that never opts in behaves exactly as before. Two edges the unconditional reset used to hide, now pinned: the cursor is **clamped** when an edit deletes tracks out from under it (a `selectedIndex` no row answers to is where a dropped sample would vanish), and an emptied document leaves it at 0 rather than −1. A SCENE switch deliberately keeps the cursor — same document, same tracks, only pattern-scoped fields differ. **Pins go red in BOTH directions** (forcing `sameDocument` false reddens 5, forcing it true reddens 3), so the distinction itself is covered rather than the happy path. ⚠️ They cannot see the selection HIGHLIGHT move — no jsdom, no React renderer; they drive the reload by calling `load` the way the bindings do. What they prove is **where the next `fileBrowser load` is aimed**, which is the half the user feels. gate: real host — select a row, edit a cell, and a FILES double-click lands where the cursor was |
| P3.5-E8g-f | fixture | **The repaint chain has no gate that can see a repaint** | todo — **a dependency decision the conductor/user owns** | E8g-a's pins stop at the topic push because there is no jsdom and no React renderer: `react-test-renderer`/`@testing-library` are not devDependencies. **So every "the surface re-rendered" claim in this project is walk-only.** Either add a renderer and pin `TrackBand`'s name from a driven store, or write it down explicitly as a walk-only property. ⚠️ Third of its kind — same species as P11-1-e (layout wrap) and P3.5-E8g-c (thin DOM fakes): a property no gate can see is a property that will regress |
| P3.5-E8g-b | spec | **`decodeAudioData` in a non-plane WebView is unverified** | **MOOT 2026-07-30 — answered by the user's E8g walk** | The walk reached `<file> → track N`, which is set only after the sample decoded and the document was written — so `decodeAudioData` **does** work in the compose WebView. The row was opened because the FOLDER door never decodes and therefore could not answer this; the LOAD door did. No work here. Kept as a record so the question is not re-asked |
| P3.5-E8g-c | fixture | **Audit the no-jsdom fakes that stop at the gesture** | done (2026-07-30) | The audit surface was **three** files and only one still had the hole — and it was the worst possible one: the test covering `chooseFolder`, **the door the user PROVED works in the real host**. It faked `body.append` and `input.remove` as no-ops recording nothing, so the single property E8g had to go measuring for — *the input is in the document when clicked* — was the one thing that door's own test could not see. Demonstrated rather than asserted: deleting `document.body.append(input)` from `pickViaInput`, making it byte-for-byte the shape `pickAudioFile` had, left **all 16 existing tests green**. It now fails one. ⚠️ The pin does **not** claim that shape is E8g's cause — real WebKit fires `change` on a detached input, measured in E8g. It pins which shape is **PROVEN in the shipping host** (E8f walked it), so a later tidy-up cannot swap it for the one that is not, and so unifying the two pickers has a net under it. The general lesson stands with P3.5-E10's: **a fake thin enough to pass either way is a green gate that cannot see the defect.** gate: the deletion that used to be invisible now goes red — verified |
| P11-1-a | spec | **`≡ panels` cannot leave the bar yet — and FX 1–4 are not the only reason** | **DECIDED 2026-07-30 (user): `≡ panels` STAYS INDEFINITELY** | The measurement stands — the five `PANEL_MENU_SURFACES` (spectral · paintmode · midi · perf · capture) have no other door, `openPanelWindow` has exactly four call sites all in `PlanePanel.tsx`, and D-SL-TOPROW-01 names a new home only for settings. **The user's ruling: keep the button.** It works, it costs one control, and retiring it would either orphan five surfaces or spend a row inventing a home for tools that are not map verbs — the same category mismatch that kept `library ▾` out of `map ▾`. Rejected alternatives, recorded so they are not re-proposed: folding them into `map ▾` (tools are not map verbs), a new `tools ▾` (a second menu, against the decision's intent to reduce bar clutter), and retiring the five surfaces outright (no evidence they are unwanted). **P11-1 is therefore complete as shipped, not scoped-pending-a-decision** |
| P11-1-b | build | **`compose` may leave the bar safely — the per-strip door already exists** | todo | Measured for P11-1: every grid strip already draws `COMPOSE ⇱` in its param row (`GridElement.tsx:265-276` ← `Plane.tsx:660` ← `PlanePanel.tsx:1092`), and the bar's button only ever opened the FIRST grid strip. Retiring it orphans nothing. Carry this into P11-1 rather than re-measuring it |
| P3.5-E9 | spec | **A loaded scoopy session makes no sound** (user, real host, 2026-07-30) | **measured → split** | **THE MEASUREMENT, 2026-07-30 — and it found a defect that is real whatever the user's own cause turns out to be.** The store has always KNOWN why a deck is quiet: `open()` collects `decodeFailures` (a kit sample whose bytes would not decode) and `missingSamples` (a track naming a sampleId the kit does not carry), and `publish()` returns immediately while the sink is not running (`companionEngine.ts:365`). **All three are read by exactly ONE surface — `CompanionPanel:229` — the browser-only shell whose door P3-L1 DELETED.** Nothing on the plane, in a strip, or in the compose window renders any of them (`grep` over `web/src/plane`, `GridPanel`, `trackRowControls`: zero hits). So in the merged app a session loads, looks completely normal, and plays nothing, with the reason on no screen anywhere — which is also why the report could not be diagnosed from here: *the app cannot tell you*. Split: **P3.5-E9a** (the silence explains itself — built) · **P3.5-E9b** (the real-host measurement, now that the app reports; user-gated). Also confirmed en route: the merged host DOES decode samples web-side and ship the PCM over the bridge (`nativeAudio.ts` `registerSample` → `sl_engine_register_sample`), so a decode failure in the WebView is a silent track in the NATIVE engine — the web and native halves are not independent here |
| P3.5-E9a | build | The silence explains itself | done (machine, webkit+chromium) — **real-host walk pending (user)** | `store/sampleReport.ts` `silenceNote(session, {engine, decodeFailures, missingSamples})` — one pure sentence, rendered by the plane's note line (after a strip load) and the compose window's bar (which opens its own session in its OWN store, so it must report its own kit). Rules pinned because each is a way the report would be worse than none: **the engine comes first and short-circuits** (a sample list under a dead engine sends a person hunting for files); **`starting` reports NOTHING** (every surface boots its sink asynchronously, and a warning that flashes on every load is how a real warning is learned as noise); the list **names up to four and counts the rest** — no silent truncation; and the line ends in the CONSEQUENCE ("those tracks are silent"), because a count without it reads as a metadata warning. 8 vitest pins. **Walk, webkit + chromium:** the imported session fixture now carries one sample that is deliberately NOT audio — it rides the import untouched (bytes are bytes) and fails to decode on load — and the walk asserts the note line names it, names the file, and says what it means. **Verified to FAIL with the note suppressed: `(no note at all)`, which is the user's experience exactly.** vitest 1523 · typecheck · 9 gates · walk matrix 7×2 · bundle last |
| P3.5-E9b | spec | The real-host measurement, now that the app reports | **RESOLVED 2026-07-30 (user, real host): "sessions are audible now"** | The diagnostic never had to be read, because the cause was found from the other end: **P3.5-E10** — the whole DSP path was compiling at `-O0`, 12.1× slow on the stretcher, and the user's original "a loaded scoopy session makes no sound" plus "huge weakness past one deck" were the same defect seen twice. With an optimised build the sessions play. E9a's `silenceNote` readout stays and stays useful: it is the door that would have said WHY, and it is now the thing to read first when a deck is quiet. Nothing further to measure here |
| P3.5-E10 | build | **The app was built with NO optimiser — `CMAKE_BUILD_TYPE` was empty, so the entire DSP path compiled at `-O0`** (user, real host, 2026-07-30: "immediately shows huge weakness when playing more than 1 deck"; the original ScoopyLoops ran three decks with extreme time-stretch on the same machine) | done (machine, measured) — **real-host confirmation pending (user)** | **NOT an architecture regression — the optimiser was simply absent.** Root `CMakeLists.txt` set no default build type, and an unset `CMAKE_BUILD_TYPE` is not a mild default: every engine object compiled as `-std=c++20 -arch arm64 -isysroot ... -mmacosx-version-min=14.8`, with **no `-O` and no `-DNDEBUG`** (`build/**/flags.make`). `cmake -B build` warned about nothing, and `build/` is the tree that produces `WizardMerged.app`. Measured 2026-07-30, same commit and source, `-O0` vs `-O3`: `sl_tape_stretch_test` **3.38s → 0.28s (12.1×)**, `sl_tape_varispeed_test` **1.39s → 0.09s (15.4×)**. A 12× slower stretcher is exactly the reported shape — one deck survives, three collapse. Fix: default to `Release` when unset, `FORCE`d into the cache with a `message(STATUS)` so the choice is visible; `-DCMAKE_BUILD_TYPE=Debug` stays one flag away. `build-pdc` was already `Release`, which is why the drift was invisible to anyone who happened to look there. ⚠️ **Every latency figure in P6 was measured through this build** — E10a. ⚠️ **No gate could have caught it**: the suite asserts correctness, never time — E10b. gate: real-host — open the Release app, run three decks with extreme stretch, hear it hold |
| P3.5-E10b | fixture | **A performance gate: N decks with extreme stretch inside a CPU budget** | done (2026-07-30) | `slengine/tools/sl_perf_test.cpp`, registered in the slengine foreach (TIMEOUT 120). Real `sl_engine`, real worlds via the C ABI, offline blocks, no device and no GUI — so it runs in routine `ctest`, unlike the real-host walk. Three decks in `timeStretch` at ratio 1.6, chosen because the core bypasses the phase vocoder when ALL active decks are neutral (`NativeAudioEngineCore.cpp:2421-2434`), so a neutral scene would have measured nothing. Deliberately NOT via the dispatcher: the question is what the ENGINE costs, and a JSON round-trip per publish would put JUCE's parser in the measurement. **⚠️ THE FIRST DRAFT FAILED ITS OWN AUDITION, AND THAT IS THE ROW'S REAL FINDING.** Written as a realtime-ratio budget alone, it was GREEN against the very regression it exists for. Measured, three decks stretched: **-O3 = 0.047x realtime, -O0 = 0.273x** — 5.8x worse and still comfortably inside a 0.5x budget. The reason is structural, not a badly chosen number: **a budget with enough headroom for a slow CI runner has more headroom than a 6x build regression needs.** One threshold cannot do both. So the gate has **two halves**: (1) **build configuration asserted directly** — `NDEBUG` absent means no optimiser, which is machine-independent, exact, and covers the whole class P3.5-E10 belongs to; a deliberate Debug build must not be able to produce a green performance gate, and the documented escape hatch is `ctest -E sl_perf_test`; (2) **the realtime budget stays deliberately loose at 0.5x**, catching what (1) cannot — an algorithmic blow-up in an OPTIMISED build. Verified both ways: green at `-O3` (exit 0), red at `-O0` (exit 1) naming the cause. Also prints `deckMissCount` from the HotFrame but does NOT assert on it — it read 0/2/12 across otherwise identical runs, so it is a diagnostic here, not a gate; P11-5 owns the live readout. ⚠️ The budget is a measurement, not a guess: re-measure and rewrite the figure here before tightening it. gate: the fixture IS the gate — `ctest --test-dir build -R sl_perf_test`, 44/44 green in Release |

## P6 — plugins on the returns

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P6-3 | build | A plugin is AUDIBLE on a return (= old P3-3-1/3-2): instantiate per-return `NativePluginSlot`, `returnFx: true`, strip sends 1–4 → return → plugin → wet lane audible for BOTH element kinds; plane_audio_test fixture (send up → return energy; send 0 → silence) | done | **PROVEN WITH A REAL AU**: new ctest gate `plugin_audible_test` (Apple-only, links the real `.mm`, SKIP 77 without AUDelay) — tape strip: tone → send → AUDelay → **tail 0.37 on MAIN after the input dies**, unfakeable; grid strip: deck world + strip send fader → **gridTail 0.28 after the world stops**; unload → tail gone. Three measured laws en route: (1) the channel mixer adds strip sends AFTER `core.render` and render() REBUILDS the send lanes every block, so tape-strip sends reach a plugin only via the core's new `hostSendFeed` buffers (seam writes last block's strip-send diff — one block late, P6-6's territory; grid sends are core-internal, same-block); (2) host-mode returns CONSUME the send bus and zero the lane on the way out — by design ("wet returns to main"), so §16's grid half pins the command chain, not the lane; (3) returns 1/2's wet stage was snapshot-gated (`ret1 != nullptr`) — a worldless host now takes the same imperative path as returns 3/4 (no-snapshot fallback, flag-guarded). Plus: lock-free `isLoadedLockFree` (audio-thread probe; `hasPlugin()` takes the slot mutex), `returnHostActive`, `returnFx` capability rides the scanner (companionEngine's hardcoded `disableReturnFx: true` now reads the capability — sends travel in the merged host, stay zeroed in the browser). `send` is 0-BASED at the ABI (§11's convention, two fixtures corrected). OPEN measurement: per-track grid send dials (world `send1Level` → lane) unproven — the strip-fader path is; dials ride P6-AUDIT. ctest 66/66 · vitest 1452 · plane+walk · bundle fresh · app boots. **Real-host: FX 1 → pick a plugin → raise a strip's SEND → wet in the mix** |
| P6-2b | build | **The `≡ → FX n` door addressed the WRONG return** (found 2026-07-30 while measuring P6-4) — the menu passed the 0-based menu ROW as the window's arg, and `FxSlotPanel` reads that arg as a 1-based `returnIndex`: so **`FX 1` sent "0"**, the panel indexed `fxSlots[-1]`, got undefined and fell through to `WaitingForState` — **"waiting for state" FOREVER**, indistinguishable from a slow engine. `FX 2`…`FX 4` quietly drove returns **1…3**, and **return 4 had no door at all**. 0 also fails `selectFxPlugin`/`fxSlot`'s own `min(1)` on the wire, so nothing on that window could have worked | done | A defect against SHIPPED P6-2/P6-3, and it would have killed their pending real-host walk on step one ("≡ FX 1 → RESCAN → pick"): the first door a person opens is FX 1, the one that could never render. Fixed on BOTH sides, because they fail differently: the menu now builds from `[1,2,3,4]` via an exported `FX_MENU_RETURNS` whose **label and arg are the same number** (pinned — the only arrangement that cannot drift again), and the panel's addressing became a pure `addressedReturn()` that returns 1–4, or **null for a present-but-invalid address, rendered as a visible refusal** ("not addressed — returns are 1–4") instead of an eternal spinner. ABSENT still means return 1 (the old `?? 1`, the browser dev host's normal case). Pins: `panelMenu.test.ts` (labels match args, all four reachable) + new `FxSlotPanel.test.ts` (0 refused by name, 5/−1/2.5/"banana" refused, absent = 1, and the invariant that no returned value can index outside `fxSlots`). vitest 1476 · typecheck clean. **Made a pure function rather than tested through the DOM on purpose: this project has no jsdom, so the house pattern renders only window-free components — the DECISION is what needed pinning, not the markup** |
| P6-4 | build | Plugin EDITOR windows, JUCE-hosted (the job FxSlotWindowController.swift did on the desktop): open/close per return; editor visibility state per F1–F4 | done | **The window machinery already existed** — `PluginEditorWindow` + lazy create + hide-without-destroy + the window's own close-box callback have been in `NativePluginHost.mm` since P6-1. What was missing was the LANE and, decisively, a DOOR. Built: two lock-free mirrors (`editorAvail`/`editorShown`) published exactly where `loaded` is, because `editorWindow` is message-thread-only and a cross-thread peek at a `unique_ptr` is a race however harmless it looks; `NativePluginSlot::editorAvailable/editorVisible` on BOTH implementations (real + the JUCE-less stub, so flag-ON≡OFF holds); ABI `sl_fx_editor_available/_visible/_set_visible`; the dispatcher's **first-ever `fxSlot` handler** (the method was in the protocol and answered by nobody) wiring `toggleEditor` and refusing the other three ops BY NAME as P7-MIX-0's; `editorVisible` + new `editorAvailable` in the ~2 Hz toolbar push, replacing the hardcoded `false` this row's marker sat on. **THE STATE IS THE WINDOW, NEVER THE REQUEST** — `setEditorVisible` marshals to the message thread and returns immediately, a plugin without an editor makes "show" a no-op, and the user can close the window by its close box; the mirror is written INSIDE the marshalled work, and the toggle reads the engine so a hand-closed editor still reopens on one click. Same display-what-is-TRUE rule as `sl_channel_monitor`. **DOOR: `≡ → FX n ⇱` → EDIT, on the FX window** — and it had to go there: the desktop's EDIT lives on `DeckMixerPanel`, which P3-P1 deliberately left DOORLESS, so building the lane alone would have shipped an unreachable feature (rule four). Recorded deviation from the byte-identical desktop panel; it rehomes to the strip at P7-MIX-0. Gates: `sl_dispatch_test` extended (no engine · returnIndex 0/5/−1 · nothing-loaded refused by name · all three unbacked ops refused by name · ABI boundary reads) + the exact FxSlotState key set re-pinned + **the real C++ push parsed through the real zod schema via `--dump-toolbar`** · `plugin_audible_test` (compiles the real `.mm`, loads a real AU) green · ctest 67/67 · vitest 1476 · 7 drift gates · both walks · bundle fresh. **HONEST LIMIT: no headless test can prove the window APPEARS** — this binary links the stub and runs with no message loop, so "a real editor opens, and closing it un-lights EDIT" is P6-AUDIT's real-host walk |
| P6-5 | build | THE MAP REMEMBERS (the user's sentence made literal): per-return fx-slot state — plugin `identifier` + opaque state blob, restored on map open, honest refusal when a plugin is missing | done (via P6-5a + P6-5b) | Split on measurement: the engine/wire half PROVES ITSELF with a real AU headlessly, the document half needs schema + migration + capture + restore + a refusal surface. One row would have been ~2× the loop's LOC guidance and a single commit spanning both. Same shape as P3-M-1's ABI/verbs split. **`mode`/`output` are deliberately NOT persisted:** nothing in the merged host can set them (P6-4 refused those `fxSlot` ops by name; `hostToHardware`/`postFader` are hardcoded false in the push), and persisting a field no control writes and no apply path reads is how a document acquires fiction. They land with the controls at P7-MIX-0 |
| P6-5a | build | The engine/wire half: read what a document must store (identifier + opaque state), and restore from it | done | Host already did the hard part — `loadAsync` has applied a non-empty state blob at instantiation since P6-1; the member `loadedIdentifier` existed with no accessor. Added: `NativePluginSlot::loadedIdentifier()` (both implementations, real + JUCE-less stub); ABI `sl_fx_plugin_identifier` / `sl_fx_plugin_state` (size-then-fill: return value is the FULL length whatever the buffer, because a silently clipped blob persists something the plugin rejects on restore) / `sl_fx_plugin_select_state`, with the stateless `sl_fx_plugin_select` now FORWARDING to it so the two doors cannot drift apart in validation; `selectFxPlugin` gains optional `state`; new `getFxSlotState` read command — a command and not a toolbar field because a blob with an impulse response runs to hundreds of KB and no UI displays it. **THE IDENTIFIER, NOT THE NAME:** `createIdentifierString` is JUCE's persistence key; a document keyed on the display name reloads a different plugin on a machine that has two, and the fixture asserts the two strings actually differ so it cannot pass while proving nothing. Signed trade, pinned: a state blob the plugin REJECTS leaves it loaded at DEFAULTS rather than failing the load — a map that remembers wrong is a bad afternoon, a map that will not open is a lost set. Gate: new `plugin_state_test` (real AUDelay, Apple-only, SKIP 77) drives load → read → drop → **restore through the DISPATCHER with the exact JSON the map layer will send** → same identifier AND same state bytes; plus the truncation contract, a corrupt blob still yielding a working plugin, and an unknown identifier leaving the slot EMPTY rather than holding some other plugin. Self-reports its sizes (`identifier 35 B, state 248 B`) so a run where the plugin saves nothing cannot look like a state round-trip that passed. ctest 68/68 · vitest 1476 · SCHEMA v96 |
| P6-5b | build | The document half: `.scoopyMap` carries per-return `{identifier, state}`; captured at save, restored on map open, honest refusal when the plugin is missing | done | **The user's requirement is now literal: a map remembers its plugins.** `MapSchema.fx` is a TUPLE of exactly four `{identifier, state}` (a 3-entry array would silently leave FX 4 unwritten on every load and read as "the plugin vanished" — refused instead, pinned). MAP **v8** + migration to four EXPLICIT empty returns, which is not the same as omitting the field: an explicit "nothing loaded" is what lets a restore tell an empty return from a map that never knew about returns. Captured from the ENGINE in `captureMap`, beside the routing read-back and for the same reason — a plugin's settings are made in its own editor (P6-4), which the document never sees, so saving what we last ASKED for would persist a blob from before the user touched a knob. Guarded independently of the routing read so a hostless build's refusal cannot cost a routing capture that already succeeded. `planApply` emits all four EXPLICITLY, empties included (the monitor/DRV law: a running engine inherits the previous map's plugins). Slots are matched by `returnIndex`, never list position — pinned, because trusting the order puts FX 3's plugin on FX 1, silently. **THE PORTABLE-MAP HAZARD, closed:** open a set on a rig lacking one plugin and that return is empty in the engine — HONESTLY so — and a naive save would then ERASE the plugin on the machine that did have it. The restore is the only party that knows it TRIED and this machine cannot resolve the identifier, so it records those and `captureFxSlots` preserves their stored entry verbatim, blob included. Deliberately NOT "keep the document's value whenever the engine reads empty", which would make unloading a plugin on purpose unsavable. **The refusal reaches a person:** `applyMap` checks the wanted identifiers against `listPlugins` BEFORE loading (the engine cannot tell you afterwards — a select is async and an unresolvable identifier just leaves the slot empty, `plugin_state_test` §6), skips those loads, and returns warnings that `openMap` carries to the plane's note line NAMED ("FX 1: this machine does not have that plugin") rather than counted. A failure to ASK is not a failure to load: a host that refuses `listPlugins` skips the check instead of accusing every plugin. Gates: 8 new vitest pins (capture · returnIndex matching · the erase hazard end-to-end · hostless keeps the document · migration · blob round-trip verbatim · tuple refusal · idempotent re-save) · vitest 1484 · ctest 68/68 · 7 drift gates · both walks. **Unblocks P8-6's map-round-trip gate**, which needed this row by name
| P6-6 | build | ~~PDC per the signed policy~~ | **refused-as-written → split (D-SL-PDC-REF-01 SIGNED 2026-07-30)** | **The plan audit found this row unbuildable and it was the NEXT row in the queue** — the only build row in the file with an EMPTY handoff cell and no gate line. Three defects: (1) *no unambiguous reading* — ARCHITECTURE.md:236-240 glosses "parallel-path compensation" as dry-vs-send alignment, but there is no separate monitor path to spare (a strip's dry is written once at `sl_channel.cpp:731-732`, comment "this is the record tap"; the meter reads the same samples; the same buffer pours to main at :752-765), so the literal reading delays what the performer hears, which D-WZ-PDC-01 forbids two sentences later. Settled by D-SL-PDC-REF-01: **WET-vs-WET**, dry untouched. (2) *clause 2 already half-shipped* — the per-RETURN latency readout ships end-to-end behind a reachable door at HEAD (P6-2's `sl_fx_latency_ms` → toolbar push → FxSlotPanel), and "per-CHANNEL" has no subject: there are no channel inserts (`sl_engine.h:360-365`), only the four per-return slots. (3) *no gate can prove it* — `NativePluginHost.mm:1026` returns latency **0** on the JUCE-less stub every non-Apple gate links, and the two binaries linking the real `.mm` are Apple-gated SKIP-77 with `plugin_audible_test.cpp:162` merely PRINTING the number. So a compensation test would pass identically with the code deleted (rule two). Also ~3 features across 5 layers, against §11's one-item-per-commit — P6-5's split one row above is the precedent |

## P7 — the deck, whole

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P7-K0 | spec | `docs/merge/NAV-SHORTCUTS.md`: audit all ~100 keymap entries — a merged-verb target per entry, the `dj`→focused-strip context map, the digit-allocation ruling (provisional: digits = scenes on the focused strip, ⌘/⌥-digits = focus jump, Tab/⇧Tab = cycle), and every dead entry parked WITH its reason. Also picks the full-viewport mechanism (plane transform vs overlay mount) | done (2026-07-30) | **`docs/merge/NAV-SHORTCUTS.md`, 683 lines. 99 keymap rows expanding to 216 chord slots: 42 have a reachable merged verb, 57 are parked with a written reason each.** Measured at HEAD, not read from the ledger — and the measurement went further than this file's own warnings. **THE FINDING THAT REFRAMES THE K-SERIES: WizardMerged has NO native keyboard at all.** `shell/` and `host/` contain no `MenuBarModel`, no `setMacMainMenu`, no `KeyPress`/`keyPressed` and no `forwardKey` handler — the greps return empty (verified independently by the conductor) — and `publishMenuTree` sits in the shell's unimplemented list, asserted at `sl_dispatch_test.cpp:153`. There is no menu bar and no key equivalent: **every keystroke is answered by JS or not at all.** So P7-K1's dispatcher is not an optimisation of an existing path, it is the ONLY possible delivery mechanism for the 90 native-owned rows. The live surface today is exactly `browserKeymap`'s 11 chords (`⌘Z ⇧⌘Z Space 1–8`), plus `GridPanel.handleKey` while a grid holds `keyboardActive`, plus `focusModel`, `ContextMenu` and the file browser's element handler. Three facts this ledger did not carry: (a) `KEYMAP` and `GridPanel.handleKey` are two undeclared halves of ONE keyboard — 12 keymap rows are ALREADY implemented in TS, so a dispatcher that binds them again fires twice; **K1's first duty is reconciliation, not addition**; (b) `GridPanel`'s six `return "forward"` sites hand keys to a root relay unmounted since P8-8 — the keys are already dropped; (c) **`scenesStore.ts` is a complete, DEAD scene-verb module** — every sender rides `link.command("patternScene", …)`, which is in neither `BrowserLink`'s 15-case switch nor `MergedLink.NATIVE_METHODS`, and its own comment advertises "hotkeys 9/0". It is the trap a K-series builder reaches for first; the live scene verb is `useCompanion.selectScene`. **RULINGS.** *Chord representation* (this row's "real first job"): keep `keys` as display, add `chords: readonly Chord[]` where `Chord = KeyEquivalent`, plus a REQUIRED `parked` reason so a dead chord cannot be silent — that is what makes P7-K3 mechanically checkable. `code` throughout, with `ö/ä/ü` by `key` because the tree already matches them that way twice. `isReservedShortcut` becomes host-scoped via a new `hostKind()` discriminator on `engineLink.ts` — **no schema change**, `MergedLink` is already a distinct class and only needs exporting. *Context map*: `dj` retires; `plane` and `strip` join; A/B pairs collapse onto the focused strip (anchored on `mapStore.selectedKey`, which already draws the ring), freeing eight chords left deliberately unbound. *Digits, PROVISIONAL per D-8*: bare 1–8 = scenes on the focused strip (a one-line change of aim to the only shortcut already working on the plane), `⌥1–8` = focus jump, Tab/⇧Tab = cycle, and **`⌘`-digits NEVER bound** — Chromium and WebKit both take `⌘1–9` for tab selection, so a `⌘`-digit binding could never be proven by a walk. *Full viewport*: overlay mount, the pattern `PlanePanel.tsx:1105-1108` already uses twice for `Matrix` and `Composer`; the plane-transform alternative is refused on two grounds — `updatePlaneView` writes `map.plane`, a PERSISTED document field, which fails V1's own "VIEW state, never a document cell edit" gate, and `scale()` magnifies where the row asks for reflow. ⚠️ Also corrects **P7-T3**, whose citation this row was told was stale: `gridHidden`/`performOn` DO exist — on the `dj` UiState (`TransportPanel.tsx:202,233-235`, `DjPanel.tsx:136,199`), not on `GridMetaState`. T3's warning is half right; re-measure, do not re-search. And it confirms **P7-N1**'s live defect mechanism: `GridPanel.tsx:2812` claims keyboard by default and `deckTile.tsx:51-55` never releases to `null`. gate: none — a spec row producing a doc has no ctest, no walk and no real-host path; the rulings' proofs ride P7-K0a, P7-K1, P7-N2 and P7-V1 |
| P7-K0a | build | `keymap.ts` gains matchable chords | done (2026-07-30) | `keys` untouched, `chords` added beside it, **neither derived from the other**. 99 display rows → **216 chord slots**. `Chord = KeyEquivalent`, so a registry shortcut and a keymap chord are literally the same value; `code` throughout with `ö ä ü #` as `{ key }`, the convention `GridPanel.tsx:2440` and `focusModel.ts:176-178` already use. **`parked` is a positional argument of `park()`, the only constructor that can produce a parked row — so parking without a reason is not expressible.** 57 rows parked, each reason transcribed from NAV-SHORTCUTS §6 and keeping its `PARK-x` class, with the test enforcing both the class prefix and non-trivial prose after it. **The chord-level collision test found THREE overlaps the token test structurally could not see** — the blindness `keymap.test.ts:6-9` admitted to: compose `KeyZ` (finger-drum preview vs FX pad 1), compose `KeyV` (reverse transport vs FX pad 4 — the audit predicted this one), and grid `⌥⇧Equal`, where **`"⌥+"` and `"⌥⇧+"` are the same keystroke** (`+` is already the shifted Equal): two display strings, one chord, an ambiguity in the scoopyloops-era Help text that only expansion could expose. All six rows involved are parked, so nothing is shadowed today. `KNOWN_COLLISIONS` re-keyed from display strings to chord ids (it was empty, so nothing was lost). **The gate is proven non-vacuous: commenting out one allowlist entry turns it red.** The tally reconciles with §6 **exactly** — 42 rows with a target (102 slots), 57 parked (114), 216 total — including the row that could have gone either way: `"+ or ="` is two slots, the shifted and the bare Equal, which is what makes the doc's 216 and the code's agree. That reconciliation is itself pinned. ⚠️ **Measured, and it corrects a premise for P7-K1:** `GridPanel.handleKey` matches **`e.key`** (`GridPanel.tsx:2321`, verified), not `e.code`. §2.4's `code` ruling stands for the keymap — but it means K1's reconciliation of the 12 `DECLARED-LIVE` rows is a **discriminant CONVERSION, not a lookup** (P7-K0a-a). ⚠️ `keymap.ts:16-22`'s three false claims deliberately left for P7-K6. gate: `keymap.test.ts` is the gate and it is green; `KEYMAP` still has no consumer but its own test (P7-K4), so there is nothing to click |

## P8 — the map holds the performance

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P8-1 | spec | MAP-SCHEMA amendment made concrete: the overlay field shape (generalizing `sessionPerf`/`perfBySession`), the write-routing law, the re-apply triggers, and the migration from today's four-field shape | done (2026-07-30) | `docs/merge/MAP-SCHEMA.md` amended in place (+~230 lines), both ⚠️ hazards rewritten to RESOLVED-BY-DECISION pointing at the section that resolves each. Amended rather than linked because a reader who lands on a hazard must find the resolution THERE; the file already carries four dated amendment sections. **THE FINDING: three of the overlay's four existing fields are backed by NOTHING** (verified independently by the conductor). `GridPerfSchema` (`mapDocument.ts:136-152`) holds `currentScene · switchMode · queuedScenes · queueLoop`. Only `currentScene` has a live counterpart. `switchMode`'s only setter rides `link.command('patternScene', …)` — answered by **neither** `BrowserLink` nor `MergedLink` (P7-K0's dead module). `queuedScenes: string[]` has no analogue: the store holds `scheduledScene: SceneLetter|null`, **ONE armed scene, not a list**. `queueLoop` and `queuedScenes` occur ONLY in `mapDocument.ts`/`mapApply.ts` and their own tests — zero real consumers. So this row was never "generalize four fields": it is **replace three fictions with what a running deck actually holds.** The fiction survived structurally — `planApply` emits `sceneSelect`/`sceneSetSwitch` and `mapStore.ts:265-272` DROPS both, so the read path looked complete while nothing issued it. **Shape (§1):** five fields on `strip.overlay: Record<sessionId, SessionOverlay>` — `scene` · `stoppedTracks|null` · `masterVolume|null` · `masterDrive|null` · `masterDriveCurve|null`. `null` is a third state, "this map has no opinion, the session's value plays" (P6-5b's distinction). A field qualifies iff it is deck state the session file lacks AND something can read it at capture and write it at re-apply TODAY. `stoppedTracks` joins; **`soloedTracks` is refused** — solo is a monitoring gesture never persisted on either host, with no document seed to fall back to. **Deliberately NO `bpm` field**: `updateGridTempo` already routes the plane's bpm through `setTempoOverride`, which wins at every publish and never reaches the Autosaver — that is not a workaround to replace, it is **the working precedent the other three parameters must copy**. **Write-routing law (§2), resolving the BLEED hazard:** the 2026-07-29 wording "plane-surface edits land in the overlay" is wrong in both directions, because the deck tile mounts the real `GridPanel` and cell editing IS a plane-surface edit. Sharpened to **parameters overlay, contents do not** — the OBJECT of the edit discriminates, not the surface. ⚠️ **FIVE doors write a session parameter at HEAD, not the three P8-4 names** — see P8-1a. **Re-apply (§3), resolving the STOMP hazard:** four triggers, one entry point. T5 (compose window) is **REFUSED by ruling** — compose must show the file as it is, or you compose against values you cannot see. ⚠️ **This corrects the hazard paragraph's own proposed fix**, which said the re-apply is "the same code on a different trigger": it is not — `EngineOp` is "one engine call, named for the ABI entry point it becomes" and the overlay has none, so re-applying through document writes would fix the stomp BY CAUSING THE BLEED. **Migration (§4):** v8→v9, `sessionPerf`→`overlay`; new fields migrate to **`null`, not a constant**, which resolves to "the session's value plays" — exactly what a v8 map sounded like. The three fiction fields are DROPPED, the first drop in this document's history, with a written carve-out at the `preserve-don't-drop` law: it protects information a reader might act on, and a field never writable and never readable is not information. **v9 bumps `MAP_SCHEMA_VERSION` only and touches nothing on the wire** — `schema:check` compares `SCHEMA_VERSION`, not `MAP_SCHEMA_VERSION`; no protocol bump. ⚠️ This row's own hazard citation (`MAP-SCHEMA.md:176-178`) is off — that is P8-0's unresolved-strip paragraph; the hazards were at `:149` and `:182` pre-edit (P8-AUDIT-a). gate: the doc lands and both ⚠️ hazards read resolved-by-decision — met |

## P9 — broad routing

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P9-3a | build | **The duplicate-cable guard moves onto the COMMIT path** (lifted out of P9-3, done 2026-07-30) | done | The row said the guard lived only in `onRecordFromStrip`; the 2026-07-30 audit found it was worse than that — by HEAD there were **two unguarded commit paths**, `commitPatch` and P3.5-E3's `patchDeckOut`, both appending to `map.routes` and firing `slRoute add` unconditionally. The engine SUMS duplicate routes, so a repeated shift-drag (or a repeated `record from`) made a strip **twice as loud with nothing on screen to explain it**. Fixed by putting the guard where the add happens rather than trusting each gesture: `routeExists(routes, route)` in `cables.ts`, called by both commit paths. **Identity is the ENDPOINTS — kind, index, sub, destination — and deliberately NOT gain or feedback**, because the same cable at another level is the same cable and a gain-sensitive guard would let "re-patch quieter" add a second summing route. Pinned: exact repeat caught · gain/feedback ignored · different source, different destination and *same index different KIND* all pass · the four sends of one strip stay four cables. Also retired the last two hand-rolled ABI encodings on this path (`srcKind: sendIndex === null ? 0 : 1` and `srcKind: 4`) in favour of `SRC_KIND`/`DST_KIND` — the E5/E6 lesson, applied before it could bite a third time. vitest 1492 · typecheck · **9** drift gates green · bundle fresh |
| P9-5 | spec | The interim other-app-audio path documented (D-WZ-VDEV-02) | done (2026-07-30) | `docs/specs/other-app-audio.md` (315 lines), plus an 8-line pointer from `ROUTING-MATRIX.md`'s D-WZ-VDEV-02 section so the governing design names the write-up. Placed in `specs/` beside `capture.md` and `routing.md` rather than `merge/`: `merge/` is the merge's process record, and P10's retirement row then has ONE file to delete. **The premise HELD this time** — measured first because three lanes running before it found schema surfaces no host answers. `slDevices/setInput` is real and answered at `SlDispatch.cpp:1009-1023` by exactly one host (refuses without `services->audio`, `:992`; sole assignment `MergedApp.cpp:93`; the headless harness gets an honest refusal by design). `deviceInput = 2` is first-class (`sl_channel.cpp:539-543`) and authorable with one gesture: `⋯ → record from` → `repointInput` (`Plane.tsx:150-165`) → `repatch` issues a real `slRoute add`. **Nothing on the chain knows the device is virtual**, which is exactly why this costs a document and not code. **Sample rate:** the merged app PINS 48 kHz (`sl_engine_create(48000.0, 512, …)`, `MergedApp.cpp:295`) and `AudioIO::open` refuses coercion rather than repitch the monitor. So a virtual device not at 48 kHz does not get resampled — **the switch FAILS**, and per P9-5b the app then goes silent with no message. ⚠️ **Three failure reports on this path are produced and rendered NOWHERE** (P9-5b/c/d), one of which turns the audio off. ⚠️ Two traps now in the doc's troubleshooting ladder: a `deviceInput → channelIn` cable is multiplied by the strip's MONITOR curve (`sl_channel.cpp:584`) and strips are created `monitor: false` (D-WZ-MON-01's feedback fix), so "I selected BlackHole and hear nothing" is the EXPECTED first state and MON is the missing gesture; and the strip meter is taken post-gate/post-level (`sl_channel.cpp:728-737`), so an unmonitored input reads exactly zero (P9-5f). ⚠️ The row's second half — the hint in the picker — is a SOURCE change and outside a spec row's licence: returned as **P9-5a** with an exact spec. gate: the doc lands; the click path rides P9-G1's third clause |
| P9-5a | build | **The hint in the input picker** — P9-5's second half | done (machine) — **real-host walk pending (user)** | The `input device` section was gated on `devices.length > 1` (`Strip.tsx:624`), and **the gate was the defect**: the person who needs to hear about BlackHole/Loopback is exactly the one who has installed nothing, has one device, and therefore saw no heading, no hint, nothing. It now always renders when `onPickInput` exists, carrying *"another app's audio → a virtual device (BlackHole/Loopback)"* with the device list beneath when there is more than one. **Two sub-lines, not one, because "one device" has two causes.** With exactly one, the line names the relaunch caveat (`refreshDevices` runs once from the plane's boot effect and `slDevices` has no rescan, so a driver installed while the app is open cannot appear — P9-5e). With none, it says *"no input devices reported"*, true both of a host that answered with nothing and of the browser dev host that has no device layer at all. **Guessing between those would be the confident-wrong label this line exists to prevent**, and `useDeviceStore.loaded` — the field that tells them apart — is not passed in. Built as an exported pure `inputDeviceMenuItems` (`Strip.tsx:116-168`), the shape `panelsMenuItems`/`mapMenuItems` already use. **Not gold-plating: the menu is assembled inside an event handler and rendered through `useContextMenu`'s portal, so extraction is the only way any pin in this repo can see it at all.** ⚠️ The pins are pure-function pins — "the ⋯ menu shows the section" is walk-only. gate: real host, on a machine with NO virtual device — `⋯` → bottom → the section names the path where there was previously nothing |
| P9-5a-a | delete | `other-app-audio.md:93` cites `PlanePanel.tsx:378` for the once-only `refreshDevices`; it is at **`:458`** | todo | Found implementing P9-5a, verified by the conductor. The claim holds, the line does not. Also fold in: §5's step 2 now describes the OLD gated behaviour and names P9-5a as the gap that closes it — that sentence is stale as of this commit |
| P9-5a-b | build | ~~The one-device / no-device lines are the natural home for the device store's `error`~~ | **CLOSED by P9-5c — decided with a mechanism, not a preference** | `ContextMenu` runs `onSelect()` then `onClose()` **synchronously** (`design/ContextMenu.tsx:165-168`) while the switch is an awaited round-trip — **so the menu that took the gesture is already gone when the host answers.** It could only report on its NEXT open: a report you have to go looking for, about a failure you did not know had happened. The note line took it instead (P3-U6 / E9a). `lastSwitch` remains readable if a menu line is ever wanted, but it would be a second surface for one event |
| P9-5b | build | **`AudioIO::setDevices` must re-attach on EVERY failure path — a failed device switch silenced the app entirely** | done (2026-07-30) | `host/src/AudioIO.cpp:102` called `detach()` and the four error returns all exited **without `attach()`** — verified before fixing. Only the success path re-attached, so a switch to a device that would not run 48 kHz left the app with no render callback at all: not the previous device, nothing, recoverable only by relaunch. And since the reason is still only a return value (P9-5c/d), what a person experienced was **an app that went quiet for good when they touched a picker**. Fixed by capturing the previous setup before mutating and restoring it on every failure. The restore is *checked*, not assumed — it re-reads the device and compares its rate to the one the engine is configured for, and only then re-attaches; attaching to a device at the wrong rate is the one outcome worse than staying silent, which is exactly why `open()` refuses coercion (D-WZ-RATE-01). When the restore itself fails the message says so (`"… (and the previous device did not come back)"`), because *your switch failed* and *your switch failed and the old device is gone* are different things to tell someone. **Principle: a refusal must cost you the CHANGE, never the audio you already had.** ⚠️ **Gate is honest, not green-by-fixture:** `deviceManager` is a concrete `juce::AudioDeviceManager` member with no injection seam, so a headless ctest cannot drive a device-rate refusal — there is no fake device layer and building one is out of scope here. ctest 44/44 still green (no regression); the proof is P9-G1's walk, or any device set to 44.1 kHz. If a fixture is wanted, it needs an injectable device interface first — that is a row, not a line |
| P9-5c | build | **The device-switch error must reach a person** | done (machine) — **real-host walk pending; no Chromium walk CAN prove it** | **The measurement that mattered was WHY it slipped a surface that already exists.** The plane routes failures to its note line through `onRefusal` (P3-U6) — but that seam fires from `ask`'s **catch**, and `SlDispatch.cpp:1017-1022` answers a failed switch with `{ok:false, error}` on a **successful dispatch**. The reply RESOLVES, so it is indistinguishable from a working one to every error path the plane has. **The one failure shaped unlike all the others was the one with no door.** `useDeviceStore.error: string|null` becomes `lastSwitch: DeviceSwitch|null` — the outcome of one gesture, **news rather than standing state** — announced by `watchDeviceSwitches(onNote)` (`devices.ts:160-167`), subscribed one line beside the `onRefusal` line (`PlanePanel.tsx:503-509`). One error surface, not a second channel invented for devices; `deviceSwitchNote` is pure and matches E9a's `silenceNote` shape rather than reinventing it. Three things the measurement decided: **(1)** the host's words go through **verbatim** — P9-5b made that load-bearing and it survives unedited, including the `(and the previous device did not come back)` tail. **(2)** A failure names the device you **PICKED**, not `current` — P9-5b restores the previous device *before* replying, so reporting `r.current` would tell someone who picked BlackHole that the app could not switch to the interface they were already happily using. **(3)** A **new record per gesture, compared by identity** — picking the same broken device twice is two pieces of news, and a value-compared string would announce the first and swallow the second: the same silence one layer up. On clearing (the answer was "it never did"): a success records itself too and **replaces** a stale failure, and `refreshDevices` clears it. A thrown refusal is deliberately NOT recorded — that path is P3-U6's, and recording both would put two lines on one surface for one gesture. ⚠️ **Pins cannot see the note line repaint: there is NO PlanePanel mount test in the tree** (P9-5c-b). Verified they see the defect rather than merely pass — disabling the subscription reddens 4, value-comparison reddens the two-identical-failures pin specifically. gate: real host with a 44.1 kHz device — the line names it, **and the audio keeps playing** |
| P9-5c-b | fixture | **The plane's note line has no mount-level pin at all** | todo | Nothing in `web/src` renders `PlanePanel` in a test. So **every** note-line claim in the tree — P3-U6's refusals, P3.5-E9a's silence report, P9-5c's device switch — is proven at the seam and then at the real host, with nothing in between. One jsdom pin mounting the panel and asserting `.plane-note` carries a pushed note would cover all three at once and catch a broken `useEffect` wiring that typecheck cannot see. ⚠️ Fourth of its kind, with P11-1-e, P3.5-E8g-c and P3.5-E8g-f — **the suite has no renderer, so every "it appeared on screen" claim in this project is walk-only** |
| P9-5c-c | delete | `other-app-audio.md`'s line citations have drifted | todo | Found editing §8: §2c cites `refreshDevices` at `PlanePanel.tsx:378`, now `:458` (same defect as P9-5a-a — fold them together). **The doc's whole discipline is that every claim cites the file it was read from**, so drifting line numbers erode exactly what makes it trustworthy. A citation sweep against HEAD, not a rewrite |

## H — host hygiene

| id | type | item | status | handoff note |
|---|---|---|---|---|
| H1 | delete | The orphan shell and the P1 spike | done | `shell/src/Main.cpp` (654 L), `CommandDispatch.{h,cpp}` + `wz_command` + `command_dispatch_test`, and all of `spike/`. **A third ghost surfaced mid-row:** `shell/generated/WZProtocol.h`, marked GENERATED with "regenerate: `npm run protocol:generate`" — a script that **does not exist** — pinning `kSchemaVersion = 30` while the live host is at **88** (`MergedMain.cpp:35`). Stale output of a retired generator, consumed by nothing once `Main.cpp` went; deleted with the rest. This is the same family as CLAUDE.md's standing warning that older docs name a `protocol:check` that `web/package.json` does not have. Nothing was ported — every deleted file's job is done by a surviving one (`wizard::sl::dispatch` for the dispatcher; WizardMerged carries the identical Linux GTK/WebKitGTK block that was the spike's stated CI value). Also corrected `MergedMain.cpp`'s own header, which still claimed it "runs alongside the legacy Wizard app (Main.cpp) … additive, not a replacement". Gates: ctest **67/67** (68 − `command_dispatch_test`, the only test deleted) · vitest 1485 · typecheck · 8/8 drift gates. Incidental fix, pre-existing: `compile_commands.json` at the repo root was a **dangling symlink** — `CMAKE_EXPORT_COMPILE_COMMANDS` was never set in the build cache, so every LSP diagnostic on C++ in this tree was noise |
| H2a | delete | The wizard engine tier — `engine/` reduced to the vendored resampler | done | **23 tests left the tree and every one is accounted for: 45 + 23 = 68, the baseline exactly.** (21 wizard-engine tests · `recorder_drain_test` · H1's `command_dispatch_test`.) ⚠️ **`engine/` was NOT deleted** — `samplerate` is defined inside it and `wz_decode` links it; it now holds only `ThirdParty/libsamplerate` + `libsamplerate_smoke`, and its CMakeLists says so at the top. Two plan assumptions were wrong and got corrected while building: (1) `engine/tools/` could not be removed wholesale — `libsamplerate_smoke.cpp` lives there; (2) `render_sink_test` was expected to "degenerate to a single implementation" and did not — it only ever **included** `WzRenderSink.h` and never instantiated one, so it lost a header and nothing else. **`decode_test` is the one real coverage change and it is NOT silent:** its second half loaded decoded audio into a `wz_engine` deck and asserted the engine PLAYED it. There is no surviving load path to point that at — giving audio to a tape IS P8-5, whose gate already reads "save a map with a looper take, reopen, hear it play" — so the claim is P8-5's to make, recorded in the file's own header. In exchange the decode half got STRONGER: it now asserts the resampled peaks (0.5 L / 0.25 R), which a decoder returning zeros or swapping channels would fail and every prior check would have passed. Seam headers `RenderSink.h` + `TakeDrainSource.h` KEPT with rewritten rationales — their two-engine reason died here, the live one (naming no engine, so `wz_record` links no ABI and stays JUCE-free for a future WASM/companion build) did not. `wz_capture`/`CaptureFake` kept: P10 names them by file. Also marked two now-false docs rather than leaving them: `ARCHITECTURE.md` §1's `engine/` row and all of §3, which specified an ABI that no longer exists — §3 kept as the donor's record because v3 inherited its conventions and transplants still cite it (`sl_tape.cpp:1`). Gates: ctest **45/45** · configure clean · vitest 1485 · 8/8 drift gates. Original plan note follows — ⚠️ **`engine/` is NOT deleted, and the reason is load-bearing:** the `samplerate` target is defined INSIDE it (`engine/CMakeLists.txt:15` → `engine/ThirdParty/libsamplerate`), and `wz_decode` links it (`host/src/Decoder.cpp:4`) — the decoder P8-5 (`tapeLoadTake` becomes REAL) needs. `slengine/CMakeLists.txt:14` confirms the scoopy port deliberately does not use libsamplerate, so nothing else supplies it. So: `rm -r engine/{src,include,tools}`, keep `ThirdParty/libsamplerate` + `libsamplerate_smoke`, and reduce the CMakeLists to that. Also `rm host/src/Wz{RenderSink,TakeDrainSource}.h` and `recorder_drain_test` (its merged twin `sl_take_drain_test` covers the same deck→drain→WAV seam PLUS the Law C-2 stamp chain); narrow `render_sink_test` to `wz_sl_engine`; drop the vestigial `PRIVATE wz_engine` from `wz_audio_io` (`AudioIO.h:2` names it only in a comment). **KEEP `RenderSink.h` + `TakeDrainSource.h`** — their two-engine justification dies here, but a second LIVE one is already written down (`host/CMakeLists.txt:52-56`): the seam is what keeps `wz_record` engine-free and JUCE-free "so a future WASM/companion build reuses it verbatim". Rewrite both header comments to the surviving reason rather than leaving a dead one. **KEEP `wz_capture` + `CaptureFake`** — wizard-era, but P10 names them by file as its existing foundation. Expect ctest 67 → 45 |
| H2b | delete | The wizard-only host services (`SessionStore`, `PackageStore`) | done | ctest **43/43**, the predicted 45 − 2. Both were reachable only from the shell H1 deleted, and both have a named successor rather than a gap: `SessionStore` (wizard's autosave, P7-06) → `FileSettingsStore`, which cites this file's atomic-write discipline as its stated precedent — so the lesson survives where the code does not, and `sl_settings_store_test` covers the same half-written-file failure on the store that ships. `PackageStore` (the `.wizard` STORED zip of session.json + Takes/, P7-04) → `.scoopyMap`, which is **a different document, not a renamed one**: it carries strips, routing and per-return plugin state (P6-5b). ⚠️ **Checked the queue before deleting rather than after** — no row wants a bundled audio export, and P8-6's round-trip gate is `.scoopyMap`'s, so nothing queued loses a dependency. Also repointed `SlSettingsStore.h:9`, whose "the SessionStore precedent" would otherwise have cited a file that no longer exists — a dangling comment is the same defect as a dangling include, one layer down |
| H3 | build | Split `MergedMain.cpp` into `wz_merged_app` + a thin entry point | done | Behaviour-preserving by construction: bodies moved verbatim into `MergedApp.{h,cpp}`, `MergedMain.cpp` is now 33 lines (the `--scan-plugin` intercept + `JUCE_MAIN_FUNCTION`), and the stale `kScoopySchemaVersion = 88` was **deliberately carried across unchanged** so this commit changes nothing — it is corrected in H5, where its gate lands. ⚠️ **The archive hazard was real and is verified not to have fired:** `NativePluginHost.mm` stays listed per EXECUTABLE, and the built app was checked with `nm` — 28 external + 88 local text symbols for `NativePluginScanner`, CoreAudioKit and AudioToolbox linked, **zero** stub markers. Had it lost the race to the no-op stub, every plugin feature would have gone quiet with a green build and green ctest. **The verification story is the interesting part of this row.** H3's own gate was "ctest green AND the real host", and the real-host half could not be closed by eye: `screencapture` here returns desktop-only (no Screen Recording permission — the giveaway was that the Terminal's own window was equally absent), so a screenshot said "no window" for the pre-H3 build too. Rather than trust either reading, H5 was built and it answers the question mechanically: 6/6 in the real WKWebView. **Do not treat a screenshot from this environment as evidence about the app.** ctest 43/43. ⚠️ **CORRECTION — this row went in with `params:check` RED and it was caught one row later.** Moving the assembly moved `kParamMap`, and `checkParamMap.ts:45` hard-codes the file it parses (`MergedMain.cpp`). H3 ran ctest and the real-host walk but NOT the nine drift gates, against CLAUDE.md's own "run all every session, not only when a row names one" — the exact instruction that exists for this. Repointed at `MergedApp.cpp` in H5's commit. Two things worth keeping from it: the gate **threw** rather than finding an empty table and passing (its `parseShellMap` contract is written for precisely this, and it earned its keep), and a refactor that relocates a gated table is a gate change, not just a code move. Original plan note follows — Pure refactor, no behaviour change — it is what lets H5 exist without a SECOND copy of the host, which is the whole complaint this section answers. `Backend`/`PanelWindow`/`GuardedWebView`/`provideResource`/`MergedApplication` → `shell/src/MergedApp.{h,cpp}` as a static lib; `MergedMain.cpp` keeps only the `--scan-plugin` intercept + `JUCE_MAIN_FUNCTION`. Add the H5 hook here so H5 stays additive: a page-loaded callback + an `evaluateJavascript` passthrough on `PanelWindow`. ⚠️ **`NativePluginHost.mm` must stay listed per EXECUTABLE, never folded into the static lib** — `shell/CMakeLists.txt:188-193` spells out that its symbols must outrank `scoopy_engine`'s no-op stub, and an archive member is pulled only if a symbol is still undefined, so folding it in can silently resolve to the stub and every plugin feature goes quiet with a green build. `plugin_audible_test`/`plugin_state_test` already do it the safe way. Gate: ctest green **and the real host** — this is a refactor of the assembly the repo verifies by RUNNING, so green ctest is explicitly not the gate |
| H4 | fixture | The walk matrix runs WebKit first | done | **14/14 — seven walks × webkit + chromium, one command: `npm run walks`.** The engine table is now `web/tools/lib/engines.mjs`, lifted verbatim from `browser_prod_test.mjs` (the only walk that ever ran more than one engine, and therefore where the WebKit persistent-profile lesson was learned); that file now IMPORTS it, so its green three-engine ship-gate run is the proof the extraction was faithful. ⚠️ **The engine is chosen by `SL_WALK_ENGINE`, not argv — and that is a fix, not a preference:** `browser_plane_test:22` and `browser_session_walk_test:25` already take `argv[2]` as the dist directory, so an argv convention made them try to serve `webkit/index.html` and die with ENOENT. A gate failing for a reason unrelated to what it tests is how a new gate gets switched off. **Two REAL findings, which is what the row was for:** (1) `browser_opfs_test`'s "directories sort first" asserted `rows[0] === "Kicks"` — an ORDERING claim checked by naming one file, true only while exactly one directory existed. Persistent profiles (which WebKit requires for OPFS at all) gave the app somewhere to keep its `Demo` folder, and a *correct* listing of "Demo, Kicks, sine.wav" failed a check about sorting. Now asserts the actual invariant — every directory precedes every file — off the row icons the panel renders (`▸`/`~`). (2) `browser_grid_test`'s two audio-meter checks polled for only 3 s; under a back-to-back matrix run an AudioContext can take longer, and it flaked ~1 run in 3. Budget raised to 10 s — the loop exits the moment the meter moves, so a healthy run is not one tick slower. Verified 5/5 after, and the fix was chosen over declaring it pre-existing because the diff was functionally identical to HEAD's and HEAD was 10/10: the honest reading was a latent race in the check, not a regression. `browser_prod_test` stays out of the matrix — it runs its own three-engine sweep and builds the bundle, so it would be minutes of duplicated work. Original plan note follows — Lift the `ENGINES` map out of `browser_prod_test.mjs:104-118` **verbatim** into `web/tools/lib/engines.mjs` (its chromium `--autoplay-policy` arg and firefox `media.autoplay` prefs are hard-won), plus `pickEngines(argv)` mirroring `:122-123`; `browser_prod_test` imports it so a green run proves the extraction. The other seven walks adopt it with default `["webkit","chromium"]` — WebKit first, because that is the shipping engine. ⚠️ **Expect real red, and that IS the row:** four walks use `chromium.launch()` not `launchPersistentContext`, and WebKit refuses OPFS in an ephemeral context — `browser_prod_test.mjs:88-103` is a written account of losing time to this and concluding wrongly ("Safari cannot do OPFS"). ⚠️ `browser_companion_audio_test.mjs:57` + `browser_worklet_test.mjs:55` launch `channel:"chrome"` for autoplay/AudioWorklet; budget a gesture-priming step rather than assuming a WebKit flag exists. Gate: all eight green in both engines; then the "Chromium walk" gate lines elsewhere in this ledger get renamed to the matrix |
| H5 | build | `MergedWalk` — a walk that runs in the REAL host | done | **6/6 in the real WKWebView**, and it closed H3's real-host gate that a screenshot could not. Asserts what only a JUCE host can answer: `window.__JUCE__` exists · `slCommand` is registered in `initialisationData.__juce__functions` · `__slPanel` was injected (the P3-4-2 defect class) · the committed bundle mounted under WKWebView · **HotFrames actually arrive in the page** (arm a listener through JUCE's own `backend.addEventListener`, read the count 1 s later — proof the 30 Hz timer, the engine and the emit path reach the page, not just that a timer ticks). Negative-tested: a deliberately false assertion yields `FAIL` with its reason, 5/6, exit 1 — it can fail, so it is not decoration. ⚠️ Two JUCE constraints found and designed around, both written into the file: `evaluateJavascript` **cannot serialise a Promise back** (`unsupportedReturnType`), so every script is synchronous and anything async is arm-then-read with a settle; and `pageFinishedLoading` can fire more than once, so the trigger is latched (two interleaved walks against one page would be blamed on the app). **OPT-IN by design** (`-DWIZARD_TEST_REALHOST=ON`, label `realhost`): it opens real windows and the real audio device, so folding it into routine `ctest` would steal focus and the sound card from whoever is working — a gate that interrupts you is one you start skipping. It BUILDS unconditionally though; an opt-in gate that stops compiling is how a walk quietly rots. **HONEST LIMITS, in the file rather than discovered later:** GUI binary, needs a window server (77 = SKIP); does NOT replace the visual pass (P6-4's "no headless test can prove a window APPEARS" still holds); asserts what the page can OBSERVE, so a wired-but-invisible control still passes |
| H5-a | build | `schema:check` — the NINTH drift gate, and the four-way drift it found | done | **Building H5 turned up a live defect: three hosts reporting three different protocol versions.** `web/protocol/schema.ts` said **96**; `SlDispatch.cpp`'s `getCapabilities` said **92**; `MergedApp.h`'s `kScoopySchemaVersion` (→ `sl_engine_create`, echoed in HotFrame slot 0) said **88**; and `sl_dispatch_test.cpp` **asserted 92 in two places**. Four and eight versions stale. **Why nothing caught it is the whole lesson, and all three lines of defence failed structurally, not by accident:** (1) `App.tsx:207` genuinely compares them and sets `SCHEMA MISMATCH: ui vN vs engine vM` — but it renders on the FALLBACK debug panel, and every real panel (`plane` included) returns before reaching it, so the backstop was defeated by ROUTING, not missing; (2) every browser walk passed and always would, because `browserLink.ts:275` reports the UI's OWN `SCHEMA_VERSION` — the browser host agrees with itself BY CONSTRUCTION and cannot fail this check however wrong native is; (3) **the ctest pin DEFENDED the drift** — `sl_dispatch_test` asserted the stale 92, so ctest went green across four bumps and would have gone red on a correct value. A comment saying "must equal" is not a gate; three of them were not either. Now `npm run schema:check` reads schema.ts as the authority and holds all three native sites to it, **checking every occurrence rather than the first** (the test has two — a first-match gate would have let the second rot, the same mistake one level in) and **failing rather than skipping when a pattern stops matching** (a renamed constant must break the gate, since silently checking nothing is how the last one survived eight bumps). Negative-tested both ways: catches the original 92-vs-96, catches "1 of 2 occurrences stale", refuses a renamed constant. **This is the eight drift gates becoming nine** — CLAUDE.md's list and §11's gate lines want updating. ⚠️ Note for the next reader: `kScoopySchemaVersion` and the `getCapabilities` value are DIFFERENT consumers (engine echo vs UI handshake) that must nonetheless carry the same number; both are inert carriers, verified — `sl_engine.cpp:490` stores it and `:516` returns it, nothing branches on it — which is why bumping was safe |
| H5 (original plan note) | — | — | — | The half no browser walk can reach: WKWebView, the real resource provider, the real dispatcher, a real `window.__JUCE__`, a real engine. `shell/tools/merged_walk.cpp` links `wz_merged_app` (+ `NativePluginHost.mm` per H3's ⚠️), boots the real `MergedApplication`, and drives a JS walk through `evaluateJavascript` with a `walkAssert` native function registered beside `slCommand`; scripts in `shell/tools/walks/`. First walk targets exactly what Chromium cannot see, taken from this ledger's own record: `slParam` reaches the engine · `__slPanelArg` is injected in a spawned window · `openPanelWindow` spawns · the ~2 Hz `slUiState` toolbar push arrives · `returnFx` is true (P7-T4's stated blind spot). ⚠️ **HONEST LIMIT, written here rather than discovered later:** this is a GUI binary — it opens real windows and needs a logged-in session, so it is a local/macOS-runner gate, not a headless-container one (`SKIP_RETURN_CODE 77` where there is no window server). It does **NOT** replace the human visual pass; P6-4 already recorded that no headless test can prove a plugin editor window APPEARS. What it converts is narrower and worth a lot: "the door is unreachable in the real app" stops being something a human finds at a phase gate |

## P11 — the master bar

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P11-0 | build | **The master's BR/REV lamps LIE — it keeps a second truth beside the decks' own** (found 2026-07-30, user-flagged framing error; defect against shipped P3-M-1b) | done | **THE LAW, from the user and from original scoopyloops: BR · TR · WIN · REV · SYNC are DECK controls. The master row FEEDS them and owns none of them.** `TransportPanel.tsx:25` puts them in the DECK block's row 2, every one reading `section` (`DeckSectionState`) through deck-scoped ops; the master row is the tempo REFERENCE (:257, :378). D4-2 got the state layer right — `DeckState.beatRepeat`/`.reverse` (`companionEngine.ts:212-213`) is the per-deck truth, and `Strip.tsx:245` reads it for the header lamps, its comment saying "so a master fan-out and a tile's own hand read the same fact". **But `PlanePanel.tsx:430-432` still holds `brOn`/`brIdx`/`revOn` as plain global `useState` with NOTHING syncing them from the decks** (the comment even calls it "master-level runtime state"). Consequences, both real: engage BR on a strip from its own header and **the master lamp stays dark while that deck repeats**; then the master's toggle computes `!brOn` from its stale boolean, so one press can re-engage what is already on or turn off what it never turned on. Fix: the master lamp becomes DERIVED from `activeDecks` (lit when any deck has BR/REV), the toggle decides from that derived value (any-on → all off, else all on), and the master keeps only the `brIdx` it will SEND — never a copy of what decks are doing. gate: vitest on the derive-and-toggle rule (any-on→all-off, none-on→all-on, mixed); walk — engage BR on one strip, the master lamp lights; press master BR, it clears **BUILT 2026-07-30:** `masterBrView(activeDecks, brOf, pendingIdx)` in `stripOps.ts` — the master's lamp, label and next-press are all DERIVED; the only state it still owns is `brIdx`, the scale it will SEND, which is an intent rather than a claim about any deck. REV derives the same way. The toggles read the derived value, so **the master always completes the gesture the lamp is showing, whoever started it**. Decks a compose window owns are excluded (they are not in `activeDecks`) — the master genuinely cannot turn those off, so lighting for them would be a second lie. A LATCHED deck's actual window wins the label; an off-scale hand-built window falls back to the pending label rather than showing nothing. Pinned as a pure function per the no-jsdom house rule (7 cases); two pins verified to FAIL against the old own-boolean master — the dark lamp AND the wrong label both reproduce. vitest 1499 · typecheck · 9 gates · bundle fresh |
| P11-1 | build | The three zones + the wizard-era retirements (D-SL-TOPROW-01) | **done (machine), SCOPED as ruled — real-host walk pending (user)** | **Zones: document (left) · master (centre) · output (right), as a CSS GRID rather than flex-with-a-spacer — and the grid is the substance, not a detail.** The centre column is `auto` between two equal `1fr` flanks, so the transport holds the **same screen position** however long the map's name is; under the old spacer, typing in the name field slid the transport sideways. Fine on a settings page, wrong for the one cluster a hand must find mid-set without looking. Zones `flex-wrap`, so a narrow window makes the bar TALLER and never hides a control — a door you cannot reach because the window is small is still unreachable. **`map ▾`** folds `save`/`export`/`open` plus the five settings panels (the one new home D-SL-TOPROW-01 names), reusing the `≡ panels` ContextMenu shape as the row requires — one menu vocabulary on the bar, not a second popover kind. **`library ▾` deliberately NOT folded in**: it acts on SESSIONS, `map ▾` acts on the MAP — filing session verbs under a menu named for the map would be a tidier bar telling a lie. **`compose` retired — a button, not a capability.** The bar's version could only ever open the FIRST grid strip, because a bar has no way to say which strip you meant; every grid strip already draws its own `COMPOSE ⇱` (`GridElement.tsx:265` → `Plane.tsx:660`) on the same `composeDeck`. **The retired door was a LESS CAPABLE DUPLICATE of one that stays** — recorded because a later audit would otherwise read the removal as a regression. `firstGrid`/`firstLoadedDeck` went with it: they existed only to answer "which deck does a bar-level compose mean?", and the honest answer was "the first one, arbitrarily". **`≡ panels` STAYS — against the decision's letter, for the decision's reason** (conductor ruling). Settings did leave it; FX 1–4 wait on P7-MIX-0; the five `PANEL_MENU_SURFACES` have no named home at all (`openPanelWindow` has exactly four call sites, all in `PlanePanel.tsx`). Retiring it as written would orphan five surfaces — rule four. The button now carries a comment naming both preconditions and the measurement behind them (P11-1-c). **`HealthReadout` beside LIM in the output zone** (P11-5b). It stays inside `Master.tsx`, so `uiOwnership.test.ts`'s pin needed no repointing — **and that is exactly why the lane did not rely on it**: a source grep is satisfied by the two merely sharing a file, which they did before AND after. "Beside LIM" is pinned against RENDERED MARKUP, verified to fail when the readout is moved back to the centre zone. ⚠️ One judgement call, trivially revertible: the output zone's caption reads **`out`**, not `master` — with the master transport and tempo now their own zone, a caption saying "master" over on the right names the wrong group. The fader is untouched and its title still says "master output" (P11-1-d). ⚠️ Review `Master.tsx` with `git diff -w`: 124 of its changed lines are pure re-indent from nesting the transport inside a zone div. Ignoring re-indent the row is +494/−122, of which 218 lines are tests. gate: real host — left to right the bar reads `PLANE · map-name · N strips · map ▾ · library ▾ · ≡ panels ‖ transport · bpm ‖ out · fader · meter · LIM · DSP`; and **typing in the map-name field must not move the transport** |
| P11-1-c | delete | **`≡ panels`' last two blockers, tracked as one row** | **retired-by-decision 2026-07-30 (user, P11-1-a): the button stays** | Opened on the assumption the button was a temporary survivor awaiting P7-MIX-0 and P11-1-a. P11-1-a decided it stays indefinitely, so there is nothing to close. **P7-MIX-0 may still rehome FX 1–4** into the strip mixer when it lands — that is P7-MIX-0's business and it does not retire the button, which still carries the five surfaces. The comment on the button should be corrected from "preconditions for deletion" to "why this stays" when something next touches it |
| P11-1-e | fixture | **The three-zone layout has no width coverage** | todo | Zones wrap rather than clip, which is what keeps every control reachable in a narrow window — and it is asserted NOWHERE, because there is no jsdom and no layout engine in the suite. The `flex-wrap`/`min-width: 0` pair is one deletion away from silently clipping the document zone. Either a browser walk that narrows the window and counts visible bar buttons, or an explicit note in the row that this is walk-only. ⚠️ Same species as P3.5-E8g-c: a property no gate can see is a property that will regress |
| P11-2 | build | Master **CLOCK** source (INT / EXT) + TAP | **done (machine) — real-host walk pending (user)** | **CLOCK, never SYNC — the headline, not a naming detail.** SYNC is a **deck** control (does THIS deck follow the master) and stays on the strip row. The master gains none, because a master control meaning "follow" would be a second source of truth about the decks — **the exact lie P11-0 had to unpick.** Two scopes, two words; the pin asserts the master markup contains no `SYNC`. **TAP is a pure fold over timestamps** (`plane/masterClock.ts`), pinned per the no-jsdom house rule P11-0 and P11-5 set. A **four-tap window**: enough for a median to out-vote one bad tap, few enough that it FOLLOWS you when you change tempo rather than averaging in the old one (pinned — tap 120 six times, switch to 150, the answer arrives). **Outlier rejection is load-bearing, not decoration:** the two real mis-taps are a MISSED tap (one interval ≈2×) and a DOUBLE tap (≈0.5×), and either drags a three-interval mean far enough to matter — one doubled interval at 120 BPM pulls a plain mean to **90**. Verified by falsification: disabling rejection turns exactly those two cases red. Below three intervals nothing is rejected, because with two there is no majority and "which one is wrong" has no honest answer. A series implying a tempo outside the tempo box's range is **REFUSED, not clamped** — clamping would silently set 20 on a fumble and teach that the button is erratic. **EXT is dead at THREE independent layers, and the row's proposed gate was not sufficient.** The row said to disable it on `midiHardware:false`. Measured: the shell answers `midiHardware:false` (`SlDispatch.cpp:93`, asserted by `sl_dispatch_test.cpp:78`); the shell implements **no clock method at all**, though `getMidiClockStatus` is fully specified in `protocol/schema.ts:605` with `locked`/`bpm`/`tickCount`; and it is absent from `MergedLink.NATIVE_METHODS`, so even a shell that DID implement it would never be asked. The protocol promises a surface nothing answers — **P11-5's disease, one domain over** (P11-2-b). ⚠️ **So the gate is deliberately NOT `caps.midiHardware`** — and this is the transferable part. **Capabilities default to FULL** (`capabilitiesStore.ts:16`, every flag true until a host narrows them, verified by the conductor), so a control gated on that flag alone renders **live for the frames before the handshake lands**, and live forever in any render path whose effect never runs. Briefly operable and permanently useless is exactly what the four rules exist to stop. `externalClockState` returns unavailable **unconditionally**; the flag only chooses which TRUE sentence to show. There is a pin on the SSR path specifically, because that is the path that exposes the mistake. When the shell grows a real clock, that function is the one place to change and its test says what must become true first. ⚠️ **Two pre-existing P3-U5 pins repointed, not loosened.** They counted `disabled` across the whole render as a proxy for "the six deck verbs"; INT and EXT are disabled by design and are not deck verbs, so the count went 6→8 and **the proxy silently started answering a different question**. Both are now scoped to the transport group by CLASS-NAME boundaries (a `</span><label` boundary returns nonsense the day the JSX is reformatted, and an empty slice would make both assertions pass while proving nothing), still assert exactly six, and were falsified against an always-enabled transport to confirm they still catch the bug they were written for. gate: real host — TAP four times and the bpm lands on your tempo (`TAP 1…4` counts up from the FIRST press); fumble one tap and it does not lurch; EXT is dim with a tooltip saying why |
| P11-3 | build | Launch QUANTUM on the bar | todo — **PREMISE FALSIFIED 2026-07-30; SPEC'D in `docs/specs/quantize.md`; SPLITS IN FOUR** | **The quantized-launch engine already exists, is sample-exact, and has ZERO callers.** `requestQuantizedLaunch(deck, refDeck, quantizeSteps)` (`NativeAudioEngineCore.hpp:1547`) is compiled into the merged app and reachable from nothing — `slengine`'s C ABI never exposed it and `sl_engine.cpp:1234` hardcodes `d.launchArmed = false` on every publish. **So this is a wiring row, not an algorithm row.** ⚠️ **This row's own premise is false:** it cited `deckQuantizePending` as evidence "the concept is the engine's". That field's writer is a hardcoded `boolArray(3, false)` (`SlDispatch.cpp:1139`) and its sole reader sits behind a door `panelMenu.test.ts:19` asserts is absent. The same falsification applies to the phase preamble. `launchQuantize` + the whole `dj` topic are equally inert — `djSetting` has zero handlers and nothing publishes `dj`. ⚠️ **The scale is wrong.** "¼ · 1 · 2 · 4 bars" does not exist in the proven implementation: the engine counts **STEPS** (1/16) and **CYCLES** (pattern LCM), and there is **no bar and no time signature anywhere in it**. The donor's scale is `off · 1 · 2 · 4 · 8 · 16 · cycle`. Keep it, so the numbers on screen are the numbers the ABI takes. ⚠️ **Whose grid is the question the merge creates and the donor already answered:** `refDeck` is a REQUIRED argument, because there is no master clock — each deck runs its own step clock at its own bpm and only the OUTPUT frame is common. quantize.md §4.2 recommends an explicit **sync-master strip** over focus-follows, so the guarantee is stateable in one sentence and the donor's silent degradation (arming against a stopped deck fires immediately) becomes a visible condition. ⚠️ **BLOCKED in the real host until P11-3a.** Its gate — "a queued scene fires on the quantum boundary" — is unachievable today and a fixture would go GREEN in vitest while WizardMerged got worse. Splits: **P11-3a** (position fan-out, a defect fix) → **P11-3b** (ABI) → **P11-3c** (control) → **P11-3d** (loop readout) |
| P11-3a | build | **A queued scene NEVER FIRES in WizardMerged — the pad stays lit forever** | done (machine) — **real-host walk pending (user)** | **The ledger's premise held exactly — rare here, and worth saying plainly.** `positionCbs` was declared (`nativeAudio.ts:61`), added to and deleted from (`:189-192`), and iterated nowhere. Fixed by fanning out from the HotFrame subscription `start()` already owned (`:96-98`). **Whole steps, and nothing more.** `stepFrame`/`framesPerStep` stay `0` — no invented sub-step resolution to make the callback look complete — and the comment at `:221` now states that as a prohibition on later rows. **What made whole steps sufficient:** the consumer at `companionEngine.ts:970` is `if (pos.step < d.switchBoundaryStep - 1) continue` — a **threshold, not an equality** — so a skipped step commits LATE BY ONE, never never. And 30 Hz only skips a 16th above ~450 BPM. Pinned by a test feeding steps `[12, 14, 16]` with the boundary at 16. Exactly one subscriber tree-wide, so nothing else wakes up. **The `playing` defect needed a SOURCE, not just a fix.** `this.step >= 0` was always true. There is no transport slot on the wire: `playheadStepDeck*` publishes a monotonic counter that FREEZES when transport stops, so "stopped" and "stopped on step 412" are byte-identical. A step-advance heuristic was **rejected** — it flickers false for 3 of 4 frames at 120 BPM and a hold timeout is an invention. The honest source was already arriving: `World.isPlaying` rides every publish, recorded per deck, reporting deck 0's — **the deck `step` itself comes from**, so the two agree about whose clock the position describes. (`deckClockLive()` is the real answer and costs a HotFrame slot + a three-host bump: P11-3a-c.) **Pins: 11 of 12 fail on the pre-fix source, and every one constructs `NativeWorldSink`** — a pin against `ScoopyAudio` would prove nothing, that path already worked. The twelfth passed both ways because at HEAD nothing was delivered *or* committed — **the E8g-c shape exactly** — so it was strengthened to count deliveries separately from commits, pinning "delivered and declined" rather than "never delivered". ⚠️ **The fan-out EXPOSES a second defect it did not cause** — see P11-3a-b. gate: real host — a queued pad on **strip A** switches on the next cycle boundary |
| P11-5 | build | HEALTH: dropout count + CPU (D-SL-HEALTH-01) | done (machine) — **real-host reachability UNPROVEN, walk pending (user)** | **BUILT: `HealthReadout` in the plane's master bar** (`plane/Master.tsx`, immediately right of the LIM lamp), reading `DSP 3%` and becoming `DSP 94% ✕2` once anything has overrun, with an ok→warn→hot colour flip sharing the LIM lamp's shell. LIM and DSP sit together because they are the same KIND of fact, checked in one glance: the limiter just sounds quiet, an overrunning engine just sounds broken, and neither says so itself. **Labelled DSP, not CPU, deliberately** — `PerfPanel`'s "CPU" is PAINT cost by its own docstring, and that word collision IS the mechanism of the failure: the one health-sounding door in the panels menu reported on the UI while the audio thread's load had none. Two rules pinned as pure functions in `design/health.ts` (no-jsdom house rule, the P11-0 precedent): **the count only ever goes up** — `deadlineMissCount_` restarts at zero when the device reopens, and `latchMisses` refuses to follow it down, because an overrun that was audible must not un-happen because the driver restarted; and **a dropout is sticky-hot** — load recovers on its own and the colour follows it back, but an overrun did not recover, so it stays hot until a click acknowledges it (which moves the baseline without resetting the latch, so a LATER overrun reads as one rather than as the running total). `—` before the first HotFrame, never `0%`: "no load" and "no engine" are the two states a health readout must not blur. `CpuMeter.tsx` RETIRED, not extended — it was a bare canvas with no number, no dropout count, and it never read `deadlineMissCount`, so the counter this row wants had no consumer anywhere in the app; its only mount was behind the `deckmixer` door, which `PlanePanel.tsx:66-70` records as retired by P3-P1 ("hangs on 'waiting for state' in the merged host"). `uiOwnership.test.ts` now asserts THREE things where it asserted two: `Master.tsx` names the one home; every panel INCLUDING `DeckMixerPanel` (which the old loop skipped) must not contain a second; and no file may reintroduce `CpuMeter`. It also reads `plane/` now, closing a real scope hole — `plane/` is where the always-on-screen surfaces live and therefore where a duplicate would most plausibly appear. ⚠️ **The rate and block size were NOT shipped, on purpose** — see P11-5a. gate: `design/health.test.ts` green (the fixture gate); real-host — the count survives the load falling back to normal | ⚠️ **The old note said "Nothing in the tree reports any of this". That is wrong at every layer, and believing it cost the project a 12x whole-build slowdown that no gate could see (P3.5-E10).** What actually exists at HEAD: the core times the WHOLE of `render()` with `steady_clock` and increments a **monotonic** `deadlineMissCount_` whenever compute exceeds the block deadline (`NativeAudioEngineCore.cpp:7863-7878`) — a compute-overrun counter, which is the BETTER number here because it isolates DSP cost from driver behaviour. Both are already published: `SL_HF_callbackLoad = 9`, `SL_HF_deadlineMissCount = 10` (`slengine/generated/sl_hotframe.inc:21-22`), written at `sl_engine.cpp:1414-1415`, pinned by `sl_hotframe_test.cpp:43-44`. A widget already reads it (`web/src/design/CpuMeter.tsx:27`) and `App.tsx:223` renders `cpu N% · dropped N`. **The whole gap is a door.** `CpuMeter` is mounted at exactly one site, `panels/DeckMixerPanel.tsx:132` (and behind a `cpuHidden` flag), and `deckmixer` is NOT in `PANEL_MENU_SURFACES` (`plane/PlanePanel.tsx:72-78`). `perf` IS in that menu but `PerfPanel` measures **paint** cost by its own docstring (`:12-16`, p95 < 2 ms/frame), not audio load — so the one health-sounding door tells you about the UI. Rule four exactly: built, published, pinned by a test, and unreachable. So this row is: give the audio-load readout a door on the master bar, and do NOT add HotFrame fields — they exist. Re-scope before building. The original ask, kept for the parts still true: a **monotonic** counter (monotonic so a glance answers "did it drop while I was not looking", which a rate cannot) (monotonic so a glance answers "did it drop while I was not looking", which a rate cannot). Readout `48k · 512 · 3%` + a colour flip and the count once anything has dropped. Deliberately NOT bundled with P9-4's per-path latency. ⚠️ `hotframe:check` gates the layout — regenerate, do not hand-edit. gate: fixture — a forced overrun increments the counter and it never decreases |
| P11-5b | build | The health readout must survive **P11-1**'s three zones | todo | P11-5 put `HealthReadout` inside `.plane-master` because P11-1 has not landed and building against a bar layout that does not exist is how a door goes missing. When P11-1 splits the bar, rehome this into the status/right zone **beside LIM** — the two are read in one glance and separating them costs the readout its context. ⚠️ It must not be dropped in the move: `uiOwnership.test.ts` pins that `Master.tsx` contains `<HealthReadout`, so a rehome out of that file goes RED and must REPOINT the pin rather than delete it. P11-1 inherits no other obligation from P11-5. gate: the pin repointed, the readout still on screen at boot |
| P11-5c | fixture | `sessionStore.test.ts`'s byte-for-byte round-trip **flakes under full-suite load** | todo | Found by Lane B taking a clean baseline at `585d45c`; pre-existing and unrelated to P11-5. `saveSession > round-trips a session through OPFS byte-for-byte` failed at **11098 ms** in a full `npm test` and passes in **777 ms** run alone; it did not reproduce on two later full runs. A ~14x wall-clock gap on a test asserting byte equality says timeout or shared-state race under vitest's parallel pool, not a persistence defect. ⚠️ Worth fixing rather than tolerating: **an intermittently red suite is how a real red gets waved through**, which is P3.5-E10's lesson one layer up. gate: 20 consecutive full `npm test` runs green | ⚠️ **CAUSE FOUND 2026-07-30 by Lane C, independently, while working P11-3a: this is P11-5d.** `sessionPackage.zip.test.ts:98` `writeFileSync`s onto the tracked fixture on every run, and this test reads it — cross-file interference under vitest's parallel pool, not a persistence defect. Reproduced at 1-in-5 full runs. **Fix P11-5d and this closes with it**; do not chase it separately.
| P11-5d | delete | **The vitest suite MUTATES a committed fixture on every run** | todo | `web/fixtures/session/session-from-ts.zip` is tracked and `src/persist/sessionPackage.zip.test.ts:98` rewrites it on every `npm test` — so `git status` is dirty after any test run and every agent in this shared tree has to decide not to stage it. It cannot simply be gitignored: it is a cross-language handshake fixture the Swift suite reads back. Either write it to a gitignored path and point the Swift suite there, or make it a `--write`-gated regeneration like the `:generate` scripts. ⚠️ Interacts directly with the shared-repo git hygiene rule — a tracked file that dirties itself is a standing invitation to a stray `git add -A`. gate: `git status` clean after `npm test` | ⚠️ **PROMOTED 2026-07-30: this is not just hygiene, it is GATE INTEGRITY.** Lane C traced P11-5c's 1-in-5 flake to exactly this write. So `npm test` is **not reproducible**, and any lane can silently commit a fixture the suite rewrote. Two lanes have now had to notice-and-not-stage it by hand. Fix: write to a temp path and compare, or make the Swift-corpus refresh an explicit `:generate` script — **never a side effect of the default gate.** gate: `npm test` twice, `git status` clean both times.


---

## Rows that resolved without being built (moved out of the active ledger 2026-07-31)

> Eighteen rows whose only content was their own dissolution — `retired`,
> `split →`, `superseded by`, `merged into`, `MOOT`, `RESOLVED by the user's
> walk`, `DECIDED`. They were competing for attention with open work in the
> ACTIVE file. Their successors are named inside each note; nothing here is
> outstanding.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P3-3-1 | build | FX returns ON in the merged host: `returnFx: true` in `SlDispatch::capabilities`, verify what that unhides on scoopy's surfaces and what the render now does; strip sends 1–4 audibly reach the return section for BOTH element kinds | **retired — superseded by shipped P6-3** | The 2026-07-28 measurement (the send/return TRANSPORT was built; the missing half was the CONFIG path, which turned out to be plugin hosting) is archived. P6-3 shipped it audible on a real AU. |
| P3-3-2 | fixture | Extend `plane_audio_test`: send level up on a tape strip and a grid strip → return bus energy appears; send at 0 → silence on the return | **retired — superseded by shipped P6-3** | P6-3's `plugin_audible_test` is the fixture this row asked for. |
| P3-M-1 | spec | Master fold-in — THE MEASUREMENT (2026-07-28): the transport panel is DESKTOP-SHELL-COUPLED — it waits on pushed UiState topics the merged shell deliberately never publishes, and speaks `menuTransport`/`menuSession`/`djSetting`/`toggleDjMode`, none of which the merged dispatch answers. So the ≡ door opens it (P3-4-2) but most of its surface is inert here; folding its VERBS means reimplementing them against the merged engine, verb group by verb group. THE LEVER for the signature verb: beat repeat lives in the core's `DeckWorld` (`beatRepeatStartStep/Length/Subdivision`, NativeAudioEngineCore.hpp:531-544) — fields the sl snapshot API cannot set (begin takes bpm/playing/startStep; track_set is track-scope). | measured → split | Measurement archived. Split into **P3-M-1a** + **P3-M-1b**. ⚠️ Standing constraint: the transport panel is DESKTOP-SHELL-COUPLED — it waits on UiState topics the merged shell never publishes and speaks verbs the merged dispatch never answers. That is why P3-P1 retired its door and why P7 rebuilds on companion lanes rather than reviving it. |
| P3-X1 | build | **Grid + tape COEXIST in one strip** (STRIP-MODEL's own sentence: "a scoopy deck with a looper recording the deck's own output") — today `element` is a one-of union, so REC on a grid strip captures the deck's output correctly but REPLACES the grid element in the document. The data-model half (a strip holding both, lanes = sum) is buildable; HOW the two render in one 340×196 box is exactly the morph question | **retired-by-decision (D-4 SIGNED 2026-07-29, D-SL-MORPH-01)** | The user chose ONE KIND PER STRIP — no composite element, no migration. The use case survives as two routed strips: **P3-R3**. |
| P3.5-E8 | build | **In compose, nothing loads a sample** (user, real host, 2026-07-30: "in compose it wont let me load any audio samples") | **measured → split** | **The row was wrong about where the door was missing.** `GridPanel` always drew both sample doors; each is only an INTENT a mounting surface must register for, and **`trackEdit` falls through to a silent `{ok:true}` when none is**. `useComposeBinding` — the surface a person actually composes in — registered NEITHER. Split: **E8a** · **E8b**. |
| P3.5-E8f | spec | **E7 and E8b share ONE unverified dependency: `<input webkitdirectory>` inside WKWebView** | **RESOLVED 2026-07-30 by the user's real-host walk — the good direction** | **RESOLVED by the user's real-host walk: E7's `folder…` button WORKS in WizardMerged.** WKWebView's open panel does offer a directory to `<input webkitdirectory>`. ⚠️ **The native `chooseDirectory` verb stays declared-and-unanswered at `schema.ts:457` and must not be built on E7's or E8b's account.** |
| P3.5-E8g-b | spec | **`decodeAudioData` in a non-plane WebView is unverified** | **MOOT 2026-07-30 — answered by the user's E8g walk** | **MOOT** — the E8g walk reached `<file> → track N`, set only after the sample decoded and the document was written, so `decodeAudioData` does work in the compose WebView. Kept so the question is not re-asked. |
| P11-1-a | spec | **`≡ panels` cannot leave the bar yet — and FX 1–4 are not the only reason** | **DECIDED 2026-07-30 (user): `≡ panels` STAYS INDEFINITELY** | **DECIDED (user): `≡ panels` STAYS INDEFINITELY.** The five `PANEL_MENU_SURFACES` have no other door and D-SL-TOPROW-01 names a new home only for *settings*. Rejected and recorded so they are not re-proposed: folding into `map ▾`, a new `tools ▾`, retiring the five surfaces. **P11-1 is complete as shipped, not scoped-pending-a-decision.** |
| P3.5-E9 | spec | **A loaded scoopy session makes no sound** (user, real host, 2026-07-30) | **measured → split** | The store has always KNOWN why a deck is quiet, and **all three signals were read by exactly ONE surface — the browser-only `CompanionPanel` whose door P3-L1 DELETED.** So a session loaded, looked normal, played nothing, and *the app could not tell you*. Split: **E9a** · **E9b**. ⚠️ Confirmed en route: the merged host decodes web-side and ships PCM over the bridge, **so a WebView decode failure is a silent track in the NATIVE engine**. |
| P3.5-E9b | spec | The real-host measurement, now that the app reports | **RESOLVED 2026-07-30 (user, real host): "sessions are audible now"** | **RESOLVED (user): "sessions are audible now."** The cause was found from the other end — **P3.5-E10**, the whole DSP path at `-O0`. E9a's readout stays and is the first thing to read when a deck is quiet. |
| P6-6 | build | ~~PDC per the signed policy~~ | **refused-as-written → split (D-SL-PDC-REF-01 SIGNED 2026-07-30)** | **Refused-as-written and split (D-SL-PDC-REF-01).** Two rulings govern P6-6a/b/c: **(1) PDC here means WET-vs-WET, dry untouched** — there is no separate monitor path to spare, so the literal reading would delay what the performer hears, which D-WZ-PDC-01 forbids; **(2) clause 2 is already half-shipped** — the per-RETURN readout ships behind a reachable door, and "per-CHANNEL" has no subject because there are no channel inserts. ⚠️ Third defect: **no gate can prove it today**, which is why P6-6b must ship the `sl_fx_set_test_latency_samples` seam. |
| P9-5a-b | build | ~~The one-device / no-device lines are the natural home for the device store's `error`~~ | **CLOSED by P9-5c — decided with a mechanism, not a preference** | **CLOSED by P9-5c — decided with a mechanism, not a preference.** `ContextMenu` runs `onSelect()` then `onClose()` **synchronously** while the switch is an awaited round-trip, so **the menu that took the gesture is already gone when the host answers** — it could only report on its NEXT open. The note line took it instead. |
| P9-5c-b | fixture | ~~The plane's note line has no mount-level pin at all~~ | **merged into P3.5-E8g-f 2026-07-30 (Lane C audit)** | Same finding as P3.5-E8g-f and P11-1-e. Its distinct consequence — **nothing in `web/src` renders `PlanePanel` in a test, so P3-U6, P3.5-E9a and P9-5c are all unpinned at the mount, and ONE jsdom pin would cover all three** — is carried verbatim as clause (b) there. Kept as a pointer so the id does not vanish from a search |
| P9-5c-c | delete | ~~`other-app-audio.md`'s line citations have drifted~~ | **merged into P9-5a-a 2026-07-30 (Lane C audit)** | The row's own note already said "same defect as P9-5a-a — fold them together". Done. Its §8/§2c finding is carried there. Kept as a pointer so the id does not vanish from a search |
| P11-1-c | delete | **`≡ panels`' last two blockers, tracked as one row** | **retired-by-decision 2026-07-30 (user, P11-1-a): the button stays** | **Retired by decision (P11-1-a): the button stays.** P7-MIX-0 may still rehome FX 1–4 — that does not retire the button, which carries the five surfaces. The comment on the button should be corrected from "preconditions for deletion" to "why this stays" when something next touches it. |
| P11-1-e | fixture | ~~The three-zone layout has no width coverage~~ | **merged into P3.5-E8g-f 2026-07-30 (Lane C audit)** | Same finding as P3.5-E8g-f and P9-5c-b — the suite has no renderer and no layout engine, so this is one row, not three. Its distinct consequence (the `flex-wrap`/`min-width: 0` pair is one deletion from silently clipping the document zone) is carried verbatim as clause (c) there. Kept as a pointer so the id does not vanish from a search |
| P11-3 | build | Launch QUANTUM on the bar | **split → superseded by P11-3a · P11-3b · P11-3c · P11-3d** (spec: `docs/specs/quantize.md`) | **PREMISE FALSIFIED: the quantized-launch engine already exists, is sample-exact, and has ZERO callers** — `requestQuantizedLaunch` is compiled into the merged app and reachable from nothing. **A wiring row, not an algorithm row.** ⚠️ `deckQuantizePending`, `launchQuantize` and the whole `dj` topic are inert — **the same falsification applies to the phase preamble.** ⚠️ **The scale is wrong: the engine counts STEPS (1/16) and CYCLES (pattern LCM); there is no bar and no time signature anywhere in it.** Keep `off · 1 · 2 · 4 · 8 · 16 · cycle`. ⚠️ **`refDeck` is REQUIRED because there is no master clock.** Design lives in `docs/specs/quantize.md`. |
| P11-5c | fixture | ~~`sessionStore.test.ts`'s byte-for-byte round-trip flakes under full-suite load~~ | **merged into P11-5d 2026-07-30 (Lane C audit)** | The cause was found: it is P11-5d's fixture write, and the row's own note already said "fix P11-5d and this closes with it; do not chase it separately". Its measurements (11098 ms under load vs 777 ms alone, 1-in-5) are carried there. Kept as a pointer so the id does not vanish from a search |
