# P3 merge ledger — rows for the loop

> **Compacted 2026-07-29, and AGAIN 2026-07-30 (Lane C audit).** `done` rows carry
> ONE-LINE summaries; their full handoff notes live VERBATIM in
> `P3-LEDGER-ARCHIVE.md` — dig there only when a specific done row's detail
> matters. Open and user-gated rows keep their notes. **The convention lapsed on
> 2026-07-30** — every row integrated that day landed multi-paragraph in the ACTIVE
> file, which reached 214 KB / 531 lines and stopped being orientable. Restored.
>
> **⟦PORT⟧ / ⟦NEW⟧ / ⟦UNKNOWN⟧ on an OPEN row** (2026-07-30, Lane C) says whether
> the shipping app in `../scoopyloops` **already implements it**, with a file and a
> line to read. **51 PORT · 31 NEW · 1 UNKNOWN** across 83 classified open rows.
> A PORT is a migration with a reference, not a design question — several rows
> below reason at length toward a conclusion the original settles in one file, and
> those carry a ⚠️ saying so.
>
> ⚠️ **`../scoopyloops` is the reference, but only its SWIFT half is.** That repo
> took four merge commits (P0-A · P1 · P2-4 · P2-5) before the P3-0 collapse, so
> its `web/src` **is this project's own web tier frozen at 2026-07-27** — citing it
> as "the original" is circular. The donor is `ScoopyLoops/*.swift` (100 files,
> 64k LOC) plus `ScoopyLoopsTests/`; `attic/` is history, never a spec.
> ⚠️ **Not to be confused with `ScoopyLoops/` INSIDE this repo, which is only the
> vendored C++ DSP core.** That collision is why nobody consulted the original for
> a whole session. **The index is `WebEngineLink.swift:365-559`** — an exhaustive
> dispatch `switch` (no `default`, so Swift enforced completeness) over all 84
> `SLPMethod` cases the old host answered.

> Created 2026-07-28 (Fable 5 review session). The merge phases (P3-0..P3-4) were
> tracked as prose in `P3-ROADMAP.md`; this file gives them the row form the loop
> protocol (`docs/ARCHITECTURE.md` §11) orients on. **Orient HERE for merge work**,
> not on `/MIGRATION.md` (wizard's pre-merge ledger, kept for the record).
>
> Statuses: `todo` / `in-progress` / `done` / `blocked(<reason>)` /
> `awaiting-decision` / `awaiting-user` / `awaiting-signoff`.
>
> **The four rules** (P3-ROADMAP + memory `verify-in-the-real-host`):
> tests pass ≠ it works ≠ it shipped ≠ **you can get to it**. Every UI row's
> handoff note must say what the visible door is and how a person reaches it in
> the JUCE WKWebView app — synthetic events and Chromium-only checks do not count
> as reachability evidence.
>
> **Decision protocol for this stretch (user, 2026-07-28):** the user is away;
> decisions queue in `MORNING-DECISIONS-2.md` with options + recommendation and
> DO NOT block the loop. Rows marked `awaiting-decision` are skipped; rows marked
> `provisional(D-n)` may build the recommended option, logged, re-tunable on
> sign-off.
>
> **⚠️ Gate suite: TEN drift gates, and `web/package.json` is the authority.**
> `params:check` · `shared:check` · `worldmap:check` · `hotframe:check` ·
> `tape:check` · `trackparams:check` · `webdist:check` · `check:tokens` ·
> `schema:check` (the ninth, H5-a) · `nativemethods:check` (the tenth, 2026-07-30 —
> added after `fxSlot` (v95) and `getFxSlotState` (v96) shipped implemented but
> UNREACHABLE, dropped by `MergedLink.NATIVE_METHODS`; **if a row claims either
> feature works, it is wrong**). Plus `engine:check`, whose drift is pre-existing
> and recorded in P6-3 — **it is NOT one of the ten**.
> **There is no `protocol:check` and no `abi:check`**, whatever older docs say.
> Run all ten every session, not only when a row's gate line names one. The suite
> has caught a stale bundle (P3-X4), two live UI defects (P3-X5), a four-way
> protocol drift that a ctest pin was actively DEFENDING (H5-a), and two features
> that shipped unreachable (the tenth gate's own reason).
>
> ⚠️ **The 2026-07-29 claim that `params:check` is RED is STALE — it is GREEN.**
> H5-a repointed the checker at `MergedApp.cpp` after H3 moved `kParamMap`. The
> measured baseline is `PARALLEL-PROTOCOL.md` §2. **Still check `git status` before
> diagnosing any red gate — more than one agent works in this tree.**
>
> **Phases in this file (2026-07-29):** P3 (close-out: P3-X2, gate P3-G1) ·
> **P3.5** the strip becomes the deck (E-rows open — **E3 is a hoisted defect, take
> it first**) · **P6** plugins on the returns · **P7** the deck, whole · **P8** the
> map holds the performance · **P9** broad routing · **P10** the virtual interface
> (earmarked, no rows). Cross-phase order: ~~P3-X2~~ → ~~P3-F3~~ → ~~P3.5-E3~~
> (all done 2026-07-30) → P6-4..AUDIT → P7 → P8 → P9 → (P10); P3.5-E1 (user-fed)
> and P3.5-E4 (E3's split-out unification) ride in parallel.

## Done before this ledger existed (for the record)

| id | item | where recorded |
|---|---|---|
| P3-0 | The collapse — one tree in scoopy.git, sources not vendored | P3-ROADMAP "Sequence", commits 3d6fa22 · 765aa24 |
| P3-1 | Universal transport — a grid strip's ⟳ ▸ ↻ ◼ drives the deck | c589211 |
| P3-2 | Master tempo from scoopy — deck params, three tempo modes, djSyncLaw as authority, four defects fixed | P3-ROADMAP "P3-2 — DONE", dad8341 · 62b5fbd · ee56aef |
| — | Strip menu (visible door replacing right-click), session-open failure surfaced, OPFS writeFile atomic-ish | 8b0e9dc · 502bc1d · 0cccde4 |

## The queue

**Loop order** (why: broken-build first per §11, then the two rows that make the
product USABLE at all — audio in the plane, sessions that save — then the honesty
fixes, then the phase's named domains):
P3-F1 → P3-U1 → P3-SES-1 → P3-SES-2 → P3-U2 · U4 · U5 · U6 → P3-2b-1..6 →
P3-U3 → P3-U7 → P3-3-1..2 → P3-4-1..2 → P3-M-1 → P3-F2 → P3-AUDIT.
Push (P3-PUSH) after every green commit.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P3-F1 | fixture | `recorder_drain_test` intermittent SEGFAULT under the full suite | done | TSan first run: `wz::Deck::ensureCapacity`'s `push_back` reallocated the chunk vector under the render — ported sl_tape.h's reserve/retire discipline + atomic `chunkCount` readers (both engines). TSan 0×8, 120 stress runs green. Reset-while-render window stays documented, not this row's scope |
| P3-F2 | fixture | `browser_prod_test` / `browser_grid_test` failures — WASM worklet env detection | done | Regenerated glue detected "worker" by a symbol AudioWorkletGlobalScope lacks → engine aborted in EVERY browser since 2026-07-27. One source-level shim atop `scoopy-worklet.js` covers dev+prod. Ship gate + grid gate passed |
| P3-U1 | build | The plane never started the engine sink | done | `plane/bootEngine.ts` `autoStartEngine` from PlanePanel's boot effect, NATIVE host only (browser keeps the companion's click — autoplay). Failures land on the plane note line. Door: grid transport reaches a RUNNING sink at boot; hearing it = P3-G1 |
| P3-SES-1 | build | Native session store — THE FLIP | done (provisional D-1) | Built one tier lower as **`slFiles`** — a contained library filesystem routed through `opfs.ts`'s adapter (`nativeFiles.ts`), flipping sessions/samples/browser in one move; browser path byte-identical. Atomic writes, segment-wise traversal refusal, trash-first remove. Full gate list in archive |
| P3-SES-2 | build | Session content into strips end-to-end in the REAL host: New → edit → save → reopen → load into strip, through the visible strip menu | machine-half done / awaiting-user | **⟦PORT⟧** donor: `PersistenceService.swift` (570 L) · `SessionZip.swift` · `menuSession` → `WebEngineLink.swift:539`. `tools/browser_session_walk_test.mjs`: drives the BUILT bundle in Chromium against a fake JUCE backend whose slFiles is a server-side library with SlDispatch's exact semantics — New in the companion → `pattern.json` lands non-empty on the native route → the strip menu lists it → click → the strip renders scene pads + the grid row → the library survives a reload. Green first run. What it deliberately is NOT: the real JUCE bridge/WKWebView — ~~**user check remaining: open WizardMerged, sessions ⇱ → New, then ⋯ on a strip → load it**~~ **⚠️ CLICK PATH STALE (2026-07-30 audit) — the `sessions ⇱` companion door was DELETED by P3-L1, which moved the library onto the plane. This pending gate would have failed at step one.** Corrected walk: **open WizardMerged → `library ▾` on the plane bar → New → then ⋯ on a strip → load it; expect scene pads and (after P3-G1's ear check) sound** |
| P3-2b-1 | schema | Tape tempo identity, document side | done | Map v5 (sync off, bpm null-honest, 4 fixtures); tape `tempoMode` timePitch/timeStretch only; sidecar `bpmAtStart` OMITTED when unknown, SCHEMA_VERSION 89, both parse directions pinned |
| P3-2b-2 | build | Tape beat length inference + Inspector override | done (provisional D-2) | `inferTapeBpm` (persist/tempo.ts): loop×stamp → snap to power-of-two bars in LOG space; >±20% off every power → null (honesty guard). Auto-fill only a null bpm; loaded-file tapes manual only |
| P3-2b-3 | build | Tape sync v1 — timePitch varispeed, zero new DSP | done | `tapeEffectiveRate` through THE LAW (sync owns magnitude, the hand keeps the sign — reverse survives; pulse relations land 1:2 not chipmunk). `applyTempo` pushes every tape each pass; SYNC/FREE on the tape row; rate slider disabled while synced. Ear check = P3-G1 |
| P3-2b-4 | spec | Tape stretch spec | done | `docs/merge/TAPE-STRETCH.md`: one reader two modes (stretcher only restores pitch); tape-tier declick (one-block primed engage + equal-power crossfade, no history ring); C-3 always closes DRY; lazy per-tape stretcher; ABI = `sl_tape_set_tempo_mode` + existing rate setter |
| P3-2b-5 | build | Tape stretch build (timeStretch to master, no pitch change) | done (provisional D-3) | Lazy per-tape `NativeBusStretcher` (asyncWarmup, render dry until warm), engage/disengage crossfades via a dry leg, LOOPING only so C-3 closes dry by construction. `sl_tape_stretch_test` green first run (pitch held ~440 vs varispeed ~880). SCHEMA v90 |
| P3-2b-6 | fixture | `tape_sync_test` coverage | done | Split across `sl_tape_stretch_test` (duration/pitch/disengage) + `plane_audio_test` (rate AND tempoMode survive a session publish; ratio measured AT the reader after re-trigger). Law arithmetic = tempo.test.ts |
| P3-U2 | build | Loaded grid strip never shows its session | done | `nameAfterSessionLoad` (stripOps): default/previous-session names follow the load, a user's rename is sacred. REC label + Inspector read `<session> · deck N` |
| P3-U3 | build | Scrub + overdub UI on tape strips | done | **Scrub ships**: unmodified wave-drag scrubs (turntable law, cue armed on release), loop brace moves to ⇧-drag. **Overdub ships**: OVR punch in/out (⌥ = replace), `overdubStart` sets the source + brackets a take, SCHEMA v91; enabled only with input patched + looping tape. plane_audio_test pins replace-with-silence-is-quieter + own-take landing |
| P3-U4 | build | Dead status plumbing | done | Take index from `slTakes list` (null-while-unknown), unresolvedRef/takeName/takeSeconds wired; `noOutput` via new `cables.hasOutput` (whole-graph). `decoding` deliberately unwired — nothing produces it. Known staleness: cross-window take deletion shows after next refresh |
| P3-U5 | build | Master bar fixes (±1 bpm honesty; empty-deck transport) | done | Steppers are honest DOCUMENT steps (comment matches code; real transient nudge queued to D-4 → later shipped as D4-2's nudge); ⟳↻◼ disable at deckCount 0 with next-step titles |
| P3-U6 | build | Error surface for dispatcher refusals | done | `send.ts` `onRefusal` seam → PlanePanel writes `<method> refused — <msg>` to the note line; console damping kept. Browser-host gaps now honestly visible |
| P3-U7 | build | Carve lands | done | Copyless by design: `slFiles` gains a read-only `/takes` mount (SCHEMA v92) — kit samples point at the recorder's WAV. `carveIntoSession` lands the region on the first EMPTY track; `doCarve` FREES the tape layer. Refusals name the next move |
| P3-3-1 | build | FX returns ON in the merged host: `returnFx: true` in `SlDispatch::capabilities`, verify what that unhides on scoopy's surfaces and what the render now does; strip sends 1–4 audibly reach the return section for BOTH element kinds | **retired — superseded by shipped P6-3** | The 2026-07-28 measurement (the send/return TRANSPORT was built; the missing half was the CONFIG path, which turned out to be plugin hosting) is archived. P6-3 shipped it audible on a real AU. |
| P3-3-2 | fixture | Extend `plane_audio_test`: send level up on a tape strip and a grid strip → return bus energy appears; send at 0 → silence on the return | **retired — superseded by shipped P6-3** | P6-3's `plugin_audible_test` is the fixture this row asked for. |
| P3-4-1 | spec | Panel audit, the list | done | `docs/merge/PANEL-AUDIT.md` — 19 panels tabled: 3 reachable, 14 mechanical menu doors (fxslot FIRST), instrument PARKED behind `pluginHosting: false`. Taste calls named for D-5 |
| P3-4-2 | build | Doors for every mechanical panel | done | `≡ panels` on the plane bar (FX 1–4 addressed per slot, surfaces, settings); shell `openPanelFn(panel, arg)` → `__slPanelArg` injected (sanitised). Door: the ≡ button itself |
| P3-M-1 | spec | Master fold-in — THE MEASUREMENT (2026-07-28): the transport panel is DESKTOP-SHELL-COUPLED — it waits on pushed UiState topics the merged shell deliberately never publishes, and speaks `menuTransport`/`menuSession`/`djSetting`/`toggleDjMode`, none of which the merged dispatch answers. So the ≡ door opens it (P3-4-2) but most of its surface is inert here; folding its VERBS means reimplementing them against the merged engine, verb group by verb group. THE LEVER for the signature verb: beat repeat lives in the core's `DeckWorld` (`beatRepeatStartStep/Length/Subdivision`, NativeAudioEngineCore.hpp:531-544) — fields the sl snapshot API cannot set (begin takes bpm/playing/startStep; track_set is track-scope). | measured → split | Measurement archived. Split into **P3-M-1a** + **P3-M-1b**. ⚠️ Standing constraint: the transport panel is DESKTOP-SHELL-COUPLED — it waits on UiState topics the merged shell never publishes and speaks verbs the merged dispatch never answers. That is why P3-P1 retired its door and why P7 rebuilds on companion lanes rather than reviving it. |
| P3-M-1a | build | Deck-scope snapshot fields (BR trio + reverse) | done | `sl_snapshot_deck_param_id/_set`, 6 keyed names, subdivision validated to 2/4/8/16/32; SlWorldApply forwards, absent = defaults. Fixture is AUDIBLE by necessity (the master step advances under a repeat by design) |
| P3-M-1b | build | Master bar verbs: BR + scale cycler + REV | done — **⚠️ FRAMING CORRECTED 2026-07-30 (user): these are DECK controls; the master only FANS OUT to them.** Original scoopyloops is explicit — `TransportPanel.tsx:25` documents the DECK block as `row 2: SYNC‹pulse› · TR‹±n› · WIN · BR‹›`, and REV/SYNC/TR/WIN/BR all read `section` (`DeckSectionState`, per-deck) via deck-scoped ops. The master row there is the tempo REFERENCE only (:257, :378). D4-2 later got the STATE layer right (`DeckState.beatRepeat`/`.reverse`), but this row's own note and its `brOn`/`revOn` mirror still read as if the master owns them — see **P11-0**, which is the live defect that framing produced | Companion runtime BR/REV state per deck, republished on change, fanned over every loaded deck. ⚠️ **FRAMING CORRECTED (user): BR·TR·WIN·REV·SYNC are DECK controls; the master only FANS OUT.** The live defect that framing produced was **P11-0** (done); the standing law is P11's SCOPE LAW block. |
| P3-PUSH | gate | Push `main` to the `scoopy` remote after each green commit (repo private; roadmap: "sixteen commits exist only on this machine" — 6 unpushed as of 2026-07-28 session start) | recurring | never push a red tree |
| P3-AUDIT | spec | Phase audit before P3-G1 | done | Four domains audited ✅ (mixer / transport / tempo+sync / FX-send transport; RETURNS blocked → P6, measured twice). Gaps materialized as P3-X1 (later retired by D-4), P3-X2, P3-X3 |
| P3-X1 | build | **Grid + tape COEXIST in one strip** (STRIP-MODEL's own sentence: "a scoopy deck with a looper recording the deck's own output") — today `element` is a one-of union, so REC on a grid strip captures the deck's output correctly but REPLACES the grid element in the document. The data-model half (a strip holding both, lanes = sum) is buildable; HOW the two render in one 340×196 box is exactly the morph question | **retired-by-decision (D-4 SIGNED 2026-07-29, D-SL-MORPH-01)** | The user chose ONE KIND PER STRIP — no composite element, no migration. The use case survives as two routed strips: **P3-R3**. |
| P3-X2 | build | DRV (master DSP) per strip — STRIP-MODEL: "reaches every strip, not just full decks"; deferred in P2 (the core has per-deck DRV; tapes have none) and still open | done | Per-channel `NativeMasterDrive` at the STRIP-MODEL tap point (post-element, PRE-level; amount 1 is a bypass BRANCH so the bit-exact path survives). SCHEMA v93, MAP v6 + migration. ⚠️ **The grid half is a DOCUMENT projection, not a live one** — a republish re-stamps the core's per-deck stage, so a live setter would be stomped. **Door: select a strip → Inspector → drive.** Discovered en route → P3-F3. |
| P3-X3 | build | Strip presets — one-click looper | done | `+ looper` beside `+ strip`: channel + tape bound, named LOOPER N, input patched, REC one press away. Recording deliberately NOT auto-started. Honest refusals when resources run out |
| P3-X4 | build | **`webdist:check` was RED at HEAD** (found 2026-07-29 running the full gate suite for the plan-adjustment commit) — the gate that exists because "the JUCE shell ships the committed `webdist/` bundle and never builds the web UI itself, so a stale bundle means the app runs old UI against new engine/protocol code" | done | The alarm was real, the danger was not: every emitted bundle was byte-identical, so no pending walk saw old UI. ⚠️ **The lesson is the gate line: `npm run bundle` must be the LAST step before `git add`, or the hash records a tree that no longer exists.** |
| P3-X5 | build | **`check:tokens` was RED at HEAD — 12 violations, all in `plane/plane.css`** (same discovery): 7 hardcoded font-sizes, 2 hardcoded z-indexes, 2 hardcoded colors, one dangling var. Mostly the P3-L1 library-popover era | done | 12 token violations in `plane/plane.css` fixed, every size substitution byte-equal at default tokens. The gate earned its keep twice — a hover that painted nothing (`--bg-hover` is defined nowhere) and a dead hex behind a live token. |
| P3-F3 | fixture | **`plane_audio_test` intermittent heap-corruption SIGABRT at HEAD** (found 2026-07-30 gating P3-X2; PRE-EXISTING — 9/30 aborts with the P3-X2 diff stashed) | done | **A REAL SHIPPING BUG, not a flake:** `slRecord/start` sized the take from the REQUEST, but a bus/main capture is ALWAYS STEREO — so every bus-tap record overflowed the drain scratch 2× and wrote **corrupt takes in the real app**. Fix: ask the engine (`sl_tape_channels`). ⚠️ **P11-6 inherits the lesson by name: ask the engine for a take's channel count, never infer it.** ASan loops need `detect_container_overflow=0`. |
| P3-G1 | gate | **Phase gate (human):** on the user's machine — a grid deck and a tape in strips, both answering level/sends/transport/sync identically; sessions save and reopen; nothing of scoopy's unreachable | awaiting-user | offered only after P3-AUDIT |

## Decisions queued (do not block)

See `docs/merge/MORNING-DECISIONS-2.md`: D-1 session store flip (veto window),
D-2 tape beat inference, D-3 tape stretch latency policy, D-4 the playful strip
morph / visual direction, D-5 panel hosting taste calls from the audit.

# P3.5 — the strip becomes the deck (queued 2026-07-29, D-4 signed)

> D-4 SIGNED live 2026-07-29 (D-SL-MORPH-01): the strip's expanded face hosts the
> REAL `GridPanel` at DJ density; strips are one-kind-each (grid OR looper — P3-X1
> retired); dead panel doors retire; compose opens in a real separate window.
> Plan file: `~/.claude/plans/lets-continue-the-merge-shimmying-treasure.md`.
> Same loop protocol, same four rules. **Loop order:**
> P3-U8 → P3-L1 → P3-D4-M → P3-C1 → P3-C2 → P3-D4-3 → P3-D4-1 → P3-D4-2 →
> P3-R1 → P3-R2 → P3-R3 → P3-P1 → P3.5-AUDIT. Push after every green commit.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P3-U8 | build | Scene pads tell the truth | done | 1-based labels via `sceneDisplayLabel` (letters stay storage identity); pads = `enabledScenes(pattern)` prefix; `+` add-pad via new `setEnabledSceneCount` (clamp 1..8; shrink falls back to A). ⇧=queue deliberately unmapped — a plain click already queues. Door: the pad row on a loaded grid strip |
| P3-L1 | build | The library lives on the plane | done | `plane/Library.tsx` popover (list/New/rename/delete/import) over sessionStore/slFiles; `createSession` (create-not-load) + `renameSession` (open→save-as→delete, loaded-session gate); companion door DELETED (browser-only shell). Walk test rewritten through the plane. ⚠️ WKWebView `<input type=file>` import picker unverified — walk at P3.5-AUDIT |
| P3-C1 | build | Compose in a separate window | done | `composeArg.ts` unpadded base64url `{deck, session}` through the EXISTING `__slPanelArg` sanitizer; `useComposeBinding` extracted (one implementation, two faces); `ComposeWindow` boots sink-then-open + pagehide autosave flush; shell `slPanelClosed` broadcast. Single-publisher rule = C2 |
| P3-C2 | build | The single-publisher rule | done | `composingDecks` lock: strip wears COMP face (channel tier stays live), master fans over `activeDecks`, session swap/drop + double-open refused; `handlePanelClosed` releases with a 500 ms grace for the closing window's flush. KNOWN LIMIT: a crashed window leaves the lock until the session drops. Machine walk green first run |
| P3-D4-M | spec | Measure + sketch the deck tile | done | `docs/merge/STRIP-DECK.md`. Verdict: **plane-side adapter, ZERO shell changes** (GridPanel@dj reads `djMeta/djPattern/djRuntime` topics + 3 HotFrame blocks; writes terminate in the companion). Real blocker: BrowserLink's single-slot handlers. Expanded tile 2×3 cells (~688×604), collapsed 340×196 exactly |
| P3-D4-3 | build | Per-deck/per-track HotFrame telemetry | done | All 144 `djTrackStep/Pos/Level` slots were zero-filled and never written (permanent step-0 wash) — `sl_hotframe` now writes them: step folded through the deck BR window + reverse mirror, pos −1-honest, level un-gated (ringing is the point). Fixture pins step ADVANCES across a boundary. engine:check drift pre-existing, recorded |
| P3-D4-1a | build | The MasterRow becomes REAL (BPM·VOL·DRV wired) | done | 8 keyed deck-scope snapshot names (masterVolume + clipper, validated); `sessionParam` seam in BrowserLink routes the row's writes to DOCUMENT verbs (`setMasterVolume/setMasterDrive`, clamped, autosaved); `GridBackend.meta` serves document truth. Audible fixture: vol 0.5 halves the peak; decoupled drive 16 audibly hotter |
| P3-D4-1 | build | The deck tile renders the REAL GridPanel | done | Plane-side per D4-M: `GridTopicNames` + `djGridTopics(deck)`, BrowserLink per-scope handler MAPS (last-mount-wins structurally impossible), `useDeckTileBinding`/`DeckFace` (pointerdown claims the keyboard), ⤢/⤡ = document cell geometry (692×612 ↔ 340×196 to the pixel). Walk: expand → real `.track-strips.density-dj` + MasterRow. Door: ⤢ on any loaded strip |
| P3-D4-2 | build | Deck verbs into the strip header | done | Expanded tile only: ▶ · REV · BR + fused-scale cycler (one BR_SCALE table shared with the master) · ‹› NUDGE (REAL: transient `nudgeStore` folded through THE LAW — engine hears 124/120, displays follow live, never the document) · SAVE (flush) · ⏏. BR/REV state moved INTO DeckState; LCM bar computed web-side. Nudge feel provisional (±4) — rides P3.5-AUDIT |
| P3-R1 | fixture | Chain→record is audibly TRUE | done | §11b: routed tone → bus-tap REC → the tape plays it back with the input dead; unpatch → silent. The engine needed NO fix — the fixture's one find was its own missing `setSource` (the trap R3 remembers) |
| P3-R2 | build | One gesture "loop that strip" | done | ⋯ `record from` lists every other strip: sets the tap AND patches through the plane's own consent flow (wouldCycle reused, duplicate cables avoided). Status line names what REC will capture. Real-host walk rides P3.5-AUDIT |
| P3-R3 | build | REC on a grid strip spawns its looper (P3-X1's defect, the signed way) | done | `Strip.onRecord` grid branch → `recordIntoLooper`: `linkedLooperFor` (cable is the link, tap is the intent) or spawn `LOOP · <src>` patched from the SOURCE'S BUS (no cycle possible), channel bound to tape. The grid element NEVER moves. STRIP-MODEL amended |
| P3-P1 | build | Retire the dead doors (djmode/transport/deckmixer) | done | `PANEL_MENU_SURFACES` exported + pinned by `panelMenu.test.ts` — the trio cannot quietly return; panels stay routed (no door ≠ deleted). ≡ menu recorded as interim scaffolding |
| P3.5-AUDIT | spec | Diff this section + STRIP-DECK.md against built rows; materialize gaps; then offer the USER GATE: library walk (no companion window), compose-window walk (edit heard, no stale world), deck-tile walk (real DJ rows in a strip), looper-routing walk, pads 1–8 | done (2026-07-29) | Every P3.5 row built; STRIP-DECK's sketch fully realized. Gaps → **P3.5-E1**. **Remaining USER GATE walks: library · compose window · looper routing · pads 1–8 · tile MasterRow audible · nudge bend-and-snap.** ✅ The looper-routing walk is now worth walking — it was gated on P3.5-E3, which shipped. |
| P3.5-E1 | build | Tile ergonomics from the real-host walk: wheel fixed (this commit); remaining — dj band control sizing/precision at plane scale, tile-vs-plane gesture arbitration beyond wheel, whatever the walk surfaces | todo (user-fed) | **⟦PORT⟧** donor: `DJModeView.swift` (2674 L) renders these same bands at FULL WINDOW width — the tile is a 692px scale-down of it, which is the ergonomics complaint. opened by the user's "hard to control its UI" — collects walk findings |
| P3.5-E2 | build | Session refused to reopen after a flam edit (sparse arrays → JSON nulls) | done | Two seams: `applyGridPattern` writes per-step arrays DENSE (`dense(a, fill)` with native neutrals — `.map` alone preserves holes); `migrateTrack` heals null entries back to neutrals so locked-out sessions open again (rescue, not tolerance — nulls elsewhere still throw). Fix rides the webdist |
| P3.5-E3 | build | **A grid strip's channel bus is SILENT** (D-SL-DECKOUT-01) — so P3-R3's signed gesture spawned a looper that recorded SILENCE | done | **BUILT: `SourceEndpoint::deckOut` (kind 4)**, fed by a new read-only core accessor `deckDryOut` (post-stretch/carve, PRE-crossfader, PRE-deck-drive); external like a device input, so it constrains no render order and can never close a cycle. Two alternative mechanisms measured and REJECTED (archived). ⚠️ Index space is the DECK's (3), never the channel's (8) — refused, not clamped. SCHEMA v94, MAP v7. Remainder split out as **P3.5-E4**. |
| P3.5-E5 | build | **Every map save silently ERASED the `deckOut` cable** (found 2026-07-30 by the plan audit, against SHIPPED P3.5-E3) — the one cable that carries a grid deck into a looper | done | **E3 shipped the write direction and not the read one, and the skip-the-unknown rule turned that into silent data loss** — `captureRoutes` DROPPED every deckOut route on save/export/autosave, and nothing was red because the fixture carried no route of that kind. Fixed as a class: the read maps are now DERIVED from the write maps (`invertKinds`), exhaustive by type, plus a pin driven off the schema's own enum so the NEXT kind fails until both directions know it. |
| P3.5-E6 | build | **Clicking a `deckOut` cable unpatched the WRONG route — or none at all** (found 2026-07-30 by the plan audit, against SHIPPED P3.5-E3) | done | Same root as E5: `unpatch` switched on a cable's SHAPE (`send === null`) instead of its KIND, so clicking a deckOut cable unpatched the wrong route or none. Fixed by `routeKeyOf(cable)` deriving the endpoint quadruple from the same tables that ISSUED it — **one table, both directions**. |
| P3.5-E7 | build | **A `.scoopySession` FOLDER cannot be imported — the folder picker was LOST when the library moved to the plane** (user, real host, 2026-07-30) | done (machine, webkit) — **real-host walk pending (user)** | **A REGRESSION against a door that worked, introduced by shipped P3-L1** — a `.scoopySession` is a DIRECTORY and P3-L1 rebuilt the import with one of the deleted panel's three doors. All three restored (`import` · `folder…` · drop target), funnelling into one reader now shared at `persist/folderImport.ts`. Walked in WebKit + chromium. ⚠️ Its WKWebView caveat is **CLOSED by P3.5-E8f** — the native `chooseDirectory` verb is not needed. |
| P3.5-E8 | build | **In compose, nothing loads a sample** (user, real host, 2026-07-30: "in compose it wont let me load any audio samples") | **measured → split** | **The row was wrong about where the door was missing.** `GridPanel` always drew both sample doors; each is only an INTENT a mounting surface must register for, and **`trackEdit` falls through to a silent `{ok:true}` when none is**. `useComposeBinding` — the surface a person actually composes in — registered NEITHER. Split: **E8a** · **E8b**. |
| P3.5-E8a | build | The compose surfaces register their sample doors | done (machine, webkit+chromium) — **real-host walk pending (user)** | `panels/sampleDoors.ts` — ONE `registerSampleDoors(link, deck, scope)` for all four surfaces, replacing three hand-written copies and the omission that caused the row; **`deck` (whose document) and `scope` (which handler slot) stay separate arguments.** ⚠️ **Second real defect fixed: the `showOpenFilePicker` branch is deleted** — the LOAD intent crosses an async hop that has already spent the transient activation that API demands (dead in Chromium, fine in WebKit). |
| P3.5-E8b | build | The file BROWSER has no home anywhere | done (machine, webkit+chromium) — **real-host walk pending (user)** | **The home is a FILES drawer in the compose surfaces** (`plane/ComposeFiles.tsx`), one implementation, two surfaces. ⚠️ **Deliberately NOT in `PANEL_MENU_SURFACES` (conductor ruling), and pinned so a later row cannot add it back** — a door that never lived in `≡ panels` cannot be orphaned when it retires, so P11-1 inherits no obligation. Three real defects found by measuring rather than reading (a duplicated `webkitRelativePath` root, lazy directory creation, and a `Choose folder…` button that rendered only in an empty state `root` never reaches here). |
| P3.5-E8c | build | **⌥-load (stretch import) is silently dropped on every compose surface** | todo | **⟦PORT⟧** donor: `asStretch` → `WebFileBrowserBinding.swift:183` → `BeatSequencer.addTrack(…, asStretch:)` `:15585` → `configureTrackAsStretch(trackIndex:)` `:15693`. ⚠️ **The row routes the flag and stops; the donor is what the flag must DO on arrival** (configure the track in stretch mode). Do not invent that half. `FileBrowserPanel` sends `asStretch: e.altKey` on BOTH load paths (`:157`, and the row-drop at `GridPanel.tsx:3259`), but `browserLink.ts:391-394` forwards only `trackIndex` and `path` — `registerSampleDoors`' `setSampleLoadHandler` has no third parameter to receive it. Holding ⌥ does exactly what not holding it does, and the panel's own tooltip promises otherwise. Pre-existing (E8a routed the intent, E8b made the door reachable), but the affordance is now in front of users. Same species as the silent-accept E8a fixed: the intent arrives and part of it is discarded. gate: vitest — the flag survives the hop; walk — ⌥-drop lands a stretch clip |
| P3.5-E8d | spec | **`fileBrowserBackend.load()`'s "THE FLIP" stub names a phase that has passed** | todo | **⟦NEW⟧** merge-local bookkeeping — no donor counterpart. `:256-258` still returns `load needs the TS document — THE FLIP (P5-06 step D). Preview works.` Unreachable from any surface registering compose-scope doors (browserLink intercepts first) but IS what answers on a surface that does not — so the honest boundary now lies. Decide: delete it, or make it name the real condition ("no composer is mounted on this surface"). gate: the decision written into the row |
| P3.5-E8e | build | The FILES drawer's width is fixed and its open state is not remembered | todo | **⟦PORT⟧** half: donor persists the drawer's OPEN state as `@AppStorage("fileBrowser.expanded")` (`ContentView.swift:150`) — a key of its own, exactly as this row asks. The WIDTH half is NEW. `.compose-files-body` is a hard 260px, not resizable, and the drawer reopens closed for every spawned compose window. `SETTING_FOLDED` exists as the native browser's shared fold key and the drawer ignores it — deliberately, since that key sizes the NATIVE frame, but the drawer has no persisted state of its own. Low priority; recorded so nobody "fixes" it by reusing the wrong key. gate: walk — width survives a reopen |
| P3.5-E8f | spec | **E7 and E8b share ONE unverified dependency: `<input webkitdirectory>` inside WKWebView** | **RESOLVED 2026-07-30 by the user's real-host walk — the good direction** | **RESOLVED by the user's real-host walk: E7's `folder…` button WORKS in WizardMerged.** WKWebView's open panel does offer a directory to `<input webkitdirectory>`. ⚠️ **The native `chooseDirectory` verb stays declared-and-unanswered at `schema.ts:457` and must not be built on E7's or E8b's account.** |
| P3.5-E8g | build | **The compose window's LOAD door opens the picker and the file never arrives — the track stays empty** (user, real host, 2026-07-30) | **DONE — confirmed by the user in the real host: "samples load now"** | Closed by **P3.5-E8g-a**. ⚠️ **The lesson worth keeping: when a door is silent, instrument it before theorising about it** — five outcomes, four of them mute, so neither a user report nor a test could name the seam; one honest header line turned the next walk into a diagnosis and retired two candidate rows on the spot. The report inverted on measurement: the file loaded every time; what failed was the evidence. |
| P3.5-E8g-a | build | **The compose grid does not repaint after a sample lands on a track** | **done — real-host walk PASSED on BOTH callers (user, 2026-07-30): "samples load now" · "files browser works too"** | **None of the four candidates was the defect.** The sample doors were the only doors in their family with no path of their own to the screen — every sibling handler republishes; these delegated the repaint to a React effect three layers away. Fix: `registerSampleDoors` republishes via `updateRuntime` (a sample landing is a runtime change), scoped peak paths moving with it. ⚠️ Its pins cannot see a repaint — **P3.5-E8g-f**. |
| P3.5-E8g-d | build | **`toggleLocatorRepeat` repaints only via the reload effect** | done (machine) — **real-host walk pending (user)** | **Inherited E8g-a's diagnosis AND its prescription; measured, the diagnosis holds and the prescription does not — that is the row's value.** `locatorRepeatActive` is a pattern-wire field absent from `toGridRuntime`, so E8g-a's one-liner would have repainted everything except the lamp the user clicked (pinned as a driven falsification). New verb `GridBackend.updatePatternRow(i, pattern)` re-projects ONE row and republishes only that topic. ⚠️ `gridDocument(deck)` must stay the ACTIVE SCENE's projection — pushing raw `sectionA` while scene C is live repaints with a pattern the engine is not playing. |
| P3.5-E8g-e | build | **A session reload resets the grid cursor to track 0** | done (machine) — **real-host walk pending; ⚠️ see P3.5-E8g-h, the user's actual want is different** | The reload effect is keyed on the session OBJECT, which every edit replaces, so `load` ran on every edit — **and also cleared `activeParams`, so every value-drag fell back to pitch after one edit**. `load` now takes the document's identity and resets only when it changes; an absent id reads as "different" both ways, so a caller that never opts in is unchanged. Cursor clamped on track deletion; a scene switch keeps it. ⚠️ **The user's actual want for the AIM is different — P3.5-E8g-h.** |
| P3.5-E8g-h | build | **An UNTARGETED sample load CREATES a new track** | **done (machine)** — real-host walk pending (user) | **⟦PORT⟧** `WebFileBrowserBinding.loadBrowserSample:203`. **The protocol already said this, and only this host disagreed.** `fileBrowser.test.tsx:94` has pinned *"omitting trackIndex is what means NEW TRACK"* since browser.md §2, and the donor implements it in those words. `browserLink.ts:392` coerced that documented absence into `grid.selectedIndex` — **re-aiming a CREATE at the cursor**, which is exactly what the user hit. The absence now travels as `null`, never `Number(...)` (`Number(undefined)` is NaN: an index that points at nothing while still looking targeted). ⚠️ **The create verb existed and reached nothing.** `addTrack` is declared (schema :1441) with **two live callers** — the MasterRow `+` and **⌘T** (`registry.ts:211`) — answered by `BrowserLink` never and routed by `MergedLink` never. **Both doors have always done nothing**, invisible because both are fire-and-forget `.catch(() => {})`. One store verb now backs all three. (Distinct from `nativemethods:check`'s class: declared-and-answered-by-nobody, not shell-handles-but-link-drops.) ⚠️ **The 16-track ceiling was enforced by NOTHING** — the constant only greyed a button, the schema's refusal was Swift's, `patternFile.ts:672` types sections with no length bound, neither engine counts tracks. `MAX_TRACKS` is the first thing that holds it, matching `maxAudioTrackCount = 16`. **The port check moved three things the lane had guessed** — the argument for reading the donor: the new track **takes the cursor** (`addTrackInternal:15656`); `loadSample` now **refuses** an out-of-range index instead of falling through while still reporting success; and `trackGain` — see below. A new track is **copied from `fresh-default.json`**, not hand-built (a hand-typed list across ~155 fields is the `SECTION_KEYS` injury one level down), deep-cloned so two appends cannot share a `steps` array. Repaint needs the **full `load`** with the document id: `updateRuntime` only walks existing rows, `updatePatternRow` refuses an index past the end, and neither republishes `gridMeta`, whose `trackCount` is what says a ninth row exists. `gridCursor.test.ts` **repointed, not deleted** — E8g-e's property is unchanged, the door it was measured through moved. ⚠️ **`trackGain` is the ONE place we do NOT follow the donor. RULED (user): unity, and NO load-time normalisation.** The donor gives an ADDED track 0.80 while fresh-session tracks sit at 1.0 — the same sample quieter depending on how its track came to exist, an inconsistency rather than a headroom policy. A sample now plays at the level it was authored at, so a kit's balance survives. **Do not add peak/RMS analysis to "improve" this** — `sampleStore.peakEnvelope` already computes it for the waveform, so it is easy to reach for, and it was ruled out. ⚠️ Expect the new track **SILENT until programmed** — `makeEmptyTrack` creates all-false steps and the donor behaves identically; only the ⌥ path lights step 0 (P3.5-E8c). gate: real host — select track 5, double-click a FILES row: new track at the end, track 5 untouched, selection moved. Targeted paths still REPLACE. **Free with this row: ⌘T and the `+` now work at all** |
| P3.5-E8g-h-a | build | **⟦PORT⟧ ⌥-stretch on the new-track path — the spec is now known EXACTLY** | todo | Enriches P3.5-E8c rather than replacing it. The donor honours ⌥ on BOTH web load paths and says so: *"the web browser applies ⌥-stretch on BOTH load paths… Native only honours ⌥ on the new-track path… that asymmetry is a papercut, not a behaviour worth porting"* (`BeatSequencer.swift:15694`). Verbatim algorithm at `:15700-15720`: `playbackMode = .stretch`; clear every step; `steps[0] = true`; grow `cellLengths`/`fullReleaseSteps`/`fullReleaseAutoExtend` to `stepCount`; `cellLengths[0] = stepCount`; `fullReleaseSteps[0] = true`. E8g-h routed the carrier (`asStretch` still stops at `browserLink`) but deliberately did not build this — projection-layer, its own row |
| P3.5-E8g-h-b | spec | **⟦PORT⟧ A companion document edit pushes NO UNDO entry** | todo | `addTrackInternal` brackets itself in `makeUndoSnapshot()` / `pushCompletedUndoEntry(scope: .topology)` (`:15617`, `:15681`). The browser `undoStore` is only ever `resetUndo`-ed — so **⌘Z after adding a track does nothing**, nor after `loadSample`, `setBpm` or `setMasterVolume`. Pre-existing and wider than E8g-h, but an accidental append is the easiest edit to want back. Decide: port the undo scopes, or write down that companion edits are not undoable |
| P3.5-E8g-h-c | build | **⟦PORT⟧ `templateStepCount` / `templateSamplerMode` have no browser home** | todo | `makeEmptyTrack` reads both from UserDefaults (`:15508`, `:15511`), so on the desktop a person can set what a NEW track looks like. The browser copies the template unconditionally — equal to the defaults today, a real divergence the moment sessions move between hosts |
| P3.5-E8g-h-d | spec | **⟦PORT⟧ MIDI tracks: the append verb only knows one kind** | todo | The donor has `addMidiTrack` and a **separate** `maxMidiTrackCount = 16` (`:15492`, `:15594`), and refuses a sample onto one (*"…is a MIDI track — it takes no sample"*). `MAX_TRACKS` here counts ALL rows and `loadSample` cannot see a MIDI row. Not reachable today; write it down or the ceiling silently means something different once `trackType` arrives |
| P3.5-E8g-f | fixture | **The suite has no jsdom and no React renderer — so NO gate in this project can see a repaint** | todo — **a dependency decision the conductor/user owns** | **⟦NEW⟧** merge-local test infrastructure — no donor counterpart. **MERGED 2026-07-30 (Lane C audit): this row now carries P11-1-e and P9-5c-b, which were the same finding written three times.** `react-test-renderer`/`@testing-library` are not devDependencies, so every pin in the tree stops at the topic push and **every "it appeared on screen" claim in this project is walk-only.** Three distinct consequences, all kept: **(a)** *the repaint chain* (from P3.5-E8g-a/-d/-e) — the sample-door and locator-repeat republishes are pinned at the store, and no gate connects that push to `TrackBand` re-rendering. **(b)** *the plane's note line has NO mount-level pin at all* (from **P9-5c-b**) — nothing in `web/src` renders `PlanePanel` in a test, so P3-U6's refusals, P3.5-E9a's silence report and P9-5c's device-switch note are each proven at the seam and then at the real host, with nothing in between; **one jsdom pin mounting the panel and asserting `.plane-note` carries a pushed note would cover all three at once** and catch a broken `useEffect` wiring typecheck cannot see. **(c)** *layout has no width coverage* (from **P11-1-e**) — P11-1's zones `flex-wrap` rather than clip, which is what keeps every control reachable in a narrow window, and the `flex-wrap`/`min-width: 0` pair is one deletion away from silently clipping the document zone; either a browser walk that narrows the window and counts visible bar buttons, or an explicit walk-only note. **The decision is one decision:** add a renderer and pin (a)+(b) from a driven store, or write all three down as walk-only properties. ⚠️ Its own precedent is **P3.5-E8g-c** (done): a fake thin enough to pass either way is a green gate that cannot see the defect. gate: the decision written into the row, or a renderer landing with pins for (a) and (b) |
| P3.5-E8g-g | spec | **`GridPanel`'s owner-echo window can silently swallow a document push** | todo | **⟦PORT⟧** ⚠️ **orphaned by the merge, not merge-invented**: the owner-echo window's only escape (`adoptNextEcho`) fires on Swift's `swiftEdit` event, which the donor emits and the merged stack never does. So the guard came across and its release did not — same species as P7-K7 and P8-P1. `GridPanel.tsx:805` drops any `gridPattern/<i>` landing within `OWNER_ECHO_QUIET_MS` of the panel's own publish, and `ownerPatterns` is **always true in this host** (`gridBackend.ts:215`). A document write from a non-grid source inside that 300 ms window is discarded **with no marker** — the `adoptNextEcho` escape exists only for Swift's `swiftEdit` event, which the browser stack never emits. Not E8g-a's cause (a picker takes seconds) and not known to bite anything today, but it is a silent-drop path with no owner. Close it or write it down as accepted |
| P3.5-E8g-b | spec | **`decodeAudioData` in a non-plane WebView is unverified** | **MOOT 2026-07-30 — answered by the user's E8g walk** | **MOOT** — the E8g walk reached `<file> → track N`, set only after the sample decoded and the document was written, so `decodeAudioData` does work in the compose WebView. Kept so the question is not re-asked. |
| P3.5-E8g-c | fixture | **Audit the no-jsdom fakes that stop at the gesture** | done (2026-07-30) | The audit surface was three files; the one that still had the hole was the test covering `chooseFolder` — **the door the user PROVED works in the real host**. Deleting `document.body.append(input)` left all 16 tests green; it now fails one. ⚠️ **The general lesson, with P3.5-E10's: a fake thin enough to pass either way is a green gate that cannot see the defect.** |
| P11-1-a | spec | **`≡ panels` cannot leave the bar yet — and FX 1–4 are not the only reason** | **DECIDED 2026-07-30 (user): `≡ panels` STAYS INDEFINITELY** | **DECIDED (user): `≡ panels` STAYS INDEFINITELY.** The five `PANEL_MENU_SURFACES` have no other door and D-SL-TOPROW-01 names a new home only for *settings*. Rejected and recorded so they are not re-proposed: folding into `map ▾`, a new `tools ▾`, retiring the five surfaces. **P11-1 is complete as shipped, not scoped-pending-a-decision.** |
| P11-1-b | build | ~~`compose` may leave the bar safely — the per-strip door already exists~~ | **done — shipped inside P11-1 (2026-07-30)** | P11-1 retired the bar's `compose` button and recorded why: it could only ever open the FIRST grid strip, and every grid strip already draws its own `COMPOSE ⇱`, so **the retired door was a less capable duplicate of one that stays**. `firstGrid`/`firstLoadedDeck` went with it. Nothing outstanding — this row was still reading `todo` against work that had already landed |
| P3.5-E9 | spec | **A loaded scoopy session makes no sound** (user, real host, 2026-07-30) | **measured → split** | The store has always KNOWN why a deck is quiet, and **all three signals were read by exactly ONE surface — the browser-only `CompanionPanel` whose door P3-L1 DELETED.** So a session loaded, looked normal, played nothing, and *the app could not tell you*. Split: **E9a** · **E9b**. ⚠️ Confirmed en route: the merged host decodes web-side and ships PCM over the bridge, **so a WebView decode failure is a silent track in the NATIVE engine**. |
| P3.5-E9a | build | The silence explains itself | done (machine, webkit+chromium) — **real-host walk pending (user)** | `store/sampleReport.ts` `silenceNote(...)` on the plane's note line and the compose window's bar. Four rules pinned because each is a way the report would be worse than none: the engine short-circuits first; **`starting` reports NOTHING**; the list names up to four and counts the rest; the line ends in the CONSEQUENCE. Verified to FAIL with the note suppressed. |
| P3.5-E9b | spec | The real-host measurement, now that the app reports | **RESOLVED 2026-07-30 (user, real host): "sessions are audible now"** | **RESOLVED (user): "sessions are audible now."** The cause was found from the other end — **P3.5-E10**, the whole DSP path at `-O0`. E9a's readout stays and is the first thing to read when a deck is quiet. |
| P3.5-E10 | build | **The app was built with NO optimiser — `CMAKE_BUILD_TYPE` was empty, so the entire DSP path compiled at `-O0`** (user, real host, 2026-07-30: "immediately shows huge weakness when playing more than 1 deck"; the original ScoopyLoops ran three decks with extreme time-stretch on the same machine) | done (machine, measured) — **real-host confirmation pending (user)** | Root `CMakeLists.txt` set no default build type, so every engine object compiled with **no `-O` and no `-DNDEBUG`** — and `build/` is the tree that produces `WizardMerged.app`. Measured same commit: stretch test **3.38s → 0.28s (12.1×)**, varispeed **15.4×**. Fix: default to `Release` when unset, FORCEd with a visible `message(STATUS)`. ⚠️ **Every latency figure in P6 was measured through this build — P3.5-E10a.** ⚠️ **No gate could have caught it — P3.5-E10b.** |
| P3.5-E10a | spec | **Re-measure P6's latency figures on an optimised build** — P6-6a calls itself "the ONLY measured, reproducible latency defect in P6" and P6-6c must publish a ~116 ms tape-stretcher figure; both numbers were taken at `-O0` | todo — **BLOCKS P6-6a/b/c** | **⟦NEW⟧** merge-local re-measurement. The *seam* P6-6a describes survives the flag: a deckOut-fed channel is one block behind because of render ORDER, and a block is a block at any optimisation level. What does not survive is every **magnitude** in the P6 notes. P6-6c's whole job is to publish a latency number into the UI, and a number derived from a 12× slower build describes an app nobody runs. Re-measure first; if the re-measurement changes where the cost actually sits, rewrite P6-6a/b/c rather than patching their notes. gate: the P6 figures restated against `-O3`, with the old and new numbers side by side |
| P3.5-E10b | fixture | **A performance gate: N decks with extreme stretch inside a CPU budget** | done (2026-07-30) | `slengine/tools/sl_perf_test.cpp` — three decks at stretch ratio 1.6 (a neutral scene measures nothing; the core bypasses the vocoder). ⚠️ **THE FINDING: the first draft failed its own audition.** A realtime-ratio budget alone was GREEN against the very regression it exists for (-O3 0.047x vs -O0 0.273x, both inside 0.5x), because **a budget loose enough for a slow CI runner has more headroom than a 6× build regression needs**. So two halves: **(1) build configuration asserted directly** — `NDEBUG` absent means no optimiser, machine-independent and exact (escape hatch `ctest -E sl_perf_test`); **(2)** the 0.5x realtime budget for an algorithmic blow-up in an optimised build. Prints `deckMissCount` but does NOT assert on it. ⚠️ The budget is a measurement — re-measure before tightening. |
| P3.5-E4 | build | **[HOISTED — runs after P6-AUDIT, before P7: D-SL-E4-HOIST-01 SIGNED 2026-07-30]** A grid strip's OWN channel bus still carries nothing — so `channelBus` capture pointed at a grid strip records silence, and its `→ main` chip names a route that carries NOTHING (the core delivers that deck to main itself, so removing that cable would not even silence it). E3 made the deck routable; this makes the grid strip's own bus real | todo | **⟦PORT⟧** the deck-side arithmetic is the donor core's `dedicatedOutput` + `deckCleanGain` path, already vendored (`NativeAudioEngineCore`); the channel tier it must meet is wizard-side, so the SEAM is new and the topology is not. **Measured inside E3 — it is the dedicated-output unification, not a patch:** flip `dedicatedOutput = true` for a deck bound to a channel so the core stops summing it to main, have the channel read the deck lane as its element and CONSUME it (the P6-3 host-return precedent, so nothing leaks to a mapped hardware out), and retire the `setDeckGainOverride` projection for level/mute. That also closes the ⚠️ in `sl_channel.h`/`sl_engine.h` (a gridDeck channel's meter reads 0) and would let P3-X2's per-strip DRV apply to grid strips at the channel tier. **Why it was NOT done in E3:** it moves the deck's ENTIRE signal path — the core's per-deck master stage runs different arithmetic on a dedicated lane (`deckCleanGain` + drive at `volume = deckMasterVol`, vs pre-sum drive at volume 1.0 with volume riding `crossfaderGain`), which is exactly what P3-D4-1a's audible fixture pins, plus crossfader/X-MIX-carve and instrument interactions. Sized like a phase row, needs its own audible A/B against D4-1a. Reachable today because E3's cable covers the gesture people actually perform. **⚠️ WIDENED by the 2026-07-30 audit — the measurement above under-scoped it:** `projectToCore` forwards all four SENDS as well as level/mute, so retiring only the level/mute projection would **double-send**; and E4 cannot be built without widening `LaneMap` to the deck lanes, which is half of the "per-deck outs" item ROUTING-MATRIX deliberately keeps recorded-not-queued — that item is a DEPENDENCY, not a neighbour. Hoisted (D-SL-E4-HOIST-01) because it is a hard dependency of P9-1's headline walk — a grid strip's send tap records SILENCE without it, `sl_channel.cpp:648-658` assigns `elemL/elemR` only for tape — and of P7-T4, and because it retires the `setDeckGainOverride` projection (`sl_engine.cpp:456-469`) that two further phases would otherwise build on top of |

# P6 — plugins on the returns (opened 2026-07-29, D-WZ-PDC-01 signed)

> The user's direction, verbatim intent: "we still need a strip mixer to control each
> volume and the fx send channel for plugin load, this can almost be as in scoopy —
> just needs to be fitted into the map system so we can also restore these settings
> (like a plugin loaded) within a map." Route: REUSE scoopyloops' own JUCE-based
> `NativePluginHost` (header already vendored; the core's per-return `NativePluginSlot`
> render machinery already in the vendored core behind `SCOOPY_PLUGIN_HOST=0`).
> The comparison audit (2026-07-29) confirmed the mixer/fx UI surfaces
> (DeckMixerPanel · FxSlotPanel · InstrumentPanel) are byte-identical in this tree —
> nothing to rebuild web-side; the work is host plumbing + map persistence.
> **Loop order:** ~~P6-1~~ → ~~P6-2~~ (+ ~~P6-2b~~, the FX door's off-by-one) → ~~P6-3~~ →
> ~~P6-4~~ → ~~P6-5a~~ → ~~P6-5b~~ (all done 2026-07-30) → **P6-6** → P6-AUDIT. Same loop
> protocol, same four rules, push after green.
>
> **USER DECISION (2026-07-29, asked directly): the deck MIXER UI waits for the
> plumbing.** Run this queue as written — audible → editors → map → PDC — and
> then design the strip-integrated mixer (deck volume + FX send channels with
> plugin load + transport, per the recorded vision) as its OWN phase (P7), with
> everything underneath it already proven. DeckMixerPanel stays doorless until
> then; per-strip LEV/SEND faders on the plane remain the mixing surface.
>
> **AMENDED 2026-07-29 (D-6 … D-12 signed, same day, later):** P7 is RE-SCOPED as
> *"the deck, whole"* — the full deck-transport tile, full-viewport navigation,
> the live keymap and the launch chooser open NOW, because none of them need
> plugins. The strip-integrated mixer above survives as P7's TAIL (row P7-MIX-0),
> still blocked on P6-AUDIT exactly as this decision required. **P8** opens for
> the map performance overlay (D-SL-MAPPERF-01), **P9** for broad routing
> (D-SL-RECFROM-01), and **P10** is earmarked for the virtual interface
> (D-WZ-VDEV-02). One defect is hoisted ahead of all of it: **P3.5-E3**.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P6-1 | build | The host compiles IN (`NativePluginHost.mm`, flag, scan worker) | done | `.mm` at `ScoopyLoops/NativePluginHost.mm` (2 documented adaptations); `SCOOPY_PLUGIN_HOST_ENABLED` = one option two implementations — app compiles it real (JUCE AU/VST3 + CoreAudioKit), `scoopy_plugin_host_stub` serves JUCE-less gate tools via lazy-archive dispatch. Flag-ON≡OFF verified region-by-region, then ctest 65/65. Smoke: scanned Apple's AUDelay out-of-process, XML printed |
| P6-2 | build | The scanner reaches the UI (fxSlot dispatch, picker live) | done | `HostServices.pluginScanner` (headless dispatcher refuses by name — same code path IS the honesty proof); ABI v3 `sl_fx_select/name/latency_ms/teardown`; `toolbarState()` pushed ~2 Hz → FxSlotPanel leaves WaitingForState. Contract PINNED by exact key-set tests + C++-built JSON parsed by the real zod schema. Real-host walk (≡ FX 1 → RESCAN → pick) rides P6-AUDIT; AUDIBLE is P6-3 |
| P6-3 | build | A plugin is AUDIBLE on a return (= old P3-3-1/3-2): instantiate per-return `NativePluginSlot`, `returnFx: true`, strip sends 1–4 → return → plugin → wet lane audible for BOTH element kinds; plane_audio_test fixture (send up → return energy; send 0 → silence) | done | **PROVEN WITH A REAL AU** — `plugin_audible_test` (Apple-only, SKIP 77): tape strip tail 0.37 on MAIN after the input dies, grid strip 0.28, unload → gone. ⚠️ **The load-bearing law: tape-strip sends reach a plugin ONE BLOCK LATE via the `hostSendFeed` seam** (P6-6a's territory); grid sends are core-internal and same-block. ⚠️ `send` is 0-BASED at the ABI. ⚠️ **OPEN measurement: per-track grid send dials unproven — they ride P6-AUDIT.** `engine:check` drift is pre-existing and recorded here. |
| P6-2b | build | **The `≡ → FX n` door addressed the WRONG return** (found 2026-07-30 while measuring P6-4) — the menu passed the 0-based menu ROW as the window's arg, and `FxSlotPanel` reads that arg as a 1-based `returnIndex`: so **`FX 1` sent "0"**, the panel indexed `fxSlots[-1]`, got undefined and fell through to `WaitingForState` — **"waiting for state" FOREVER**, indistinguishable from a slow engine. `FX 2`…`FX 4` quietly drove returns **1…3**, and **return 4 had no door at all**. 0 also fails `selectFxPlugin`/`fxSlot`'s own `min(1)` on the wire, so nothing on that window could have worked | done | The `≡ → FX n` menu passed the 0-based menu ROW while the panel reads a 1-based `returnIndex`, so **FX 1 sent 0 and waited for state forever**, FX 2–4 drove returns 1–3, and **return 4 had no door at all** — a defect against shipped P6-2/P6-3 that would have killed their walk on step one. Fixed both sides: `FX_MENU_RETURNS` where label and arg are the same number, and a pure `addressedReturn()` rendering a visible refusal instead of an eternal spinner. |
| P6-4 | build | Plugin EDITOR windows, JUCE-hosted (the job FxSlotWindowController.swift did on the desktop): open/close per return; editor visibility state per F1–F4 | done | The window machinery existed since P6-1; what was missing was the lane and **a DOOR**. Two lock-free mirrors, ABI `sl_fx_editor_*`, and the dispatcher's first-ever `fxSlot` handler (the other three ops refused BY NAME for P7-MIX-0). **THE STATE IS THE WINDOW, NEVER THE REQUEST.** ⚠️ **DOOR: `≡ → FX n ⇱` → EDIT** — it had to go there because the desktop's EDIT lives on the doorless `DeckMixerPanel`; it rehomes at P7-MIX-0. ⚠️ **No headless test can prove the window APPEARS — P6-AUDIT's walk, by nature not omission.** |
| P6-5 | build | THE MAP REMEMBERS (the user's sentence made literal): per-return fx-slot state — plugin `identifier` + opaque state blob, restored on map open, honest refusal when a plugin is missing | done (via P6-5a + P6-5b) | Split on measurement into **P6-5a** + **P6-5b**. ⚠️ **`mode`/`output` are deliberately NOT persisted** — nothing in the merged host can set them, and persisting a field no control writes is how a document acquires fiction. They land with the controls at **P7-MIX-0**. |
| P6-5a | build | The engine/wire half: read what a document must store (identifier + opaque state), and restore from it | done | `loadedIdentifier()` + ABI `sl_fx_plugin_identifier`/`_state`/`_select_state`, the stateless select FORWARDING to the stateful one so validation cannot drift. ⚠️ **THE IDENTIFIER, NOT THE NAME** — a document keyed on display name reloads a different plugin on a machine with two. ⚠️ Signed trade, pinned: **a rejected state blob leaves the plugin loaded at DEFAULTS rather than failing the load.** Gate `plugin_state_test`. SCHEMA v96. |
| P6-5b | build | The document half: `.scoopyMap` carries per-return `{identifier, state}`; captured at save, restored on map open, honest refusal when the plugin is missing | done | **A map remembers its plugins.** `MapSchema.fx` is a TUPLE of exactly four `{identifier, state}`; MAP **v8** + migration to four EXPLICIT empty returns. Captured from the ENGINE, because a plugin's settings are made in its own editor. ⚠️ Slots matched by `returnIndex`, **never list position**. ⚠️ **The portable-map hazard is closed**: a restore that could not resolve an identifier records that, and capture preserves the stored entry verbatim rather than erasing the plugin on the machine that had it. **Unblocks P8-6.** |
| P6-6 | build | ~~PDC per the signed policy~~ | **refused-as-written → split (D-SL-PDC-REF-01 SIGNED 2026-07-30)** | **Refused-as-written and split (D-SL-PDC-REF-01).** Two rulings govern P6-6a/b/c: **(1) PDC here means WET-vs-WET, dry untouched** — there is no separate monitor path to spare, so the literal reading would delay what the performer hears, which D-WZ-PDC-01 forbids; **(2) clause 2 is already half-shipped** — the per-RETURN readout ships behind a reachable door, and "per-CHANNEL" has no subject because there are no channel inserts. ⚠️ Third defect: **no gate can prove it today**, which is why P6-6b must ship the `sl_fx_set_test_latency_samples` seam. |
| P6-6a | build | The `hostSendFeed` one-block seam — the ONLY measured, reproducible latency defect in P6 today | todo — **MEASURED 2026-07-30, and it SPLITS IN TWO** | **⟦NEW⟧** no donor: the donor has no host-side channel mixer at all — its sends are core-internal, so the `hostSendFeed` one-block seam is a merge artefact. **The render order, read at `sl_engine.cpp:315-392`:** (1) `tapes.renderPlayback` — **this block's tape audio already exists**; (2) lanes zeroed; (3) `hostSendFeed[s] ← stripSendHold[s]`, i.e. LAST block's channel-send diff (:338-347); (4) `core.render` — the plugin gathers lane + feed; (5) `sendAfterCore[s] ← lane`, isolating the core's own contribution (:355-357); (6) `deckOutPtrs ← core.deckDryOut` — **only valid after render** (:370-374); (7) `channels.mixInto` adds the strips' sends (:375-376); (8) `hold = lane − sendAfterCore` → next block's feed (:381-392). **So `mixInto` cannot simply move before `core.render`: it consumes deckOut pointers that do not exist until render has run.** That is what makes this a real row and not a reorder. **THE SPLIT, and it is the row's whole point:** a channel whose element is a **TAPE** needs only tape audio (step 1) and its own send levels — nothing from the core — so its send contribution CAN be computed before `core.render` and arrive **same-block**. A channel fed by a **`deckOut` cable** cannot: that deck renders *inside* `core.render`, so its one block is **irreducible**. Build: a partial early pass computing tape-sourced channels' send contribution pre-render, leaving deckOut-fed channels on the existing hold path. **Do NOT let the row promise otherwise** — an honest readout must say a deckOut-fed send is one block behind, because it always will be. Gate: an audible fixture pinning tape-send and grid-send arrival in the SAME block, plus a second pin that a deckOut-fed send is still exactly one block behind (the irreducible case, asserted rather than wished away) |
| P6-6b | build | Wet-vs-wet return alignment per D-SL-PDC-REF-01 + the test seam that makes it provable | todo | **⟦NEW⟧** ⚠️ **and the donor deliberately does NOT do this** — `AudioEngineFacade.swift:795-798` exposes plugin latency and says in its own comment that *no PDC is applied*. So there is no shipped answer to copy, and P6-6's long reasoning toward WET-vs-WET was unavoidable rather than wasted. Each host return's wet delayed by (max active return latency − its own) before it sums to main; every dry path untouched. **The seam is the point:** add `sl_fx_set_test_latency_samples` so alignment is provable at 0 / 64 / 1024 samples on the STUB build — without it no headless gate can produce a nonzero latency and the row cannot be gated at all (see P6-6's defect 3). Gate: alignment asserted at three latencies on the stub, plus the real-AU path unchanged |
| P6-6c | build | Per-element latency published + its UI surface | todo | **⟦PORT⟧** half: per-RETURN latency is `AudioEngineFacade.pluginLatencySamples(returnIndex:)` `:797` → `NativeAudioEngineBridge.mm:588`, which the merged tree already ships (P6-6 defect 2). The per-ELEMENT half is NEW. Clause 2's genuinely-new half. **First: read P6-6's defect 2** — the per-return readout already ships; what is missing is per-ELEMENT. ⚠️ Honesty constraint the audit raised: a readout showing a 2 ms plugin while staying silent about a ~116 ms tape stretcher is less honest than no readout — the stretcher's delay must be in the same number or the row must say why not. ⚠️ Mechanical: any new `FxSlotState` field hard-breaks P6-2's exact key-set pin. Any ms figure computed web-side inherits P9-3(c)'s 512/48000 hardcode — so this row either lands after P9-3(c) or carries its own live-blocksize source |
| P6-AUDIT | spec | Diff this section against built rows; then the user gate: load a plugin on FX1, hear a strip's send through it, tweak it in its editor, save the map, reopen — the plugin and its settings come back | todo | offered after all rows. **Walk items banked by earlier rows, each with its reason:** (1) P6-2/P6-3's `≡ FX 1 → RESCAN → pick` — now that P6-2b fixed the door it can actually be walked (before, FX 1 was the one window that could never render); (2) **P6-4's editor: EDIT opens the plugin's real window, and closing it by its own close box un-lights EDIT** — no headless test can prove a window appears (the gate binaries link the stub and run with no message loop), so this is real-host-only by nature, not by omission; (3) P6-4's honest-refusal path: EDIT stays disabled with a reason on a plugin that has no editor of its own |

# P7 — the deck, whole (opened 2026-07-29; D-SL-DECKFULL-01 · D-SL-LAUNCH-01 · D-SL-NAV-01 signed)

> The user's redirection, verbatim intent: "a strip will not hold ALL items at once …
> the scoopy strip window (deck) will be pretty much exactly the scoopy loops dj deck
> view WITH its deck transport … this way we can be sure our advanced scoopy system
> functions correctly ongoing, we can switch to composing mode from here." Plus: a
> launch prompt for PLANE or COMPOSE, and "the list is long and should work fully, so
> we need to be able to high decks and strip to use our navigation system — strips
> should be full viewport on command and then be able to tab through the individual
> strips in full size."
>
> **What this phase is NOT:** a new deck UI. Every classic component is already ported
> (`TransportPanel.tsx::DeckBlock` + `DjMasterBox` + `LcmBar`, `ScenePads.tsx`,
> `NudgeBox.tsx`, `MasterRow.tsx`, `trackRowControls.tsx`). But `DeckBlock` speaks
> `deckSection` / `transportDeck` / `menuTransport` / `djSetting` and reads the
> `toolbar` + `dj` UiState topics — none of which the merged dispatch answers (the
> P3-M-1 measurement; the reason P3-P1 retired those doors). So the rows below
> **REBUILD each row on companion lanes, the P3-D4-2 pattern**, using DeckBlock as the
> source list. Same for shortcuts: ~90 of ~100 keymap entries are `owner:"native"`,
> dispatched by a Swift `HotkeyManager` that does not exist here, through a `forwardKey`
> command `shell/` never handles — the keymap's own header already names the TS
> dispatcher as the plan, and P7-K1 finally builds it.
>
> **Loop order:** P7-K0 → P7-L1 → P7-L2 → P7-L3 → P7-N1 → P7-N2 → P7-K1 → P7-K2 →
> P7-K3 → P7-T1 → P7-T2 → P7-T3 → P7-T4 → P7-T5 → P7-V1 → P7-V2 → P7-V3 →
> P7-MIX-0 (when P6-AUDIT signs) → P7-AUDIT → P7-G1. Same loop protocol, same four
> rules, push after green.
>
> **⚠️ Global order across phases — THIS BLOCK IS THE ONE AUTHORITY (rewritten
> 2026-07-30).** The audit found THREE blocks in this file each stating a global
> order and two of them stale at HEAD; the other two (:42-44, :148-150) now defer
> here. Current order:
> ~~P3-X2~~ → ~~P3-F3~~ → ~~P3.5-E3~~ → ~~P6-4~~ → ~~P6-5a/b~~ → ~~P3.5-E5~~
> (all done 2026-07-30) → ~~P3.5-E7 · E8a · E9a~~ · ~~E10~~ (USER-FOUND, real host —
> done 2026-07-30, all four awaiting the user's real-host confirmation) →
> ~~P3.5-E8b~~ → **P3.5-E8g (USER-FOUND 2026-07-30, real host — the compose LOAD
> door opens the picker and the file never arrives; blocks basic use, comes
> FIRST)** → **P3.5-E10a — BLOCKS the P6 chain below** →
> **P6-6a → P6-6b → P6-6c** (P6-6 refused-as-written,
> D-SL-PDC-REF-01) → P6-AUDIT → **P3.5-E4** (HOISTED by D-SL-E4-HOIST-01) → P7 →
> P8 (**P8-0 first**, D-SL-MAPOPEN-01) → P9 → (P10).
> P3.5-E1 stays user-fed. P3.5-E9b is user-gated and diagnostic. P3.5-E10b (the
> performance gate) rides off-chain like the H rows — it blocks nothing and
> nothing blocks it.
>
> **⚠️ E10a is not optional bookkeeping.** P6-6a/b/c exist to measure and publish
> latency, and every figure they were written against came from an `-O0` build
> that ran the stretcher 12× slow. Building them on those numbers would ship a
> PDC figure describing an app nobody runs.
>
> **Verify, every row:** `ctest --test-dir build --output-on-failure` +
> `npm run typecheck && npm test` + the **nine** drift gates (`schema:check` joined
> at H5-a; **there is no `protocol:check` and no `abi:check` — see CLAUDE.md**);
> UI rows add a walk against the BUILT bundle. Real-host reachability in
> WizardMerged is proven at P7-AUDIT/P7-G1 — tests pass ≠ it works ≠ it shipped ≠
> you can reach it — and since H5 a machine can check part of it:
> `-DWIZARD_TEST_REALHOST=ON` builds `MergedWalk`, which drives the REAL shell in
> WKWebView. It does not replace the human visual pass; it means "the door is
> unreachable" no longer has to wait for one.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P7-K0 | spec | `docs/merge/NAV-SHORTCUTS.md`: audit all ~100 keymap entries — a merged-verb target per entry, the `dj`→focused-strip context map, the digit-allocation ruling (provisional: digits = scenes on the focused strip, ⌘/⌥-digits = focus jump, Tab/⇧Tab = cycle), and every dead entry parked WITH its reason. Also picks the full-viewport mechanism (plane transform vs overlay mount) | done (2026-07-30) | **`docs/merge/NAV-SHORTCUTS.md`, 683 lines. 99 keymap rows → 216 chord slots: 42 with a reachable merged verb, 57 parked with a written reason each.** ⚠️ **THE FINDING THAT REFRAMES THE K-SERIES: WizardMerged has NO native keyboard at all** — no `MenuBarModel`, no `KeyPress`, no `forwardKey` handler. **Every keystroke is answered by JS or not at all**, so P7-K1's dispatcher is the ONLY delivery mechanism for the 90 native-owned rows. ⚠️ `KEYMAP` and `GridPanel.handleKey` are two undeclared halves of ONE keyboard (12 rows already live in TS) — **K1's first duty is reconciliation, not addition.** **LIVE RULINGS:** `code` throughout (`ö/ä/ü` by `key`); `hostKind()` on `engineLink.ts`, no schema change; `dj` retires, `plane`/`strip` join, A/B pairs collapse onto `mapStore.selectedKey`; **digits PROVISIONAL (D-8)** — 1–8 = scenes on the focused strip, ⌥1–8 = focus jump, Tab/⇧Tab = cycle, **⌘-digits NEVER bound**; **full viewport = OVERLAY MOUNT**, never the plane transform (`updatePlaneView` writes a PERSISTED field). Also corrects **P7-T3** and confirms **P7-N1**'s defect. |
| P7-K0a | build | `keymap.ts` gains matchable chords | done (2026-07-30) | `keys` untouched, `chords` added beside it, **neither derived from the other**; 216 slots, reconciliation with the doc itself pinned. **`parked` is a positional argument of the only constructor that can produce a parked row, so parking without a reason is not expressible.** ⚠️ **The chord-level collision test found THREE overlaps the token test structurally could not see** (P7-K0a-b). Gate proven non-vacuous. ⚠️ **Corrects a premise for P7-K1: `GridPanel.handleKey` matches `e.key`, not `e.code` — P7-K0a-a.** ⚠️ `keymap.ts:16-22`'s three false claims left for **P7-K6**. |
| P7-K0a-a | build | **K1's dispatcher must CONVERT discriminants:** `GridPanel.handleKey` matches `e.key`, the keymap declares `code` | todo — **P7-K1 depends on it** | **⟦PORT⟧** donor settles the discriminant: `HotkeyManager.musicalKeyMap` is keyed on the macOS **virtual keyCode** (`HotkeyManager.swift:16-33`), i.e. layout-independent — which is what `code` means in the web keymap. K0a's `code` ruling matches the shipped host. Found measuring K0a, verified by the conductor at `GridPanel.tsx:2321` (`const key = e.key`). The 12 `DECLARED-LIVE` rows are implemented on `key` and declared on `code`; they agree on US-QWERTY and **diverge on QWERTZ** — the exact bite `keyForward.ts:104-108` records. So K1's "reconciliation, not addition" is a conversion rather than a lookup. gate: unit — a QWERTZ-shaped event resolves the same row in both surfaces |
| P7-K0a-b | spec | Three chord collisions are **allowlisted, not resolved** | todo | **⟦PORT⟧** the collision is INHERITED: `"⌥+"` vs `"⌥⇧+"` comes from the donor's own Help text (`Generated/ShortcutList.swift`), so the ambiguity predates the merge and the donor never resolved it either. compose `Z`, compose `V`, grid `⌥⇧Equal`. All six rows are parked so nothing is shadowed today, but **whichever un-parks first must pick a different chord for the other**. `⌥⇧Equal` is the interesting one: `"⌥+"` and `"⌥⇧+"` are the same keystroke, so one of those two rows has no chord of its own and the Help text has been ambiguous since the scoopyloops era. gate: the decision written into the row, or `KNOWN_COLLISIONS` shrinks |
| P7-K0a-c | delete | `KNOWN_COLLISIONS` is keyed by `chordId` now, and a stale display-keyed entry would silently never match | todo | **⟦NEW⟧** merge-local test hygiene. Guarded today by the staleness test (every allowlisted id must actually collide, or it goes red). Recorded so the next editor does not re-key it back to display strings. Low priority |
| P7-K1b | build | The dispatcher needs a **keyup lane**: `R`/`U`/`T`/`Y` are hold gestures whose RELEASE must fire | todo | **⟦PORT⟧** donor: `HotkeyManager` registers a `keyUpMonitor` beside `monitor` (`HotkeyManager.swift:37-38`), and its NK-2 comment is about exactly this lane — 18 key-down/key-up entries that a single edit had silently split, leaving keys that sound a note and never release it. `browserKeymap.ts:155` registers `keydown` only. `keyForward.ts:222-248` documents the exact hazard — the nudge that stayed bent off-grid. Fold into P7-K1 rather than shipping a dispatcher that can strand a hold. gate: vitest — a hold gesture releases |
| P7-K0b | spec | **What does `⌘S` save on the plane** — the focused strip's session, the `.scoopyMap`, or both? | todo — **user/conductor call** | **⟦PORT⟧** ⚠️ **half of this is already answered and the row does not know it.** `Generated/ShortcutList.swift`'s Session section is `⌘N New Session · ⌘S Save Session · ⇧⌘S Save Session As… · ⌘O Load Session…` — **in the shipping app `⌘S` saves the SESSION.** The `.scoopyMap` has no donor, so the only open question is whether the map gets a chord of its own, not what `⌘S` means. `saveSession` and `saveMapAs`/`attachAutosave` are both live, so the chord is ambiguous today and would ship ambiguous. Must be answered before P7-L1 surfaces session save. gate: the decision written into the row |
| P7-V1a | build | `deckTile.tsx:84-103` registers five per-deck handler slots with **no cleanup** — a second mount of the same deck silently kills the first's grid edits on unmount | todo — **P7-V1 depends on it** | **⟦PORT⟧** ⚠️ **the donor hit this exact defect and its fix is written down**: `ScoopyLoopsApp.swift:353` records that the `if djModeEnabled` swap *"tore down and cold-rebuilt"* the other face, and the shipped answer is to keep BOTH mounted and toggle `opacity`/`allowsHitTesting`/`disabled`/`accessibilityHidden` (`:375-396`). That is a guarantee-by-construction for V1, not a cleanup handler. The header at `:10-11` claims the defect "cannot exist here": true for DIFFERENT decks, false for two mounts of ONE deck, which is exactly what a full-viewport overlay creates. Either fix the cleanup or have V1 guarantee single-mount by construction. gate: vitest — unmounting a duplicate deck tile leaves the original's handlers live |
| P7-K4 | build | **The shortcut list still has no door** — `KEYMAP`'s only consumer is its own test | todo | **⟦PORT⟧** ⚠️ **this is an 86-line donor feature the row proposes to redesign.** `ShortcutListView.swift` (55 L) renders `Generated/ShortcutList.swift` (160 L, 20 sections) in `ShortcutsWindowController.swift` (31 L) as **Help ▸ Keyboard Shortcuts…**. Read those three before designing a Help overlay. Named inside P7-K1 and hoisted so it survives K1 being scoped down. Cheapest honest door: a Help overlay on the plane bar, parked rows dimmed with their `parked` reason as the title — which also makes the 57 parked rows visible rather than merely recorded. gate: real-host — a person can read the shortcut list without leaving the app |
| P7-K5 | delete | `GridPanel.tsx`'s six `return "forward"` sites hand keys to a relay unmounted since P8-8; `:2640`'s comment still promises a shortcut library that is gone | todo | **⟦NEW⟧** merge-local: the relay these sites forward to is wizard-era and already unmounted. No behaviour change — the keys are already dropped. It is the comment P7-K1 will read and believe. Land with P7-K0a |
| P7-K6 | delete | `keymap.ts:16-18` documents `Generated/ShortcutList.swift` (absent from the tree) and `npm run protocol:check` (nonexistent); `:20-22` names P8-8 as the future dispatcher | todo | **⟦PORT⟧** ⚠️ **the row's premise needs one word changed: those claims are false HERE and TRUE THERE.** `Generated/ShortcutList.swift` exists in the donor and its header names `web/scripts/generate-protocol.ts` + `npm run protocol:generate` + `protocol:check` as the live mechanism. So the comment is a donor-accurate comment orphaned by the merge — the decision is *delete or REBUILD the generator*, not *delete a lie*. The first thing a K-series session reads, and three of its claims are false — this is where P7-K1's THREE FALSE PREMISES came from. Land with P7-K0a |
| P7-K7 | spec | `scenesStore.ts` is a **dead module that looks live**: every sender rides `patternScene`, answered by no host; its consumers are retired `≡` panels | todo | **⟦PORT⟧** ⚠️ **not a dead module — an ORPHANED one.** `patternScene` is answered in the donor at `WebSceneBinding.swift:119` (`WebEngineLink.swift:547`). The module is live there and silent here because its Swift binding did not come across. Reframes the row from *mark it dead* to *port its handler or reroute*. Route it to `useCompanion.selectScene` or mark the module dead at the top. As-is it is a working-looking API that ships silence, and its comment advertises hotkeys 9/0 that a K-series builder will wire up first. gate: the decision written into the row, or the senders rerouted with a vitest pin |
| P7-K8 | build | `MAX_DECKS = 3` is declared twice — `companionEngine.ts:170` and `stripOps.ts:22` | todo | **⟦NEW⟧** merge-local duplication. Low priority, but divergence would be silent AND would mis-size the digit allocation K0 just ruled. gate: one declaration, imported by both |
| P7-L1 | build | ComposeWindow gains session open / new / save UI (surface `sessionStore`'s existing verbs; reuse the plane's `library ▾` popover shape) — and works with NO plane or map alive | todo | **⟦PORT⟧** donor: `PersistenceService.swift` (570 L) · `SessionZip.swift` (169 L) · `RecentSessionsManager.swift` (120 L); the verbs are `menuSession` → `WebEngineLink.swift:539`. gate: walk matrix (`npm run walks`, webkit+chromium since H4) — new → edit → save → reopen inside a lone compose window |
| P7-L2 | build | Launch chooser in the shell: boot prompts PLANE / COMPOSE; the compose path opens ComposeWindow on deck 0 and creates NO map (D-SL-LAUNCH-01). Today `MergedMain.cpp:334` is an unconditional `openPanel("plane", "", isMain)` | todo | **⟦NEW⟧** ⚠️ **and the donor answered the same want differently — worth putting to the user.** It has ONE `WindowGroup` (`ScoopyLoopsApp.swift:180`) and RESTORES the last face from session metadata (`AutoSaveManager.isDJMode`, read at `ScoopyLoopsApp.swift:521`) rather than asking at boot. "Remember which face" is a shipped alternative to a chooser. gate: ctest green; both doors reachable at boot — real-host proof rides P7-G1 |
| P7-L3 | fixture | Boot-path walk: chooser → compose (mapless session round-trip) and chooser → plane (today's boot, unchanged) | todo | **⟦NEW⟧** follows P7-L2. gate: walk green on the built bundle |
| P7-N1 | build | Strip focus model — **mostly ALREADY BUILT; this row is now the keyboard half only** | todo | **⟦PORT⟧** donor: `MasterRowFocusIndices.swift`, plus `resyncKeyboardTarget()` (`ScoopyLoopsApp.swift:427`) — which "guards on `djModeEnabled` itself, reading committed state". That IS the keyboard-claim RELEASE this row says is missing. **⚠️ PREMISE ~⅔ FALSE (2026-07-30 audit).** A single-focused-strip store and a visible ring already exist: `mapStore.selectedKey` plus the `.plane-strip.selected` accent outline. Building a second focus concept would put a THIRD accent outline on the same element and split selection in two. **What is actually missing is the keyboard half — and the audit found a LIVE DEFECT the row never recorded: clicking into an expanded deck tile currently kills Space and 1–8 across the whole plane window**, because GridPanel claims them by default and nothing releases the claim. Rewrite this row as: fold `deckTile.tsx::claimKeyboard`'s module state into `selectedKey`, and make the claim RELEASE. gate: walk — the existing ring follows clicks; arrows still land in the expanded tile; **Space and 1–8 still work on the plane after clicking into a tile and back out** |
| P7-N2 | build | Focus traversal: Tab/⇧Tab cycles strips in stable geometry order; digit scheme per K0's ruling | todo | **⟦PORT⟧** ⚠️ **and it exposes a COLLISION in P7-K0's provisional digit/Tab ruling.** In the donor **Tab and ⇧Tab are already taken**: ⇧Tab switches Compose↔DJ view (`HotkeyManager.swift:398`) and Tab is deliberately swallowed in DJ mode (`:405`). The donor's deck-cycle chord is bare **`-`** (`:430`). K0's "Tab/⇧Tab = cycle" is provisional (D-8) and needs re-ruling against this. gate: walk — the ring visits every strip and wraps |
| P7-K1 | build | **The JS dispatch layer** (the keymap's own deferred "TS dispatcher"): plane-window keydown → keymap lookup by context → command. Retires the `forwardKey`/`useNativeKeyForwarding` lane; registry-owned entries (⌘Z etc.) keep their path. ~~Keep `Generated/ShortcutList.swift` honest or retire it in the same commit — `protocol:check` gates its drift~~ | todo | **⟦PORT⟧** ⚠️ **the whole row is a 3026-line shipped implementation**: `HotkeyManager.swift`. The lane it proposes to retire is the donor's `forwardKey` → `WebEngineLink.swift:555` → `handleForwardKeyCommand` `:576`, which the merged shell never grew. Read `HotkeyManager` before designing dispatch. **⚠️ THREE FALSE PREMISES in that last clause (2026-07-30 audit) — do not act on it:** `Generated/ShortcutList.swift` is not in this tree, `protocol:check` does not exist, and the lane K1 proposes to retire (`forwardKey`/`useNativeKeyForwarding`) is **already unmounted** in WizardMerged — the live keyboard dispatcher is elsewhere. **Also blocking as scoped:** the dispatcher "keymap lookup by context → command" cannot be built against `KEYMAP` as it exists, because `ShortcutEntry.keys` is a human-readable DISPLAY string, not a matchable chord. Producing a matchable form is P7-K0's real first job. Two more the row omits: `isReservedShortcut` will refuse chords in the merged JUCE host that are only reserved in a real browser (the merged host is a `BrowserLink`), and after the whole K-series the shortcut list still has **no door** — `KEYMAP`'s only consumer in the tree is its own test. gate: web green (typecheck + test + the eight drift gates); the collision test extends per-context |
| P7-K2 | build | Focused-strip semantics: deck-A/B pair entries (Q/W/E vs A/S/D, `-` switch deck, ⌘⇧1/2/3 send-to-deck) collapse to one chord set targeting the focused strip — a keymap edit plus a dispatcher `strip` context | todo | **⟦PORT⟧** the ENTRY LIST is the donor's (`Generated/ShortcutList.swift`, "DJ Mode" section); the COLLAPSE onto a focused strip is the merge's own change, because the donor has fixed decks A/B/C and no strip model. Port the list, design the collapse. gate: unit — every former A/B pair resolves against focus; walk — Q/W/E drive the focused strip |
| P7-K3 | fixture | Coverage gate: every keymap entry either dispatches to a live handler or is explicitly parked. No silent dead chords | todo | **⟦PORT⟧** shape: the donor gated exactly this with `protocol:check` against `Generated/ShortcutList.swift` — a staleness gate on the generated render. The merged form (every entry dispatches or is explicitly parked) is stronger and is the merge's own. the fixture IS the gate; red on any orphan entry |
| P7-T1 | build | Classic **toolbar row** in the expanded tile: OPEN (library popover addressed to this strip) · ■ · ▶ · » (companion transport + skipStep) · DBL (a new companion pattern-double verb). D4-2's header verbs (REV · NUDGE · SAVE · ⏏) migrate DOWN into this row — one control, one home | todo | **⟦PORT⟧** donor: `GlobalToolbarView.swift` (561 L) is the classic toolbar; verbs via `menuTransport` → `WebEngineLink.swift:537`. gate: walk — the row renders and every verb round-trips; ctest untouched-green |
| P7-T2 | build | **SYNC/pulse + TR · WIN · BR row**: the beat-repeat variants beyond the fused scale (per-deck playhead has existed since D4-3); SYNC ~~promoted out of~~ **also rendered in the tile, staying on** the grid row | todo | **⟦PORT⟧** donor: the DECK block's row 2 (`TransportPanel.tsx:25`, already cited by P11's SCOPE LAW), driven by `deckSection` → `WebToolbarBinding.swift:390` (`WebEngineLink.swift:463`). **AMENDED by D-SL-FACES-01 (SIGNED 2026-07-30): SYNC does NOT move.** The tile's row is the expanded-face instance of the same store state — `Strip.tsx:903-921`/:1136 render these unguarded by `expanded` today while :965 gates deliberately when it means to, so "both faces, one state" is the built convention. "One control, one home" governs verbs with side effects (D4-2's SAVE/⏏), NOT performance surfaces. gate: an audible fixture for one TR/WIN variant (the D4-3 shape) + walk, **plus: the collapsed face still plays** |
| P7-T3 | build | **Scene row parity**: pads 1–8 (P3-U8's pads, ~~rehomed~~ **mirrored**) + S · R · CU · SCN · MUTE + GRID/PERF toggles (SCN is the pin latch) | todo | **⟦PORT⟧** donor: `djSetting` → `WebEngineLink.swift:465`. ⚠️ **The row's "find the real fields first" is answered twice over** — P7-K0 located `gridHidden`/`performOn` on the `dj` UiState, and the donor persists the GRID toggle per deck as `djMode.deckGridHidden(deck)` (`DJModeView.swift:195`), confirming it is a per-deck UI pref and not session state. **AMENDED by D-SL-FACES-01: the pads do NOT leave the collapsed strip** — launching a scene from a collapsed strip is the plane's primary performance gesture, and at 692×612 per expanded tile only one or two decks stay visible, so the collapsed face has to stay playable. ⚠️ **Stale citation, verify before building:** the row cited `GridMetaState`'s `gridHidden`/`performOn`; **neither field exists under those names**, and the one that does exist is hardcoded false on every push. Find the real fields first. gate: walk — each toggle flips real state; vitest pins the SCN latch routing; **the GRID/PERF toggle is walked at FULL VIEWPORT too, not only in the tile** — `.dj-deck-grid.cells-hidden` already exists and its comment says hiding the cells lets the strip stack "sit at its natural height … the point of the toggle is to GIVE the height back", which is only fully useful once P7-V1 has given the strip real width |
| P7-T4 | build | **Master row completes**: BPM/VOL/DRV are real since D4-1a; S1–S4 master sends become real (`masterSends` meta was `[]`; the lanes exist since P6-3) | todo | **⟦PORT⟧** ⚠️ **the donor SETTLES this row's blocking question — do not re-derive it.** `GlobalToolbarView.swift:242-290`'s mixer console holds the deck-master send levels and its own comment says they are **console routing, persisted**, explicitly unlike the transient mute/solo beside them — i.e. a scope distinct from a strip channel's own faders. Pinned by `ScoopyLoopsTests/DeckSendPersistenceTests.swift`. Read both before writing UI; the row's "decide which one owns the value" already has a shipped answer. **⚠️ PREMISE FALSIFIED by the 2026-07-30 audit — re-measure before building.** "Only the meta array is missing" is false: a grid deck's four master sends are ALREADY driven from the strip channel's own send faders, so building a second control writing the same lanes gives the deck TWO send sources that fight. Decide which one owns the value BEFORE writing UI. Depends on **P3.5-E4** (hoisted) — a grid strip's channel contributes no element until E4 lands. Also: this row creates a FOURTH plane-surface session-parameter write and lands a phase BEFORE P8-4, which is the row that decides where such writes persist — so either name its persistence target here or accept a rewrite at P8-4. gate: audible fixture — master send up → return energy (extends plane_audio_test). NOT provable in Chromium: `returnFx` is false in the browser host |
| P7-T5 | fixture | Tile walk: the expanded tile shows all four classic rows, compared against the scoopy deck view reference | todo | **⟦PORT⟧** the comparison reference is `DJModeView.swift` (2674 L) — the row's own "scoopy deck view reference". gate: walk matrix green (webkit+chromium since H4); real-host confirmation rides P7-G1 — or `MergedWalk` for the reachability half |
| P7-V1 | build | **Full-viewport mode**: the focused strip fills the viewport on command (mechanism per K0's ruling — a VIEW state, NEVER a document cell edit), one key drops back to the map | todo — **PURPOSE RESTATED 2026-07-30 (user), and it changes the layout** | **⟦PORT⟧** shape: the donor's answer to "the deck needs room" is a full-WINDOW face swap (`djModeEnabled`, `ScoopyLoopsApp.swift:375-396`), not a per-strip viewport — so the merged feature is new, but the MOUNT DISCIPLINE (both trees mounted, opacity-toggled, never torn down) is shipped and is what P7-V1a needs. **This mode is not about seeing more STEPS — it is about the TRACK STRIP reaching a usable size.** The tile already mounts the REAL `GridPanel` (P3-D4-1) and `djmode.css:72-83` is explicit that DJ density renders *the same* TrackBand controls compose does — "sends 3–4, mod slots, free-rate … **Scale here, never fork the component**" — so every track carries all five rows: `1 play·STEP·RATE·locator · 2 name·trim bar(S/waveform/E)·M·S·IN · 3 gain·pitch·filter·pan·volume · 4 modifier sliders · 5 send sliders`. Five rows at `0.85em` inside 692×612 IS the "hard to control its UI" the first real-host walk reported. **So the extra width goes to the STRIP, not to more cells** — full-viewport adds no controls, it gives the existing ones their room back. **Therefore P3.5-E1 (tile ergonomics) rides WITH this row, not later**: they are the same problem measured from two ends. gate: walk — enter/leave; the saved map's plane geometry is untouched afterwards; **and the five track rows are operable at full size** |
| P7-V2 | build | Tab cycles strips AT full size (reuses N2's ring); the compose door stays reachable from the full-viewport face | todo | **⟦UNKNOWN⟧** the donor has no strip ring to cycle; whether a Tab ride has a counterpart was not cheap to establish. gate: walk — a Tab ride through 3 strips at full size, compose opens from there |
| P7-V3 | fixture | Viewport walk including looper strips at full size (channel face — no grid) and keyboard reachability of every verb | todo | **⟦NEW⟧** confirmed: the donor has no looper at all (its "tape" is a DJ reverse-hold), so a looper's full-size face has no reference. ⚠️ **A looper strip has no expanded face at all** — `expanded` is gated on `isGrid` (`Strip.tsx:239`), so there is no 692×612 design for it either, and STRIP-DECK's whole spec is one sentence ("they simply show the channel face, no grid"). A 340×196 channel face stretched to a 1400px viewport is mostly emptiness. **Proposal (mockup Plate 3):** give the width to `TapeWave` — P3-U3's scrub and loop-brace gestures gain real precision from it — with the take ladder, meter, routing chips and the Inspector's DRV in a fixed right rail. No new transport verbs: P7-P1 is user-deferred. gate: walk green |
| P7-MIX-0 | spec | The strip-integrated mixer (deck volume + FX send channels with plugin load) designed on proven plumbing — the 2026-07-29 earmark, unchanged | blocked(P6-AUDIT not signed) | **⟦PORT⟧** donor: `GlobalToolbarView.swift:242-290` (mixer console) + `FxSlotWindowController.swift` + `openFxSlotWindow` (`WebEngineLink.swift:509`); `DeckMixerPanel.tsx` is already byte-identical in this tree. its own rows materialize when P6 closes |
| P7-P1 | build | Looper transport parity: deck-like transport on tape strips — the user's own example was "rate slider with fixed values" | blocked(user-deferred 2026-07-29 — "later on") | **⟦NEW⟧** no looper in the donor. opens on the user's call; scope sketch rides the STRIP-MODEL amendment |
| P7-AUDIT | spec | Diff this section against NAV-SHORTCUTS.md + STRIP-DECK.md; materialize every gap as a row; then offer the gate | todo | audit before gate, per §11 |
| P7-G1 | gate | **Human, real host:** the launch chooser's both doors · the expanded tile IS the classic deck with live verbs · a full-viewport Tab ride · the shortcut list worked by hand | awaiting-user (after P7-AUDIT) | the four rules, on the user's machine |

# P8 — the map holds the performance (opened 2026-07-29, D-SL-MAPPERF-01 signed)

> The user: "a map should basically hold all settings saved, but be flexible on devices
> and hardware routings of course. anything that is worth storing so it can be restored
> easily for a performance." Sync information ALREADY persists in full (per element:
> `bpm`, `syncToMaster`, `tempoMode`, `pulseRelation`, `transpose`, `rate`, `loop`,
> `takeRef`, plus `transport.masterBpm`) — this phase adds the layer that does not.
>
> The overlay is also the answer to MAP-SCHEMA.md's two ⚠️ hazards, which is why they
> are marked settled there: **plane-surface session-parameter edits land in the
> OVERLAY, compose edits land in the SESSION**, and the overlay is re-applied after any
> republish. P3-D4-1a crossed the "settle this first" line when MasterRow's BPM/VOL/DRV
> became real document writes; D-SL-MAPPERF-01 legalizes that retroactively and P8-4
> moves where the value lands (the control does not change).
>
> The fx-slot half of "the map remembers" stays in P6 as **P6-5** — P8-6's gate needs it.
>
> **Loop order:** P8-1 → P8-2 → P8-3 → P8-4 → P8-5 → P8-6 → P8-AUDIT → P8-G1.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P8-0 | build | **A map open LOADS its strips' sessions** (D-SL-MAPOPEN-01 SIGNED 2026-07-30) — the blocking hole the plan audit found under ALL of P8 | todo | **⟦PORT⟧** want, not mechanism: `DJSessionPreloader.swift` preloads deck sessions from a FOLDER and keeps them in memory "for instant deck switching", with `loadingProgress` and `errorMessage`. ⚠️ **It loads in the BACKGROUND with progress; P8-0 specifies a BLOCKING sequential load.** The donor's shape is worth reading before committing to blocking. **Opening a map never opens a single session.** `mapFiles.ts:70-88` is slMap/open → loadMap → setMap → applyMap and stops; boot is session-free (`PlanePanel.tsx:374`); the only production `useCompanion.open()` callers are the manual strip menu (`PlanePanel.tsx:769`), the compose return (:146) and `ComposeWindow.tsx:43`. So reopen a saved map today and every grid deck is EMPTY — no world, no scene, no MasterRow values, no DRV — and the symptom already renders at `Inspector.tsx:424-432` ("session not loaded"). P8-2/3/4/6 all sat on a load path that restored the strip but never its content. Build: resolve each grid strip's `sessionId` via `useCompanion.open(sessionId, deck)` and WAIT, before any overlay op; an unresolvable session becomes an UNRESOLVED STRIP preserving reference AND overlay (the shape MAP-SCHEMA.md:176-178 already promises), never a silently empty one. Blocking, not progressive — P8-3's re-apply needs deterministic order. Gate: fixture — save a map with a loaded grid strip, reopen, the deck has its world; a missing session yields an unresolved strip that still carries its overlay |
| P8-1 | spec | MAP-SCHEMA amendment made concrete: the overlay field shape (generalizing `sessionPerf`/`perfBySession`), the write-routing law, the re-apply triggers, and the migration from today's four-field shape | done (2026-07-30) | `MAP-SCHEMA.md` amended in place (+~230 lines); both ⚠️ hazards now read RESOLVED-BY-DECISION. ⚠️ **THE FINDING: three of the overlay's four existing fields are backed by NOTHING** — `switchMode`'s only setter rides the dead `patternScene` verb, `queuedScenes` has no analogue (the store holds ONE `scheduledScene`), and neither has a real consumer; their return is parked as **P8-1b**. **§1:** five fields on `strip.overlay` — `scene · stoppedTracks · masterVolume · masterDrive · masterDriveCurve`, `null` meaning "this map has no opinion". `soloedTracks` REFUSED. **No `bpm`** — `setTempoOverride` is the working precedent the other three must copy. **§2 write-routing law: parameters overlay, contents do not** — the OBJECT of the edit discriminates, not the surface. ⚠️ **FIVE doors write a session parameter at HEAD, not three — P8-1a.** **§3:** four re-apply triggers, one entry point; **T5 (compose) REFUSED by ruling.** ⚠️ **Re-applying through document writes would fix the stomp BY CAUSING THE BLEED — the overlay needs its own non-`EngineOp` path (P8-2a).** **§4:** v8→v9, new fields to `null`; `MAP_SCHEMA_VERSION` only, **no protocol bump**. ⚠️ Its own hazard citation is off — **P8-AUDIT-a**. |
| P8-1a | build | **Inspector's grid-strip DRV is a FIFTH session-parameter door, and it is not in P8-4** | todo — **land WITH P8-4, not after** | **⟦NEW⟧** the overlay is a merge concept. `Inspector.tsx:417,421` call `setMasterDriveCurve`/`setMasterDrive` → session document + autosave, the same bleed P8-4 reroutes for MasterRow — and `Inspector.tsx:409-412` says "one value, two doors" in its own comment. Reroute BOTH or the bleed survives at one door and reads as an intermittent bug. gate: vitest — neither door writes the session |
| P8-2a | build | **The overlay's apply path leaves `EngineOp`** | todo | **⟦NEW⟧** same. Sharpens P8-2(b). `sceneSelect`/`sceneSetSwitch` (`mapApply.ts:47-54`) are not engine calls — `EngineOp` is "one engine call, named for the ABI entry point it becomes" (`:23`) and these have none, which `mapStore.ts:265-272` proves by dropping them. Delete both from the union; give the overlay its own `applyOverlay(deck, overlay)` called AFTER `useCompanion.open()` resolves. **Two tiers in one ordered op list is what forced one ordering onto two clocks** and let a "tested" read path issue nothing. MAP-SCHEMA §3. gate: vitest — the op union no longer carries a non-engine verb |
| P8-2b | build | **Override lanes for `masterVolume` / `masterDrive` / `masterDriveCurve`** | todo | **⟦NEW⟧** same. `publish()` re-seeds the clipper block and masterVolume from `d.session.pattern` on EVERY call (`companionEngine.ts:365-420`), so an overlay applied by writing the session is stomped by the next republish AND re-opens the bleed. `tempoOverrideBpm` + `resolveWorldBpm` (`:348-353`) is the shape to copy; `stoppedTracks` needs `seedStopped` (`:424`) to consult the overlay before the file. **This is the mechanism BOTH hazards share.** gate: fixture — a republish does not lose the overlay |
| P8-3a | build | **T3 — a strip SWAPPING sessions must re-apply the overlay** | todo | **⟦NEW⟧** same. `PlanePanel.tsx:786` awaits `useCompanion.open(sessionId, deck)` and never re-applies. **This is the entire justification for keying by (strip, sessionId)** — swap away and back and the tweaks must return — and no P8 row listed it. Same `applyOverlay` entry point as T1/T2. gate: fixture — swap away, swap back, the tweaks return |
| P8-3b | build | **T2 must chain off `open()`'s promise, not the 500 ms timer** | todo | **⟦NEW⟧** same. `PlanePanel.tsx:146-149` fires `setTimeout(() => open(session, deck), 500)` fire-and-forget. A re-apply hung off the timer lands while the read is still in flight and overlays the OLD world. D-SL-MAPOPEN-01 already noted P8-3 "gets a completion hook to hang off"; this is that hook, written as a row. gate: fixture — no timer in the re-apply path |
| P8-4a | spec | `setEnabledSceneCount` from the plane writes the library session — **CONFIRM, do not reroute** | todo | **⟦NEW⟧** same. `Plane.tsx:611-616` → `companionEngine.ts:851-881` → session + autosave. **RULED (MAP-SCHEMA §2): it STAYS a session write, because a map cannot own a scene that does not exist.** This row is a comment plus a pin asserting it, so a later audit does not "fix" it into the overlay. gate: the pin exists |
| P8-1b | spec | `switchMode` / `queuedScenes` / `queueLoop` return **when something answers them** | todo | **⟦PORT⟧** the three parked fields are donor-live: `patternScene` → `WebSceneBinding.swift:119`. Their return is gated on a handler, and the handler exists in the donor — same reframing as P7-K7. Parked by P8-1 §5 and dropped at the v8→v9 migration. `switchMode` needs the K-series' `scenesStore` reconciliation (its only setter rides the dead `patternScene` verb; its only state rides a `scenes/<deck>` topic no merged host publishes — P7-K7). A queue needs a queue: the store has ONE `scheduledScene`, not a list. **Re-add to the overlay in the same commit as the control, never before.** gate: the control exists first |
| P7-T4-a | spec | **P7-T4's persistence target is RULED: `strip.sends[4]`** | todo | **⟦PORT⟧** donor: `ScoopyLoopsTests/DeckSendPersistenceTests.swift` pins deck-send persistence; `GlobalToolbarView.swift:265-267` states the scope. The ruling matches the donor. MAP-SCHEMA §6. Decision 5 (2026-07-25): the channel owns the send's LEVEL, so it is strip scope, not session scope, and the overlay holds session-scope values only. The field is already schema'd (`mapDocument.ts:176-178`) and already applied (`mapApply.ts:203-205`), so **T4 adds NO persistence code and P8-4 will not rewrite it** — which answers the "name its persistence target here or accept a rewrite at P8-4" warning on T4. ⚠️ `patternFile.ts:691`'s `deckMasterSendRow` has zero consumers in `web/src`: persisting there writes what nothing reads. T4's open question is its one-owner ruling, not its target |
| P8-AUDIT-a | spec | The P8-1 row's hazard citation is off by a paragraph | todo | **⟦NEW⟧** doc hygiene. It points at `MAP-SCHEMA.md:176-178`, which is the unresolved-strip paragraph (P8-0's citation, correct there). The two ⚠️ hazards were at `:149` and `:182` pre-amendment. Fold into P8-AUDIT |
| P8-2 | build | Overlay store + re-apply at map load (extends `planApply`; `MAP_SCHEMA_VERSION` bump + a named migration, per the strict-Zod discipline) | **todo — REWRITTEN 2026-07-30, the old gate was already green with zero new code** | **⟦NEW⟧** the overlay is a merge concept. **The audit's most expensive finding: "extends `planApply`" described the ONE half that has been complete and tested since the first commit.** READ side done: `mapApply.ts:170-178` (`perfFor` → `sceneSelect` → `sceneSetSwitch`). CAPTURE side never existed: `rememberPerf` (`mapDocument.ts:545`) has NO production caller in the tree — every caller is a test (`mapDocument.test.ts:569,570,582,601`; `mapApply.test.ts:467,486`); `stripOps.ts:135` only initialises `sessionPerf: {}`. ISSUE side dropped on the floor: `mapStore.ts:265-272` `case "sceneSelect": case "sceneSetSwitch": … return;`. And the stated gate is ALREADY MET by `mapDocument.test.ts:577-594`, which round-trips a populated `sessionPerf` — so this row could have been marked done against a passing test while nothing on the plane could PRODUCE an overlay value and nothing could RESTORE one to a running deck. **Now three parts:** (a) CAPTURE — every plane gesture that changes scene/switch state calls `rememberPerf` (hook `Plane.tsx:611` and P7-T3's SCN row); (b) ISSUE — `mapStore.ts:265` drives `useCompanion.selectScene(scene, {deck, immediate})` instead of returning early (there is no ABI entry point and never will be — this is a companion verb); (c) the schema bump. **Gate rewritten: the round trip must START from a UI gesture, not from `rememberPerf` called by the test.** Needs P8-0 |
| P8-3 | build | Re-apply after a compose republish: P3-C2's republish / `handlePanelClosed` lane triggers an overlay re-apply for that deck — the stomp hazard closed in code | todo | **⟦NEW⟧** same. gate: fixture — republish with different values, the overlay's value wins after re-apply |
| P8-4 | build | Plane session-parameter writes land in the overlay (the MasterRow BPM/VOL/DRV reroute — same control, different persistence target; amends where D4-1a's writes settle) | todo | **⟦NEW⟧** same. gate: fixture — a tweak in map A is invisible in map B using the same session |
| P8-5 | build | `tapeLoadTake` becomes REAL: an ABI path to load a take/file into a tape (today a documented no-op at `mapStore.ts:220`, so a reopened map's tape audio never comes back). Sized like P3-2b-5 | todo | **⟦NEW⟧** no looper in the donor. gate: ctest — save a map with a looper take, reopen, hear it play |
| P8-6 | fixture | **The map round-trip GATE** (MAP-SCHEMA's "a gate, not a hope"): a grid strip with a scene + an overlay tweak, a looper with a take, and an fx plugin (needs P6-5) → save → reopen → everything back | todo | **⟦NEW⟧** `.scoopyMap` has no donor. one fixture, mirroring `sl_route_test`'s wipe-rebuild-compare shape |
| P8-P1 | build | MIDI mapping persistence | blocked(the merged shell publishes no `midiLearn` topic — `attachMidiLearn` subscribes to nothing, `midiHardware: false`) | **⟦PORT⟧** ⚠️ **orphaned, not unbuilt**: `midiLearn` → `WebMidiLearnBinding.swift:265` (`WebEngineLink.swift:553`), on `MIDILearnSystem.swift` + `MIDIManager.swift` (661 L). The row is blocked because that binding did not come across, not because the behaviour is undesigned. a recorded dependency, not a promise; opens when the shell grows the topic |
| P8-AUDIT | spec | Diff this section against the MAP-SCHEMA amendment; materialize gaps | todo | before the gate |
| P8-G1 | gate | **Human:** build a set, tweak levels/scenes on the plane, save the map, edit that session in compose, reopen — every tweak is back and NOTHING bled into the library session | awaiting-user | on the user's machine |

# P9 — broad routing (opened 2026-07-29; D-SL-RECFROM-01 · D-WZ-VDEV-02 signed)

> Opened by the user's question — "we need to map out the new routing matrix as well,
> how good is the backend for this already? … we should find intuitive ways to choose
> audio inputs for looper (via fx send from a deck e.g. or the virtual interface that
> would allow other app audio right in the session)". Governing design:
> `docs/merge/ROUTING-MATRIX.md`.
>
> **The backend answer, measured 2026-07-29: strong, and the gaps are UI.** `sl_route_*`
> carries 128 ramped routes; sources `channelOut` · `channelSend` · `deviceInput` ·
> `fxReturn`; destinations `channelIn` · `sendBus` · `main`; Kahn-ordered zero-latency
> chains with edit-time cycle refusal and consented one-block feedback edges; the whole
> graph enumerates and rebuilds to bit-identical audio. `slengine/tools/sl_route_test.cpp`
> proves all of it, including **send tap → another strip's input** — the exact path the
> user asked for, engine-proven, with NO UI able to author one (`patchDrag` is always
> built with `send: null`). So this phase is authoring surfaces, missing kinds, and the
> hygiene debt the measurement surfaced — not engine work.
>
> **Not queued here, recorded as designed-not-built:** the cue bus and per-deck outs
> (lanes the core already produces that `sl_route_*` cannot name), multiple hardware
> output pairs, and the per-hardware-output limiter (guard G1 — the watchdog is
> main-bus-only). They open on the user's call.
>
> **Loop order:** P9-1 → P9-2 → P9-3 → P9-4 → P9-5 → P9-AUDIT → P9-G1.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P9-1 | build | `record from ▸` grows every source kind (D-SL-RECFROM-01): other strips' **send taps** S1–S4 and **FX returns** (the record-the-wet path STRIP-MODEL names), alongside today's bus + device inputs. Reuses the existing consent flow (`wouldCycle`) and duplicate-cable guard | todo | **⟦PORT⟧** partial: the source kinds are engine-side and vendored; the record path is `capture` → `WebCaptureBinding.swift:85`. The `record from ▸` gesture itself is merge-side. gate: walk — a deck's send into a looper's record tap, heard back; fixture pins the `channelSend` route the gesture authors |
| P9-2 | build | The Matrix grows AUTHORING: the sources × destinations grid editor (ROUTING-MATRIX's second view — today `Matrix.tsx` is a read-only ledger with one ✕ per row), plus route-gain editing (`slRoute/setGain` is dispatch-live with zero web callers) and the feedback toggle | todo | **⟦PORT⟧** donor: `openAudioRoutingWindow` (`WebEngineLink.swift:494`) → `AudioRoutingWindowController.swift`, whose header names its contents as "pickers, input config, **`RoutingMatrix`**"; the write verbs are `setPerTrackOutputRouting` / `setDeckOutputChannels` / `setSendOutputChannel` → `WebAudioCommandBinding.swift:37`. gate: walk — add, re-gain and remove a cable from the grid; the ledger and the plane cables agree |
| P9-3a | build | **The duplicate-cable guard moves onto the COMMIT path** (lifted out of P9-3, done 2026-07-30) | done | The row said the guard lived in one gesture; **the audit found TWO unguarded commit paths** (`commitPatch` and E3's `patchDeckOut`), and **the engine SUMS duplicate routes — so a repeated shift-drag made a strip twice as loud with nothing on screen to explain it.** Fixed by `routeExists(routes, route)` where the add happens. ⚠️ **Identity is the ENDPOINTS — kind, index, sub, destination — deliberately NOT gain or feedback**, or "re-patch quieter" would add a second summing route. |
| P9-3 | build | Routing hygiene, ~~all three~~ **the remaining two** measured 2026-07-29: the duplicate-cable guard moves into `patch()` proper (today only `onRecordFromStrip` checks, so a repeated shift-drag silently doubles gain); feedback edges are **created muted** (ROUTING-MATRIX's own standing precedent, not honoured — `commitPatch` sends gain 1); `feedbackMs()` reads the LIVE block size and sample rate instead of hardcoding 512/48000, so the price shown on every cable and in the consent menu is true | todo | **⟦NEW⟧** merge-local routing hygiene. ⚠️ **Item text is STALE: the duplicate-cable guard shipped as P9-3a** — the remaining scope is TWO items (feedback edges created muted; `feedbackMs()` off the live block size), not three. gate: vitest per fix + a walk for the muted-feedback path |
| P9-4 | build | Accumulated path latency (guard G4 from `pd-modular-routing.md`, half-built): a per-PATH total on the cable/strip, not just the per-cable `+10.7 ms`. Feeds P6-6's PDC surface — and has a live customer already, the tape-strip `hostSendFeed` seam that puts a tape's send one block behind a grid's into the same plugin | todo | **⟦NEW⟧** no donor — the donor applies no PDC (`AudioEngineFacade.swift:795-798`). gate: vitest on the accumulation math + the number visible in the walk |
| P9-5 | spec | The interim other-app-audio path documented (D-WZ-VDEV-02) | done (2026-07-30) | `docs/specs/other-app-audio.md` (315 lines) + a pointer from `ROUTING-MATRIX.md`; placed in `specs/` so P10's retirement row has ONE file to delete. **The premise HELD** — the path is authorable today with one gesture, and **nothing on the chain knows the device is virtual**, which is why this costs a document and not code. ⚠️ **The merged app PINS 48 kHz and `open()` refuses coercion, so a virtual device at another rate FAILS the switch.** ⚠️ **Two traps in the ladder:** a `deviceInput → channelIn` cable is multiplied by the MONITOR curve and strips are created `monitor: false`, so "I selected BlackHole and hear nothing" is the EXPECTED first state; and the meter is post-gate, so an unmonitored input reads exactly zero (**P9-5f**). |
| P9-5a | build | **The hint in the input picker** — P9-5's second half | done (machine) — **real-host walk pending (user)** | The `input device` section was gated on `devices.length > 1` and **the gate was the defect** — the person who needs to hear about BlackHole is exactly the one who has installed nothing. It now always renders when `onPickInput` exists, with **two sub-lines because "one device" has two causes** (the relaunch caveat, **P9-5e**; or "no input devices reported", true of both a bare host and the browser dev host). Built as an exported pure function: **the menu is assembled in an event handler and rendered through a portal, so extraction is the only way any pin can see it.** ⚠️ "The ⋯ menu shows the section" is walk-only. |
| P9-5a-a | delete | **`other-app-audio.md`'s line citations have drifted** (citation sweep, not a rewrite) | todo | **⟦NEW⟧** doc hygiene — no donor counterpart. **MERGED 2026-07-30 (Lane C audit): this row now carries P9-5c-c, which was the same sweep found twice.** `other-app-audio.md:93` cites `PlanePanel.tsx:378` for the once-only `refreshDevices`; it is at **`:458`** (found implementing P9-5a, verified by the conductor — the claim holds, the line does not). §2c carries the **same** stale citation (from **P9-5c-c**, found editing §8). Also fold in: **§5's step 2 describes the OLD gated behaviour and names P9-5a as the gap that closes it** — stale since P9-5a shipped. ⚠️ **The doc's whole discipline is that every claim cites the file it was read from, so drifting line numbers erode exactly what makes it trustworthy.** ⚠️ **P9-5e's note carries the same `:378` citation** — sweep it too. gate: a citation sweep against HEAD, not a rewrite |
| P9-5a-b | build | ~~The one-device / no-device lines are the natural home for the device store's `error`~~ | **CLOSED by P9-5c — decided with a mechanism, not a preference** | **CLOSED by P9-5c — decided with a mechanism, not a preference.** `ContextMenu` runs `onSelect()` then `onClose()` **synchronously** while the switch is an awaited round-trip, so **the menu that took the gesture is already gone when the host answers** — it could only report on its NEXT open. The note line took it instead. |
| P9-5a-c | build | The section renders in the browser dev host too, where there is no device layer at all | todo | **⟦NEW⟧** merge-local; the `hostKind()` seam it rides is P7-K0's ruling. Deliberate: the row says render whenever `onPickInput` exists, and `Plane.tsx:556` passes it unconditionally. The sub-line says "no input devices reported", so it is **not a lie** — but a host-scoped hint (cf. NAV-SHORTCUTS §2.5's `hostKind()`) would be honest rather than merely not-wrong. Rides whatever row lands `hostKind` |
| P9-5b | build | **`AudioIO::setDevices` must re-attach on EVERY failure path — a failed device switch silenced the app entirely** | done (2026-07-30) | `AudioIO.cpp:102` called `detach()` and **all four error returns exited without `attach()`**, so a failed switch left the app with **no render callback at all** — recoverable only by relaunch. Fixed by capturing the previous setup and restoring it on every failure; **the restore is *checked*, not assumed**, because attaching at the wrong rate is worse than staying silent. ⚠️ **Principle: a refusal must cost you the CHANGE, never the audio you already had.** ⚠️ **Gate is honest, not green-by-fixture** — no injection seam exists; that is now **P9-5h**. |
| P9-5c | build | **The device-switch error must reach a person** | done (machine) — **real-host walk pending; no Chromium walk CAN prove it** | **The measurement that mattered was WHY it slipped a surface that already exists:** `onRefusal` fires from `ask`'s catch, but a failed switch answers `{ok:false, error}` on a **successful dispatch** — the reply RESOLVES, so **the one failure shaped unlike all the others was the one with no door.** `useDeviceStore.error` becomes `lastSwitch` — **news rather than standing state** — subscribed one line beside `onRefusal`. Three decisions: host words go through **verbatim**; a failure names the device you **PICKED**, not `current`; **a new record per gesture, compared by identity**, because picking the same broken device twice is two pieces of news. ⚠️ **Pins cannot see the note line repaint — P3.5-E8g-f.** |
| P9-5c-a | build | The **"previous device did not come back"** consequence needs a structured flag, not prose matching | todo — schema bump, **Lane A / C++** | **⟦NEW⟧** merge-local schema shape. E9a's law is that the line ends in the CONSEQUENCE, and this one cannot: telling a person *"nothing will sound until you pick a device that works"* requires knowing the restore failed, and the only signal is the **tail of a C++ string** (`AudioIO.cpp:132`). Substring-matching host prose across the seam is the hand-mirror this codebase forbids, so P9-5c passes the reason verbatim and stops. Fix: `slDevices/setInput`'s reply gains `restored: boolean` — moves all three hosts `schema:check` compares. gate: vitest pin + the note line ends in the consequence |
| P9-5c-b | fixture | ~~The plane's note line has no mount-level pin at all~~ | **merged into P3.5-E8g-f 2026-07-30 (Lane C audit)** | Same finding as P3.5-E8g-f and P11-1-e. Its distinct consequence — **nothing in `web/src` renders `PlanePanel` in a test, so P3-U6, P3.5-E9a and P9-5c are all unpinned at the mount, and ONE jsdom pin would cover all three** — is carried verbatim as clause (b) there. Kept as a pointer so the id does not vanish from a search |
| P9-5c-c | delete | ~~`other-app-audio.md`'s line citations have drifted~~ | **merged into P9-5a-a 2026-07-30 (Lane C audit)** | The row's own note already said "same defect as P9-5a-a — fold them together". Done. Its §8/§2c finding is carried there. Kept as a pointer so the id does not vanish from a search |
| P9-5d | build | **`Backend::deviceError` must reach a person** | todo | **⟦PORT⟧** donor: `AudioDeviceManager.swift` (1661 L) owns device failure; the read verb is `getAudioInputStatus` → `WebAudioCommandBinding.swift:37` (`WebEngineLink.swift:433`) — declared in the merged schema and answered by nobody. `MergedApp.cpp:76` stores the device-open failure, `MergedApp.h:68` declares it, **nothing reads it** — so an interface that cannot run 48 kHz yields an app that launches, looks completely normal, and is silent, with the reason sitting in a string nobody prints. Publish it at boot the way P3-U1's start failure lands on the note line. ⚠️ shell → **Lane A**. gate: real host — boot with the device at 44.1 kHz and the app says so |
| P9-5e | build | **The input picker never re-scans** | todo | **⟦PORT⟧** ⚠️ **the donor's answer is stronger than this row's proposed fix.** `AudioDeviceManager.refreshDevices()` `:265` plus a PUSHED `device-list-changed` event off CoreAudio notifications (`WebAudioCommandBinding.swift:16`), and `refreshAudioDevices` is a first-class method (`WebEngineLink.swift:433`). The row proposes re-listing when the ⋯ menu opens; the shipped app never needed the menu opened. `refreshDevices` runs once, in the plane's boot effect (`PlanePanel.tsx:378`), and `slDevices` carries `list`/`setInput` only. The scoopy-era `enumerateAudioDevices` + `device-list-changed` path that `AudioPanel.tsx:34-52` polls is **unanswered by the merged host** (`sl_dispatch_test.cpp:151-158`). So a device installed or plugged in after launch is invisible until relaunch — which for this row's own workflow means "install BlackHole, then restart the app". Cheapest fix: re-`list` when the ⋯ menu opens, the way that menu already re-reads the session library. gate: real host — plug an interface in with the app running, it appears in ⋯ |
| P9-5f | build | **An input you are not monitoring is invisible** | todo | **⟦PORT⟧** ⚠️ **already solved and already on the wire in the donor**: `MicChannel.inputLevel` (`GlobalToolbarView.swift:398-434`, a pre-gate peak with its own decay) published as HotFrame `micInputLevel` (`WebToolbarBinding.swift:503`). The row asks for exactly this and calls it "wanted". The strip meter is taken off post-gate, post-level samples (`sl_channel.cpp:728-737`), and a device-input cable is multiplied by the monitor curve (`:584`) with strips created `monitor: false` (`stripOps.ts:123-133`). So a strip receiving a loud signal reads **exactly zero** until MON is pressed, and nothing anywhere shows signal-present at the input. Wanted: a pre-gate input-present indication, or a MON hint on a strip whose input is live. Cheap engine-side — the input buffers are already in hand at `sl_channel.cpp:539-543`. gate: signal at the input lights something with MON dark |
| P9-5g | build | **A `deviceInput` route does not remember WHICH device it meant** | todo | **⟦PORT⟧** ⚠️ **and the donor's answer is not the row's.** The row proposes storing the device NAME beside the index; the donor stores the CoreAudio **`deviceUID`** (`AudioDeviceManager.swift:305`, `:736`) — stable across renames and duplicate names, which a display name is not. Routes store `deviceInput` + a channel INDEX, never a device name (`mapApply.ts:71-77`). Reopen a map on another machine and the strip points at whatever input 0/1 is — usually a microphone — under a perfectly ordinary channel name; `channelLabel` can only warn when the index is out of RANGE (`devices.ts:101-113`). Same class as P6-5b's plugin portability and the same answer: store the name beside the index and warn on mismatch. ⚠️ MAP schema field — **collides with P11-4**, the only queued MAP bump; sequence them or fold in |
| P9-5h | fixture | **A device-rate refusal cannot be gated — `AudioIO` has no injection seam** | todo — **C++, Lane A** | **⟦NEW⟧** merge-local — no donor counterpart (the donor's `AudioDeviceManager` owns its own device layer). Written as a row because **P9-5b's handoff said in as many words that it is one**: *"If a fixture is wanted, it needs an injectable device interface first — that is a row, not a line."* `Backend::deviceManager` is a concrete `juce::AudioDeviceManager` member, so no headless ctest can drive the failure path P9-5b fixed, and the only proof today is P9-G1's walk or a device set to 44.1 kHz. ⚠️ **This is the one shipping-defect class in P9-5 with no gate at all** — P9-5b was a real silence-the-app bug found by reading, not by a red test. Also unblocks a fixture for **P9-5d**. gate: the four failure returns of `AudioIO::setDevices` each provable headlessly |
| P9-AUDIT | spec | Diff this section against ROUTING-MATRIX.md; materialize gaps; the cue bus, per-deck outs, multi-output and G1 stay recorded-not-queued unless the user calls them | todo | before the gate |
| P9-G1 | gate | **Human, real host:** a deck's send tap into a looper records the send · an FX return routed into a strip records the wet · another app's audio arrives through a third-party virtual device | awaiting-user | on the user's machine |

# P10 — the virtual interface (EARMARKED 2026-07-29, D-WZ-VDEV-02; no rows yet)

> Unparks D-WZ-VDEV-01's native **Wizard Out** device — 16 channels / 8 stereo pairs,
> app-agnostic and Loopback-style, so any running app's audio lands in the session
> without a per-app bridge. Opens after P9-AUDIT; P9-5 carries the interim path
> meanwhile, so nothing in P9 waits on this.
>
> What already exists: the capture ABI (`host/include/wz_capture.h` — `WZ_SRC_PROCESS`,
> `WZ_SRC_SYSTEM_MIX_EXCEPT`, `WZ_SRC_VIRTUAL_INPUT`, topology generation, timestamped
> delivery) and the full macOS design (ARCHITECTURE.md §"capture" — process-tap via
> `CATapDescription` → private aggregate device; the virtual device as a standalone
> AudioServerPlugIn loopback driver read back as a normal CoreAudio input; macOS 14.4
> floor; PipeWire null-sink on Linux). What does NOT: any real backend (only
> `host/src/CaptureFake.cpp`), and any `sl_route_*` `virtualDeviceInput` SOURCE KIND —
> the capture machinery belongs to the legacy `wz_engine` world and does not reach the
> current engine at all. MIGRATION.md's P2-05/06/07 rows are unparked by this decision
> and point here. Rows materialize when the phase opens.

# H — host hygiene (opened 2026-07-30, D-SL-ONEHOST-01 signed)

> **Not a phase — a cross-cutting cleanup**, so it does not queue behind P6/P7 and takes
> no gate of its own. It started as a user question — *"why are we using two different
> webviews, JUCE and WK?"* — whose literal answer is that we are not: `juce::WebBrowserComponent`
> **is** WKWebView on macOS and WebKitGTK on Linux, one engine named at two levels. But
> measuring it found two things that are real.
>
> **(a) Three files looked like "the app" and one was.** `shell/src/MergedMain.cpp` is the
> host. `shell/src/Main.cpp` had been ORPHANED since P3-0 step 2 deleted the `Wizard`
> target (2026-07-27) — 654 lines no target compiled that still read like a live shell.
> `spike/` was still built ON BY DEFAULT, serving *another repo's* bundle
> (`../scoopyloops/webdist`), six days after its own stated deletion condition was met.
> Behind them sat a donor tier alive only via its own tests.
>
> **(b) The walk gates run in the wrong engine, and the tree already knew.**
> Seven of eight `web/tools/browser_*.mjs` hardcode `chromium`; the app ships WebKit.
> `browser_prod_test.mjs:88-103` is a written account of a **WebKit-only** OPFS behaviour
> that "Chromium and Firefox happen to tolerate". Worse, the JuceLink half is invisible to
> *any* browser walk — `MergedMain.cpp` itself records an `slParam` listener that was
> empty (every param written was discarded) and `__slPanelArg` never injected (every
> addressed window opened unaddressed). Both shipped green. P7-T4 already says it plainly:
> *"NOT provable in Chromium: `returnFx` is false in the browser host."* The bill arrives
> as `awaiting-user` gates — P6-AUDIT, P7-G1, P8-G1, P9-G1 all end their phase blocked on
> a human. That is the right gate for a VISUAL claim and the wrong one for "the door is
> reachable at all".

| id | type | item | status | handoff note |
| --- | --- | --- | --- | --- |
| H1 | delete | The orphan shell and the P1 spike | done | Deleted `shell/src/Main.cpp` (654 L, orphaned since P3-0), `CommandDispatch.*` + `wz_command` + its test, and all of `spike/`. ⚠️ **A third ghost surfaced mid-row:** `shell/generated/WZProtocol.h`, marked GENERATED by a script that does not exist here, pinning `kSchemaVersion = 30` against a live host at 88 — deleted too. Nothing was ported. Incidental pre-existing fix: the root `compile_commands.json` was a dangling symlink, so **every C++ LSP diagnostic in this tree was noise**. ctest 67/67. |
| H2a | delete | The wizard engine tier — `engine/` reduced to the vendored resampler | done | `engine/` reduced to the vendored resampler; **23 tests left the tree and every one is accounted for (45 + 23 = 68).** ⚠️ **`engine/` was NOT deleted** — the `samplerate` target is defined inside it and `wz_decode` links it. ⚠️ **`decode_test`'s engine-play half has no surviving load path, so that claim is now P8-5's to make**, recorded in the file's header; the decode half got stronger in exchange. `wz_capture`/`CaptureFake` kept — P10 names them by file. ctest 45/45. |
| H2b | delete | The wizard-only host services (`SessionStore`, `PackageStore`) | done | `SessionStore` and `PackageStore` deleted — both reachable only from the shell H1 removed, **both with a named successor rather than a gap** (`FileSettingsStore`, which cites this file's atomic-write discipline; `.scoopyMap`, **a different document, not a renamed one**). ⚠️ **Checked the queue before deleting rather than after** — nothing queued loses a dependency. ctest 43/43. |
| H3 | build | Split `MergedMain.cpp` into `wz_merged_app` + a thin entry point | done | Bodies moved verbatim into `MergedApp.{h,cpp}`; `MergedMain.cpp` down to 33 lines. ⚠️ **The archive hazard was real and verified not to have fired** — `nm` on the built app shows zero stub markers; had `NativePluginHost.mm` lost the race, every plugin feature would have gone quiet with a green build. ⚠️ **CORRECTION — this row went in with `params:check` RED**: it ran ctest and the real-host walk but NOT the drift gates. **A refactor that relocates a gated table is a gate change, not just a code move.** ⚠️ **Do not treat a screenshot from this environment as evidence about the app** — `screencapture` here returns desktop-only. That is why H5 exists. ctest 43/43. |
| H4 | fixture | The walk matrix runs WebKit first | done | **14/14 — seven walks × webkit + chromium, one command: `npm run walks`**, engine table lifted verbatim into `web/tools/lib/engines.mjs`. ⚠️ **The engine is chosen by `SL_WALK_ENGINE`, not argv** — two walks already take `argv[2]` as the dist dir, and **a gate failing for a reason unrelated to what it tests is how a new gate gets switched off.** Two real findings: an ORDERING claim checked by naming one file (true only while one directory existed), and a 3 s audio-meter poll that flaked ~1 in 3 under a matrix run. `browser_prod_test` stays out — it runs its own three-engine sweep. |
| H5 | build | `MergedWalk` — a walk that runs in the REAL host | done | **6/6 in the real WKWebView**, closing H3's real-host gate a screenshot could not. Asserts what only a JUCE host can answer, including **HotFrames actually arriving in the page**; negative-tested, so it is not decoration. ⚠️ Two JUCE constraints designed around: `evaluateJavascript` cannot serialise a Promise back, and `pageFinishedLoading` can fire more than once (latched). ⚠️ **OPT-IN** (`-DWIZARD_TEST_REALHOST=ON`, label `realhost`) because it takes real windows and the audio device — **but it BUILDS unconditionally, since an opt-in gate that stops compiling is how a walk quietly rots.** ⚠️ **LIMITS: needs a window server (77 = SKIP); does NOT replace the visual pass; asserts what the page can OBSERVE, so a wired-but-invisible control still passes.** Its original plan note is archived. |
| H5-a | build | `schema:check` — the NINTH drift gate, and the four-way drift it found | done | **Three hosts reporting three different protocol versions** (`schema.ts` 96 · `getCapabilities` 92 · `kScoopySchemaVersion` 88), and `sl_dispatch_test` **asserted the stale 92**. ⚠️ **All three lines of defence failed structurally:** the mismatch banner renders only on a fallback panel every real panel returns before; every browser walk reports the UI's OWN version and **agrees with itself BY CONSTRUCTION**; and **the ctest pin DEFENDED the drift across four bumps.** `schema:check` now reads `schema.ts` as the authority, checks **every** occurrence rather than the first, and **fails rather than skips when a pattern stops matching**. **This is the eight drift gates becoming nine.** |

# P11 — the master bar (the top row, final form) — opened 2026-07-30

> Opened by the user, verbatim: *"the top row should include master sync tempo and
> actions, master meters and capture … what did original scoopy have that we are
> missing?"* — after judging the shipped bar *"old from wizard plane and includes
> many item we are not asking for in the final version."*
>
> **Governing decisions (all signed 2026-07-30):** D-SL-TOPROW-01 (three zones;
> `save`/`export`/`open` → `map ▾`; `≡ panels` and `compose` leave the bar) ·
> D-SL-XFADER-01 (crossfader returns with ASSIGNABLE sides) · D-SL-HEALTH-01
> (dropout count + CPU) · D-SL-CAPTURE-01 (one CAPTURE verb, no bounce).
> **Mockup:** Plate 5 of the 2026-07-30 mockup sheet.
>
> **The measurement behind the phase (2026-07-30):** the bar today is
> `map name · N strips · save · export · open · library ▾ · ≡ panels · compose ·
> spacer · Master` — four file/panel buttons BEFORE the performance cluster.
> `Master.tsx` is in better shape than expected and is KEPT: a real STEREO meter
> off `outputPeakL/R`, the limiter lamp, the fader, ▸ ↻ ◼, BR + fused scale, REV,
> and the honest ±1 BPM steppers. What does not exist anywhere in the tree:
> **CPU / xrun / dropout telemetry (nothing in `web/protocol`, no topic, no
> field)** · a **main-mix-to-disk recorder** (`mainMix` is only a looper strip's
> record TAP) · **TAP** · a **launch quantum** (though `deckQuantizePending` is in
> `ToolbarUiState`, so the engine concept exists) · **solo** of any kind, deck or
> send · an **INPUT channel** surface (scoopy's `mic` block: gain · monitor · mute
> · 4 sends). Recorded-not-queued from scoopy's contract: **bounce**
> (`activeIsBouncing` — needs an offline render path no row owns) and **per-deck
> output pair routing** (`deckOutputChannels`/`outputPairs`).
>
> **⚠️ THE SCOPE LAW for this phase (user, 2026-07-30) — read before touching any row:**
> **BR · TR · WIN · REV · SYNC are DECK controls. The master row FEEDS them and
> owns none of them.** Original scoopyloops is unambiguous: `TransportPanel.tsx:25`
> documents the DECK block as `row 2: SYNC‹pulse› · TR‹±n› · WIN · BR‹›`, and every
> one of those reads `section` (`DeckSectionState`) through deck-scoped ops; the
> master row carries the tempo REFERENCE and how synced decks follow it (:257, :378).
> A master control here is a FAN-OUT VERB with a DERIVED lamp — never a second
> source of truth. P11-0 is the live defect the old framing already produced, and
> the master's own `SYNC` is renamed **CLOCK** so the two scopes stop sharing a word.
> **These controls get their real work done in the STRIP** (P7-T2's `SYNC/pulse +
> TR · WIN · BR` row); P11 only feeds them.
>
> **Loop order:** P11-0 → P11-1 → P11-2 → P11-3 → P11-5 → P11-6 → P11-4 →
> P11-AUDIT → P11-G1. P11-0 first: it is a defect against shipped behaviour, and
> every other master control in this phase must follow the rule it establishes.
> Crossfader last of the builds: it is the only one needing a MAP schema bump.

| id | type | item | status | handoff note |
|---|---|---|---|---|
| P11-0 | build | **The master's BR/REV lamps LIE — it keeps a second truth beside the decks' own** (found 2026-07-30, user-flagged framing error; defect against shipped P3-M-1b) | done | `masterBrView(activeDecks, brOf, pendingIdx)` in `stripOps.ts` — the master's lamp, label and next-press are all DERIVED from the decks; **the only state it still owns is `brIdx`, the scale it will SEND, which is an intent rather than a claim about any deck.** REV derives the same way. **The master always completes the gesture the lamp is showing, whoever started it.** Decks a compose window owns are excluded. Two pins verified to FAIL against the old own-boolean master. |
| P11-1 | build | The three zones + the wizard-era retirements (D-SL-TOPROW-01) | **done (machine), SCOPED as ruled — real-host walk pending (user)** | **Zones: document · master · output, as a CSS GRID rather than flex-with-a-spacer** — the centre is `auto` between two equal `1fr` flanks, so **the transport holds the same screen position however long the map name is** (under the old spacer, typing in the name field slid it sideways). Zones `flex-wrap`, so a narrow window makes the bar TALLER and never hides a control. `map ▾` folds save/export/open + the five settings panels; **`library ▾` NOT folded in — it acts on SESSIONS.** **`compose` retired — the bar's version could only ever open the FIRST grid strip, so it was a less capable duplicate (this closes P11-1-b).** **`≡ panels` STAYS** (P11-1-a). **`HealthReadout` beside LIM in the output zone — this closes P11-5b**, pinned against RENDERED MARKUP because a source grep is satisfied by two things merely sharing a file. ⚠️ The output caption reads `out` — **P11-1-d**. ⚠️ Review `Master.tsx` with `git diff -w`: 124 changed lines are pure re-indent. |
| P11-1-c | delete | **`≡ panels`' last two blockers, tracked as one row** | **retired-by-decision 2026-07-30 (user, P11-1-a): the button stays** | **Retired by decision (P11-1-a): the button stays.** P7-MIX-0 may still rehome FX 1–4 — that does not retire the button, which carries the five surfaces. The comment on the button should be corrected from "preconditions for deletion" to "why this stays" when something next touches it. |
| P11-1-d | spec | **Is `out` the right caption — and does the master FADER belong in the centre?** | todo — fold into P11-AUDIT | **⟦PORT⟧** the donor master bar is `GlobalToolbarView.swift` (561 L) — read what it captions and where its fader sits before ruling. P11-1 renamed the output caption because the re-zoning made a bare `master` ambiguous. The deeper question the split surfaced and did not answer: the master LEVEL fader is arguably a performance control, and it now sits in the before-or-after zone. D-SL-TOPROW-01 lists the centre as transport · tempo · clock · quantum · BR · REV — no fader — so P11-1 followed the decision. Worth one deliberate look before P11-G1 |
| P11-1-e | fixture | ~~The three-zone layout has no width coverage~~ | **merged into P3.5-E8g-f 2026-07-30 (Lane C audit)** | Same finding as P3.5-E8g-f and P9-5c-b — the suite has no renderer and no layout engine, so this is one row, not three. Its distinct consequence (the `flex-wrap`/`min-width: 0` pair is one deletion from silently clipping the document zone) is carried verbatim as clause (c) there. Kept as a pointer so the id does not vanish from a search |
| P11-2 | build | Master **CLOCK** source (INT / EXT) + TAP | **done (machine) — real-host walk pending (user)** | **CLOCK, never SYNC — the headline, not a naming detail:** a master control meaning "follow" would be a second source of truth about the decks, the exact lie P11-0 unpicked; the pin asserts the master markup contains no `SYNC`. TAP is a pure fold over timestamps — a four-tap window, with **outlier rejection load-bearing** (one doubled interval at 120 BPM pulls a plain mean to 90; verified by falsification), and a series outside the box's range **REFUSED, not clamped**. ⚠️ **EXT is dead at THREE independent layers** (`midiHardware:false`; no clock method implemented at all though `getMidiClockStatus` is fully specified; absent from `MergedLink.NATIVE_METHODS`) — **P11-2-b**. ⚠️ **THE TRANSFERABLE PART: the gate is deliberately NOT `caps.midiHardware`. Capabilities default to FULL, so a control gated on that flag alone renders live for the frames before the handshake lands, and live forever in any render path whose effect never runs** — briefly operable and permanently useless. `externalClockState` returns unavailable unconditionally; the flag only chooses which TRUE sentence to show. ⚠️ Two P3-U5 pins repointed, not loosened — they counted `disabled` across the whole render as a proxy for six deck verbs, and INT/EXT silently made the proxy answer a different question. |
| P11-2-a | build | **TAP has no keyboard door** | todo | **⟦PORT⟧** donor: `TapTempoEngine.swift` driven from `HotkeyManager.swift`'s global monitor, which exists precisely so transport chords survive a focused TextField — the same hazard this row's gate names. The one live control in the cluster is mouse-only, and tapping tempo with a mouse is the awkward way to do it — every instrument binds it to a key or a pad. `commands/keymap.ts` is the home, and P7-K0's chord work is the vehicle. gate: a key taps the tempo with the plane focused, and does NOT fire while the map-name field has focus |
| P11-2-b | spec | **`getMidiClockStatus` is specified and unimplemented — decide which way it resolves** | todo | **⟦PORT⟧** donor: `getMidiClockStatus` → `WebMidiCommandBinding.swift:29` (`WebEngineLink.swift:440`), on `MIDIManager.swift` (661 L) + `NativeMidiClockOut.{cpp,hpp}` + `MIDIOutputEngine.swift`. **External clock is implemented and shipped there** — so "implement it" is a port with a reference, not a design. Measured for P11-2 and re-verified by the conductor: specified once in `schema.ts:605` (locked/bpm/tickCount), handled **zero** times in `SlDispatch.cpp`, absent from `MergedLink.NATIVE_METHODS`. Either implement it (**Lane A / C++** — and EXT becomes live through the single function that gates it) or mark it unimplemented in the schema so the protocol stops promising it. Leaving it is the P11-5 shape this project keeps paying for. ⚠️ Blocks **P8-P1** too |
| P11-2-c | spec | **The tap window's FEEL is unverified by anything but arithmetic** | todo | **⟦PORT⟧** ⚠️ **the row says the feel "is unverified by anything but arithmetic" and "only the real host answers" — but there is a SHIPPED, TUNED reference.** `TapTempoEngine.swift` (51 L, whole file): `tapTimeout = 1.2 s` (vs the merged 2.5 s), minimum 4 taps / 3 intervals, a ROLLING window of 8 (vs the merged fixed 4), and a plain average with only a `>= 0.03 s` sanity filter. ⚠️ Note the merged build is arguably BETTER here — the donor's plain mean is exactly the failure P11-2 documents (one doubled interval pulls 120 to 90) — so this is a constants comparison, not a rewrite. The maths is pinned exactly; whether a 4-tap window and a 2.5 s series timeout feel right under a hand is a judgement only the real host answers. If the walk says it lags a tempo change, `TAP_WINDOW` is the dial; if a slow tap keeps resetting, `TAP_TIMEOUT_MS` is. Both are named constants carrying their reasoning, so this is a one-line change once someone has actually tapped it |
| P11-3 | build | Launch QUANTUM on the bar | **split → superseded by P11-3a · P11-3b · P11-3c · P11-3d** (spec: `docs/specs/quantize.md`) | **PREMISE FALSIFIED: the quantized-launch engine already exists, is sample-exact, and has ZERO callers** — `requestQuantizedLaunch` is compiled into the merged app and reachable from nothing. **A wiring row, not an algorithm row.** ⚠️ `deckQuantizePending`, `launchQuantize` and the whole `dj` topic are inert — **the same falsification applies to the phase preamble.** ⚠️ **The scale is wrong: the engine counts STEPS (1/16) and CYCLES (pattern LCM); there is no bar and no time signature anywhere in it.** Keep `off · 1 · 2 · 4 · 8 · 16 · cycle`. ⚠️ **`refDeck` is REQUIRED because there is no master clock.** Design lives in `docs/specs/quantize.md`. |
| P11-3a | build | **A queued scene NEVER FIRES in WizardMerged — the pad stays lit forever** | done (machine) — **real-host walk pending (user)** | **The ledger's premise held exactly — rare here.** `positionCbs` was declared, added to and deleted from, and **iterated nowhere**; fixed by fanning out from the HotFrame subscription `start()` already owned. ⚠️ **Whole steps, and nothing more** — `stepFrame`/`framesPerStep` stay `0`, and `nativeAudio.ts:221` now states that as a **prohibition on later rows**; sufficient because the consumer is a threshold, not an equality. **The `playing` defect needed a SOURCE, not just a fix:** `playheadStepDeck*` FREEZES when transport stops, so "stopped" and "stopped on step 412" are byte-identical; a step-advance heuristic was rejected and `World.isPlaying` taken instead. `deckClockLive()` is the real answer — **P11-3a-c**. 11 of 12 pins fail on the pre-fix source; the twelfth passed both ways (**the E8g-c shape**) and was strengthened. ⚠️ **The fan-out EXPOSES a second defect it did not cause — P11-3a-b.** |
| P11-3a-b | build | **A queued scene on strip B/C commits against DECK 0's clock** | todo — **found by P11-3a; land before P11-3b/c** | **⟦PORT⟧** donor: per-deck publish is explicit — `AudioEngineFacade.publish(deckA:…deckB:…deckC:…)` `:1444-1464` carries `launchArmedA/B/C`, and `QuantizePollTracker` (`DJModeView.swift:574`) is per-sequencer. The donor never had one shared step. `EnginePosition` carries one `step`, sourced from `playheadStepDeck0`, but `companionEngine.ts:963-979` iterates **all three decks** against it — its own comment admits they "will not share a boundary". Harmless in the browser (one deck by definition); on the plane `Plane.tsx:627` arms real per-strip decks, so a scene queued on B or C switches on **A's** grid, and a stopped deck 0 gates B/C entirely via the `pos.playing` early-return. ⚠️ **P11-3a exposed this rather than causing it** — before that commit no deck fired at all. Needs a per-deck position: `EnginePosition.deck?: number` with the sink fanning out once per deck and the consumer filtering, or `position(deck)` on `WorldSink`. Touches `scoopyAudio.ts`, `nativeAudio.ts` **and `companionEngine.ts`** — sequence against whoever holds that claim. gate: real host — a queued pad on strip B switches on **B's** cycle, with strip A stopped |
| P11-3a-c | build | **Publish `deckClockLive` so the sink reads transport from the ENGINE** | todo — C++, **Lane A** | **⟦PORT⟧** `deckClockLive()` is the donor core's own accessor, already vendored and already used at `sl_engine.cpp:1454`; only the wire is missing. `position().playing` currently mirrors the last published `World.isPlaying` — last *commanded*, not a readback — because no HotFrame slot carries transport and `playheadStepDeck*` freezes rather than resetting. The engine knows: `NativeAudioEngineCore::deckClockLive()` is already used at `sl_engine.cpp:1454` to blank the DJ track playheads, but it is not on the wire. Costs a HotFrame slot + regeneration + a schema bump moving all three hosts. Would also let a REFUSED publish stop lying. gate: ctest — a stopped deck reports live=0 while its playhead slot holds its last step |
| P11-3b | build | The quantized-launch **ABI**, through `slengine` to the core that already implements it | todo — needs P11-3a | **⟦PORT⟧** ⚠️ **exact signature already shipped**: `AudioEngineFacade.requestQuantizedLaunch(deck:refDeck:quantizeSteps:)` `:1619` → `nativeBridge.requestQuantizedLaunch(forDeck:refDeck:quantizeSteps:)` `:1623`, cancel at `DJModeView.swift:960`, `launchArmed` surviving publish at `:1457`. This is a transcription. `sl_deck_request_quantized_launch(e, deck, ref_deck, quantize_steps)` + a cancel, forwarding to the core's existing `requestQuantizedLaunch`/cancel (`NativeAudioEngineCore.cpp:3714-3724`); `launchArmed` must survive `sl_snapshot_begin` instead of being hardcoded false at `sl_engine.cpp:1234`; and `deckQuantizePending` must be published from the real `launchFiredSequence(deck)` counter rather than a constant. ⚠️ C++ → **Lane A**. Keep the donor's guarantees: a held deck stays warm (its stretcher keeps running), the boundary is CYCLE-relative not clock-relative, and it waits an extra block rather than align to a grid about to move. gate: ctest — an armed deck's step 0 lands on the reference's boundary sample |
| P11-3c | build | The QUANTUM **control**, and the sync-master strip it needs | todo — needs P11-3b | **⟦PORT⟧** ⚠️ **with a DELIBERATE DIVERGENCE that should be stated as one.** Donor: `LaunchQuantize` (`DJModeView.swift:96-105`) — **ONE GLOBAL quantize for both deck AND track launch**, default `.cycle`, persisted as `djMode.globalLaunchQuantize` `:176`; and **the reference deck is chosen automatically**, `armQuantizedLaunch` `:933-954` taking the first OTHER audibly-playing, non-pending deck with `steps = quantize.steps ?? ref.patternLCM`. quantize.md §4.2's explicit sync-master strip REPLACES a shipped answer — the reason (making the donor's silent degradation a visible condition) is good, but it is a divergence and the user should see it as one. Scale `off · 1 · 2 · 4 · 8 · 16 · cycle` (quantize.md §4.1), value on the master bar, **reference on the strip**. ⚠️ Cover the donor's two silent degradations: `armed == 0` holds a deck silent INDEFINITELY, and arming against a stopped or self deck fires immediately — both must be visible states, not surprises. ⚠️ Resolves P11-3's pads/SCN collision as: **the pads own WHICH scene, QUANTUM owns WHEN** — `GridScenes` is live and already queues on the LCM, while `ScenePads`/SCN are dead at every layer (P7-K7), so SCN must not return as a third spelling. gate: real host — the quantum changes when a scene lands, and the master strip is named on screen |
| P11-3d | build | The looper says whether its loop is **cycle-aligned** | todo | **⟦NEW⟧** confirmed — the donor has no looper at all, exactly as the row already says. There is NO donor answer — ScoopyLoops has no looper at all (its "tape" is a DJ reverse-hold), so this is a decision, not a port. Today: a loop is a buffer with in/out FRAMES, record-stop makes the loop exactly the captured count (`sl_tape.cpp:453-480`), and sync changes the RATE of a fixed region — **it never re-cuts the loop**. So a loop that is not a whole number of cycles will not stay aligned and **no amount of launch quantization fixes it**; launching on a boundary only moves where the drift starts. RULED (quantize.md §4.4): **infer and report, never alter** — show the inferred bar count from the existing `inferTapeBpm` and mark a loop that is not cycle-aligned. Explicitly **NOT** silent trimming: editing audio the user played is discovered months later. Quantized CAPTURE (`sl_tape_record_start_quantized`, spec'd at `SL-ABI-V3.md:168-174`, NOT BUILT) is the real answer and rides with P7-P1 when the user opens it. ⚠️ **Unit clash to settle first:** that API's `division` is a DENOMINATOR (1 = bar) while the launch engine's `quantizeSteps` is a STEP COUNT (16 = bar) — the same numeral means opposite things. gate: a non-aligned loop says so |
| P11-4 | build | The crossfader returns, sides ASSIGNED per strip (D-SL-XFADER-01) | todo | **⟦PORT⟧** ⚠️ **shipped whole, and the row's scope is NARROWER than the donor's.** `XfaderSide { own, a, b }` (`DJModeView.swift:201-213`) with `deckA/B/CXfaderSide` `:179-181` — **plus `returnXfaderSide(index)` `:185` and `inputXfaderSide` `:186`**: the FX returns and the audio input take a side too, which this row does not mention. Also `crossfaderEngaged` `:187` and the whole X-MIX block `:188-192`. Two shipped decisions to weigh: the third state is **`own`** ("plays at its own volume, ignores the crossfader") rather than "neither", and it is persisted **GLOBALLY in UserDefaults, deliberately NOT in the session file** — the donor's own comment at `:182-184` says why ("global performance routing"). That bears directly on this row's MAP-schema-bump plan. The engine already carries `crossfaderEngaged`/`crossfaderPosition`; what is missing is WHICH STRIPS it acts on, because D-SL-MORPH-01 retired the deck-pair model. Needs a per-strip document field (side A / B / neither) + an assignment item in the strip `⋯` menu → **MAP schema bump + migration** (default: neither, so an existing map sounds identical). Sequenced last of the builds for that reason. gate: audible fixture — fader at A silences B-side strips and not neither-side ones; migration pinned both directions |
| P11-5 | build | HEALTH: dropout count + CPU (D-SL-HEALTH-01) | done (machine) — **real-host reachability UNPROVEN, walk pending (user)** | **`HealthReadout` in the master bar**, right of LIM: `DSP 3%` → `DSP 94% ✕2` once anything has overrun. **Labelled DSP, not CPU, deliberately** — `PerfPanel`'s "CPU" is PAINT cost by its own docstring, and **that word collision IS the mechanism of the failure**. Two rules pinned as pure functions: **the count only ever goes up** (the engine's counter restarts when the device reopens; the latch refuses to follow it down) and **a dropout is sticky-hot** until acknowledged. **`—` before the first HotFrame, never `0%`.** `CpuMeter.tsx` RETIRED, and `uiOwnership.test.ts` now reads `plane/` too. ⚠️ **Rate and block size NOT shipped, on purpose — P11-5a.** ⚠️ **The row's original claim that "nothing in the tree reports any of this" was wrong at every layer** — the counter was computed, published and pinned, and a widget already read it. **The whole gap was a door** — rule four exactly. |
| P11-5a | build | **Publish the device sample rate + the REAL block size**, so the health readout can say `48k · 512` | todo | **⟦PORT⟧** donor: the device block size is real — `NativeJuceDeviceHost.{cpp,hpp}` carries it, and `setAudioBufferFrames` is a first-class verb (`WebEngineLink.swift:433` → `WebAudioCommandBinding.swift:37`). ⚠️ Confirms the row's own warning that `preferredBufferFrames` is a PREFERENCE, not the readback. P11-5 shipped `DSP n%` and deliberately did NOT show either, because neither is reachable: there is no scalar for them in `HOT_FRAME_SCALARS`, and `AudioIO::open` (`host/src/AudioIO.cpp:27-29`) sets `setup.sampleRate` but **never** `setup.bufferSize` — so the device block size is whatever CoreAudio chose and nothing reads it back. `max_block_frames = 512` is the ENGINE's chunk, not the device's callback size, and printing it would put a lying number on the one readout that must be trusted absolutely. ⚠️ `enumerateAudioDevices.preferredBufferFrames` is not the answer twice over: it is a PREFERENCE (0=auto/128/256/512), and it has zero implementation in `shell/`/`host/`/`slengine/` — a scoopy-Swift-era surface the merged host does not answer. Fix: APPEND `deviceSampleRate` + `deviceBlockFrames` fed from `device->getCurrentSampleRate()` / `getCurrentBufferSizeSamples()`, regenerate via `hotframe:generate` (never hand-edit), and have `healthView` prefix them. ⚠️ Appending touches the schema, so this is a **Lane A / C++ row** and the bump must move all three hosts `schema:check` compares. gate: `hotframe:check` + the readout shows the device's ACTUAL block size after a device switch, not 512 |
| P11-5b | build | ~~The health readout must survive P11-1's three zones~~ | **done — shipped inside P11-1 (2026-07-30)** | P11-1 placed `HealthReadout` **beside LIM in the output zone**, as this row required, and pinned it against RENDERED MARKUP (verified to fail when the readout is moved back to the centre zone) — **deliberately not by a source grep, which is satisfied by two things merely sharing a file**. Because it stayed inside `Master.tsx`, `uiOwnership.test.ts`'s pin needed no repointing. Nothing outstanding — this row was still reading `todo` against work that had already landed |
| P11-5c | fixture | ~~`sessionStore.test.ts`'s byte-for-byte round-trip flakes under full-suite load~~ | **merged into P11-5d 2026-07-30 (Lane C audit)** | The cause was found: it is P11-5d's fixture write, and the row's own note already said "fix P11-5d and this closes with it; do not chase it separately". Its measurements (11098 ms under load vs 777 ms alone, 1-in-5) are carried there. Kept as a pointer so the id does not vanish from a search |
| P11-5d | delete | **The vitest suite MUTATES a committed fixture on every run** | **done (2026-07-30)** — closes P11-5c with it | The write was the symptom; the cause was in the packer. `packSession` let fflate stamp each entry with `Date.now()`, and **DOS timestamps tick every 2 s** — so packing the *same* package 2.5 s apart returns **10 differing bytes of 541,935**, at exactly the 10 zip-header offsets. Byte-for-byte the diff the fixture showed after every run. ⚠️ **This also means P11-5c's recorded cause was wrong** — nothing in the TS suite reads `session-from-ts.zip` at all; `sessionStore.test.ts` reads `session.zip`, which nothing writes. The flake is the same stamp: pack, work, pack again, red whenever the gap straddles a tick (~40 ms idle, but 5.6–9.9 s under load). Fixed with `SESSION_ZIP_MTIME` — a **local-time string, not an epoch**, because fflate encodes DOS date/time through `getFullYear()/getHours()`, so a fixed *instant* would encode differently per timezone and the committed fixture would false-red for anyone outside this one. Verified byte-identical UTC-8 → UTC+14. **Both approaches, because the pin makes them cheap:** the suite *compares*, and `session:generate` is the only writer — so **the comparison IS the rot guard**, reddening the moment `packSession` moves and naming the script. Comparisons now report a first-differing **index** rather than `toEqual` on two half-megabyte arrays. ⚠️ The lane declined to claim the flake fixed on the grounds it never saw it, resting on mechanism instead: the compared outputs are now bit-identical regardless of elapsed time. **Impossible, not merely unobserved.** gate MET (conductor): `npm test` ×2, no fixture in `git status` either time |
| P11-5d-a | spec | **⟦NEW⟧ The handshake's Swift half cannot be verified from here** | todo | `session-from-ts.zip` is an **export**, `session.zip` a frozen **import**, and no `.swift` is tracked in this repo — so a change in `SessionZip.swift` is invisible to every gate here and the interop proof silently becomes two tests of one language. ⚠️ Now checkable: `../scoopyloops/ScoopyLoopsTests/` is the donor's suite — confirm whether the session-zip tests live there and record how to run them, or pin the pairing with a version marker inside both archives |
| P11-5d-b | fixture | **⟦NEW⟧ A test that blocks the event loop cannot time out** | todo | Proved: `sessionStore.test.ts` under `--testTimeout=2000` ran **6931 ms and passed** — its awaits resolve as microtasks against the in-memory fake OPFS, so timers starve. **No CPU-bound test over a fake backend in this suite can ever fail for taking too long**; a genuine hang shows as a hung gate rather than a red one |
| P11-5d-c | spec | **⟦NEW⟧ `session:generate` is deliberately NOT an eleventh drift gate** | todo (decision, no work) | Recorded so a later session does not "fix" it. The freshness check runs inside `npm test` — so it fires on every gate rather than only when someone remembers a script |
| P11-5d-d | build | **⟦NEW⟧ `session-pattern.json` is 540 KB, and it is ~⅓ of the suite's wall-clock** | todo | The five heaviest tests are zod passes over this one fixture. It is the real desktop artifact, so shrinking it trades fidelity for speed — a deliberate call, not a drive-by |
| P11-6 | build | CAPTURE: the main mix to the library (D-SL-CAPTURE-01) | todo | **⟦PORT⟧** donor: `capture` → `WebCaptureBinding.swift:85` (`WebEngineLink.swift:559`), on `GlobalAudioRecorder.swift` (267 L) + `ToolbarRecorder.swift` (322 L) + `CaptureWindowController.swift`. ⚠️ **And one file this row does not mention: `CrashSafeWAVWriter.swift`** — the donor solved crash-safety for exactly this recorder, which matters more for a main-mix capture than for anything else in the app. One verb: arm/stop, elapsed time on the button while it runs, the file lands beside a looper's takes. The recorder EXISTS — this needs a main-mix source and a door, and **P3-F3's lesson applies directly: ask the engine for the take's channel count, never infer it from the request** (a bus/main capture is always stereo, and inferring it mono corrupted every take and the heap). `activeIsBouncing` stays unimplemented, recorded. gate: fixture — capture a known tone off main, the WAV has energy at the right rate and width; walk — the button shows elapsed time and the file appears in the library |
| P11-AUDIT | spec | Diff this section against the mockup sheet's Plate 5 + `ToolbarUiState`'s 23 keys; materialize gaps | todo | **⟦PORT⟧** every named carry-over has a donor answer. **solo** — `GlobalToolbarView.swift:242-290`: per-deck output mute/solo AND per-send solo, with the load-bearing law that **deck mute is applied AFTER the per-deck send tap, so a muted deck still feeds its FX sends** (that is what makes "solo a send, mute the decks" produce only the wet). **INPUT channel** — `MicChannel` `:381` (gain · monitor · mute · 4 sends), metered pre-gate. **bounce** — `isBouncing` `ScoopyLoopsApp.swift:1337`. **per-deck output pairs** — `setDeckOutputChannels` (`WebEngineLink.swift:433`). Named carry-overs to rule on: **solo** (deck AND send — the plane has mute and no solo on any surface), the **INPUT channel** (`mic`: gain/monitor/mute/4 sends), **bounce**, **per-deck output pairs**. Each ends the audit with a row or a written reason |
| P11-G1 | gate | **Human, real host:** tempo from TAP, a scene launching on the quantum, the crossfader moving assigned strips, CAPTURE producing a playable file, and the health readout counting a real dropout | awaiting-user | on the user's machine, after P11-AUDIT |
