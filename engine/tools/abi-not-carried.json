{
  "_doc": "Boundary fields declared in web/protocol/schema.ts that the engine does NOT yet carry. Every entry is a REAL gap (the field is inert in the running app until carried), not a shrug. Enforced by web/scripts/checkAbiCoverage.ts. Format: name -> reason.",
  "params": {
    "gain": "per-channel param storage lands with the world builder (P1-03); until then only master-global mainGain is carried",
    "pan": "per-channel param storage lands with the world builder (P1-03)",
    "mute": "per-channel param storage lands with the world builder (P1-03)",
    "solo": "per-channel param storage lands with the world builder (P1-03)"
  },
  "hotframe": {}
}
