/**
 * Root component — P0-02 scaffold placeholder. The real console/strip surface
 * and the EngineLink connection state land in later P0 increments (transport in
 * P0-07). For now this just proves the React + Vite + TS toolchain builds and
 * mounts.
 */
export function App() {
  return (
    <main>
      <h1>Wizard</h1>
      <p>The patchbay-recorder. Walking skeleton — engine not yet connected.</p>
    </main>
  )
}
